schema_version: "1.1"
name: one-call-not-agent
context:
  scaffold_script: "mkdir -p lib && printf 'module.exports = { widget: 1 };' > lib/thing.js && git init -q && git add -A && git -c user.email=eval@fankeel -c user.name=eval commit -qm init"
