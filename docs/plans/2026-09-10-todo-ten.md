---
status: design-intent
---

# TODO 十條一次清 Implementation Plan

**Goal:** 清掉 `TODO.md` 的 `## Ready` 一條與 `## Needs a decision` 九條，九條的決定已在 design 閘門逐條取得。

**Architecture:** 八個 task。六個是「檔案加測試」可以各自派人；Task 3 與 Task 4 共用 `lib/stages.js`，Task 4 要等 Task 3 落地後量位元數，所以串成一組；Task 8 是三處已知文字的編輯，留在 session 裡做。design class 那條不做，只搬到正確的 heading。

**Tech Stack:** Node.js，`node --test`，零執行期相依。`node:util` 的 `parseArgs`、`node:fs`、`node:path`。eval case 由 `scripts/eval.js` 透過 headless `claude -p` 執行，只在 verify 跑。

**Spec:** [2026-09-10-todo-ten-design.md](2026-09-10-todo-ten-design.md)

## Global Constraints

從 `CONTRIBUTING.md`、`package.json`、`.fankeel/map.md` 與測試套件取得，值照抄：

- 測試指令是 `npm test`，展開為 `node --test`。`package.json` 沒有 `dependencies` 也沒有 `devDependencies`，**不得新增任何相依**。
- **新的測試檔與新的 `lib/`、`scripts/` 檔必須先 `git add` 才會被 `tests/source.test.js` 看見**：它讀 `git ls-files`，未 staged 的新檔案掃不到，每個匯出的名字都要有 importer。
- 測試要暫存目錄一律經由 `tests/tmp.js` 的 `tmp(prefix)`，不得自行 `mkdtempSync`。
- `lib/*.js` 是純函式，**不得反向引用 `scripts/` 或 `hooks/`**；只能由 `scripts/` 與 `hooks/` 引用 `lib/`。`scripts/*.js` 是 `lib/` 的薄包裝。
- **`hooks/` 底下不得新增檔案**（`CONTRIBUTING.md:35`）。本計畫一支都不加。
- 每個 stage 的注入 block 上限是 **2400 字元**（`tests/render.test.js:546`，`size < 2400`），量的是字元不是位元；Task 4 的 `budget` 用 UTF-8 位元數，是另一把尺，兩者都要過。
- `tests/render.test.js:346` 用 `WORDS[ALWAYS.length]` 釘 ALWAYS 的條數：**不得加第五條**，只能在既有四條裡加字。
- `tests/agents.test.js:35` 釘 `plugin.json` 的 `agents` 與 `agents/` 目錄 exact-set；Task 1 加第三個時兩邊一起改。
- 文件歸檔照 `.fankeel/map.md`：`docs` 是 reference、`docs/plans` 是 plan、`docs/decisions` 是 decision、`docs/reports` 是 report、`docs/archive` 是 archive、`evals` 今天是 reference（Task 7 改 `fixture`）。新頁面要進 `docs/README.md` 索引。
- `docs/archive/`、`docs/plans/`、`docs/reports/` 是有日期的紀錄，**不得事後改寫內容**；Task 5 只在 `*-provenance.txt` 末尾追加四行，不改既有行。
- `TODO.md` 的規則由 `scripts/todo-check.js` 執行：`## Waiting` 每條結尾是 `lifts when: <event>. MM-DD`；連結不得落在 plan、decision、report、archive 四種 role 的檔案上；每條有長度上限。
- 基準是 `84c4346`，`ℹ pass 1289 / fail 0`，`docs-check` 與 `todo-check` 都 exit 0，存於 `.fankeel/build/2026-09-10-todo-ten/baseline-test.txt`。收尾時通過數不得低於 1289，兩個 check 仍 exit 0。
- 版號一律跑 `scripts/version.js`，不手改 — 本計畫不動版號，land 才動。
- agent 檔與 hooks 清單在 Claude Code 啟動時讀一次；Task 1 的新 agent 要在**新開的終端**才派得到，verify 量 `modelUsage` 時要記得。

## File structure

| 檔案 | 負責什麼 |
|---|---|
| `agents/fankeel-reviewer.md` | 新。build 的 per-task reviewer 與 verify 的 adversary 共用的 agent：只讀、sonnet |
| `.claude-plugin/plugin.json` | manifest。`agents` 陣列加第三個 |
| `tests/agents.test.js` | agents 的 exact-set 測試，`NAMES` 加一 |
| `tests/inventory.test.js` | 新。skills 目錄與 hooks 檔的 exact-set |
| `lib/stages.js` | 規則本體。Task 3 改 `ALWAYS[2]`、build 第三條與兩處 reviewer 措辭；Task 4 每 stage 加 `budget` |
| `skills/fankeel/SKILL.md` | 主 skill。Task 3 加 `## Calibration` 一節 |
| `tests/stages.test.js` | 規則的既有測試。Task 3 加 reachability test |
| `lib/stage-registry.js`、`scripts/stage-registry.js` | 新。從 `lib/stages.js` 與七個 stage skill 產生 `skills/registry.json` |
| `skills/registry.json` | 新，generated。每 stage 一筆 |
| `tests/stage-registry.test.js` | 新。重產生比對、預算斷言 |
| `docs/reports/evidence/*/*-provenance.txt` | 七個。各追加四行 |
| `evals/<case>/` | 四個新目錄 |
| `lib/docs.js`、`.fankeel/docs.json`、`docs/documents.md`、`tests/docs.test.js` | role `fixture` |
| `docs/collisions.md`、`TODO.md`、`docs/README.md`、`docs/subagents.md`、`docs/pipeline.md`、`README.md` | 文件改寫與索引 |
## Task 1: `fankeel-reviewer` agent

**Files:**
- Modify: `agents/fankeel-reviewer.md` — 新檔，build 的 per-task reviewer 與 verify 的 adversary 共用的契約
- Modify: `.claude-plugin/plugin.json` — `agents` 陣列加第三個路徑
- Modify: `skills/fankeel-build/SKILL.md` — 步驟 5 的 reviewer 段落補一句 dispatch 說明
- Modify: `skills/fankeel-verify/SKILL.md` — adversary 段落補一句 dispatch 說明
- Modify: `docs/subagents.md` — 「兩個 agent」改寫成「三個」，補一段 reviewer 與 reader 的差別
- Test: `tests/agents.test.js` — `NAMES` 加第三個名字，兩個測試的名字從 both 改寫
- Read: `agents/fankeel-reader.md` — frontmatter 形狀與正文語氣的範本
- Read: `agents/fankeel-judge.md` — 另一個既有 agent，一併比對語氣與 Refusals 段落的寫法

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 每一處編輯都附著它要替換的確切舊文字與確切新文字，新檔 `agents/fankeel-reviewer.md` 的全文也在這裡；抄寫、跑測試。

步驟：

1. 先寫失敗的測試。在 `tests/agents.test.js` 第 10 行，把

   ```
   const NAMES = ['fankeel-reader', 'fankeel-judge'];
   ```

   換成：

   ```
   const NAMES = ['fankeel-reader', 'fankeel-judge', 'fankeel-reviewer'];
   ```

   同一個檔案第 23 行，把 `test('both agents parse, name themselves after their file, and cannot edit', () => {` 換成 `test('every agent parses, names itself after its file, and cannot edit', () => {`。

   同一個檔案第 35 行，把 `test('the manifest ships both, and no others', () => {` 換成 `test('the manifest ships them all, and no others', () => {`。

   測試本體一行都不動——兩個測試原本就用 `for (const name of NAMES)` 與 `manifest.agents` 逐一比對，`NAMES` 變長它們自動吃到第三個名字。

2. `npm test` 看它紅：`agents/fankeel-reviewer.md` 還不存在，第一個測試裡 `front()` 對它呼叫 `fs.readFileSync` 會拋出 ENOENT；第二個測試裡 `manifest.agents`（兩個路徑）跟 `NAMES.map(...)`（三個路徑）對不上，`assert.deepEqual` 也紅。

3. 新增 `agents/fankeel-reviewer.md`，全文：

   ```markdown
   ---
   name: fankeel-reviewer
   description: Read-only reviewer for build's per-task review and verify's adversary — reads a diff, a brief or an evidence table against what it was supposed to prove, and returns only what it defeats and why. Cannot call Edit, Write or NotebookEdit.
   tools: [Read, Grep, Glob, Bash]
   model: sonnet
   status: current
   last_verified: 2026-09-10
   source_of_truth: lib/render.js
   ---

   You are a reviewer. The session that sent you has a brief, a range or a
   table, and a question already answered once; you read for what still
   stands and what does not, and you return only what you defeat.

   ## Job

   Read what the brief names — a brief file, a pinned `git` range, an
   evidence table — and hold it against the claim it was supposed to prove.
   Build's per-task reviewer and verify's adversary are the same contract
   read over two different shapes: a diff against a brief, or a table
   against the claims it carries. Everything you open is spent in a context
   that is thrown away; what you return lands in the parent's and stays
   there for the rest of its session, so say only what you defeat, and why.

   ## Tools

   `Read`, `Grep`, `Glob` and `Bash`. `Bash` is here for `git` and nothing
   else mutates through it: inspect with `git show`, `git diff` and `git
   log` only — never `git commit`, `git checkout`, `git add`, `git merge`
   or anything else that changes the working tree, the index, `HEAD` or
   branch state. `Edit`, `Write` and `NotebookEdit` are not on the list and
   cannot be called.

   ## Refusals

   - Do not change a file, the index, `HEAD` or branch state, by any tool.
     If the question cannot be answered without changing one, say so and
     stop.
   - Do not dispatch a subagent of your own.
   - Do not answer from memory what a diff or a table would say — open it,
     or say you did not.
   - Do not praise, and do not restate what already holds. A clean pass is
     the single word `clean`, not a summary of what was fine.

   ## Return

   Only what you defeat, and why — one line per finding, most serious
   first, or the single word `clean`. Every line you return stays in the
   parent's context for the rest of the session.
   ```

4. 在 `.claude-plugin/plugin.json` 第 11 行，把 `"agents": ["./agents/fankeel-reader.md", "./agents/fankeel-judge.md"],` 換成 `"agents": ["./agents/fankeel-reader.md", "./agents/fankeel-judge.md", "./agents/fankeel-reviewer.md"],`。

5. `npm test` 看它綠：`agents/` 底下現在有三個檔案對上 `NAMES` 的三個名字，`manifest.agents` 也對上。

6. 在 `skills/fankeel-build/SKILL.md`，第 5 步 reviewer 模板後面那句，把

   ```
      Give it the brief path and the range — never a paste of the session's
      history.

      **When the user has said, this session, not to dispatch**, the reviewer runs
   ```

   換成：

   ```
      Give it the brief path and the range — never a paste of the session's
      history. Dispatch it as `subagent_type: fankeel-reviewer`; the model
      comes from that agent file, not typed by hand here.

      **When the user has said, this session, not to dispatch**, the reviewer runs
   ```

   模板本身（`THE RANGE` 起的那個引號區塊）一行不動——它帶的是每次不同的 range、brief 與 rows，這句加在模板外面。

7. 在 `skills/fankeel-verify/SKILL.md` 的 `## The adversary` 段落，把

   ```
   It gets **paths, never a paste**, and is asked only for the rows it defeats — say
   why, because every line it returns lands here and is re-read on every later turn.

   It **reads the method rather than probing it.** Red-green belongs to this session
   ```

   換成：

   ```
   It gets **paths, never a paste**, and is asked only for the rows it defeats — say
   why, because every line it returns lands here and is re-read on every later turn.
   Dispatch it as `subagent_type: fankeel-reviewer` too — the same agent as
   build's per-task reviewer — and let its file pin the model rather than
   typing one here.

   It **reads the method rather than probing it.** Red-green belongs to this session
   ```

8. 在 `docs/subagents.md` 第 28 行起，把

   ```
   ## The two agents this plugin defines

   Two subagent types are not just described in prose — they are declared as
   `agents` in `.claude-plugin/plugin.json` and shipped as files under `agents/`:
   `fankeel-reader` and `fankeel-judge`. Both carry `tools: [Read, Grep, Glob,
   Bash]` — Edit, Write and NotebookEdit are simply absent from the list, so
   calling either to change a file is refused by the harness rather than left to
   a rule somebody has to remember. Bash stays on the list for `git` and this
   plugin's own scripts, a named residual rather than a claim that either agent
   cannot write anything.
   ```

   換成：

   ```
   ## The three agents this plugin defines

   Three subagent types are not just described in prose — they are declared as
   `agents` in `.claude-plugin/plugin.json` and shipped as files under `agents/`:
   `fankeel-reader`, `fankeel-judge` and `fankeel-reviewer`. All three carry
   `tools: [Read, Grep, Glob, Bash]` — Edit, Write and NotebookEdit are simply
   absent from the list, so calling any of them to change a file is refused by
   the harness rather than left to a rule somebody has to remember. Bash stays
   on the list for `git` and this plugin's own scripts, a named residual rather
   than a claim that any of the three cannot write anything.
   ```

   這一段行數不變（10 行換 10 行），後面所有行號不移位。

9. 同一個檔案，把第 70–72 行——`does not rather than inventing one...` 那句、接著的空白行、再接著 `## Why this is the best-value text in the plugin` 這個 heading——換成（新段落插在空白行與 heading 之間）：

   ```
   does not rather than inventing one (`scripts/judge.js:50-66`, `indexRow`).

   `fankeel-reviewer` runs at `model: sonnet`, and answers neither kind of
   question above: it is the shared contract behind build's per-task reviewer
   and verify's adversary, both of which dispatch `subagent_type:
   fankeel-reviewer` instead of writing a model by hand. What tells it apart
   from `fankeel-reader` is not the tool list — both carry the same four — but
   the shape of the question: a reader is asked what a file says, a reviewer
   is asked what a diff or a table gets wrong, and it returns only what it
   defeats. This is also the only place `dispatch.floor` is actually enforced
   by the harness rather than by a rule someone has to remember:
   `SubagentStart`'s payload carries only `agent_id` and `agent_type`
   (`docs/subagents.md:354`), so a hook cannot see, and cannot check, what
   model a dispatch asked for — the agent file's `model: sonnet` is the one
   pin the harness itself reads before launch.

   ## Why this is the best-value text in the plugin
   ```

   這段插入 14 行（13 行正文加 1 行分隔的空行），第 72 行以後的每一行都往下移 14 行——`docs/subagents.md:340` 那句「The field for it is `agent_id`」現在在第 354 行，上面引用的行號就是插入後的位置，不是插入前的。跑 `grep -n "The field for it is" docs/subagents.md` 確認真的落在第 354 行；不是的話，行號照 `grep` 的結果改，不要用心算的數字。

10. `node scripts/docs-check.js`，確認沒有因為這次插入多出新的 `past-end` 或壞掉的引用——`docs/subagents.md` 目前沒有其他檔案引用它的行號（已用 `grep -rn "subagents\.md:[0-9]" docs/*.md skills/ agents/ output-styles/` 查過，扣掉 `docs/archive`、`docs/reports`、`docs/plans` 之後是空的），所以這次位移只影響第 9 步自己新加的那個引用。

11. `npm test` 看整套仍然綠：這一步的 SKILL.md 與 `docs/subagents.md` 改動沒有測試覆蓋，跑全套是為了確認第 1–5 步的紅綠沒有被後面的編輯弄壞。

    附註：`docs/README.md:152` 那一列「the two agents the plugin ships」在這個 task 落地後會少算一個，但設計沒有把 `docs/README.md` 列進這個 task 的檔案清單——本 task 不動它，留給之後的稽核或另一個 task 處理。

12. Commit：`feat: add the fankeel-reviewer agent`

## Task 2: exact-set 推廣到 skills 與 hooks

**Files:**
- Modify: `tests/inventory.test.js` — 新檔，全文
- Test: `tests/inventory.test.js` — 新檔，全文
- Read: `tests/agents.test.js` — 新測試檔案風格的範本（`'use strict'`、`node:test`、`node:assert/strict`、`ROOT`）
- Read: `lib/stages.js` — `NAMES` 在 `:440`（`const NAMES = STAGES.map((s) => s.name);`），在 `:574` 匯出，不改
- Read: `.claude-plugin/plugin.json` — `hooks` 區塊裡每個 `command` 字串是 `${CLAUDE_PLUGIN_ROOT}/hooks/x.js` 的形，不改
- Read: `hooks/hooks.json` — 不存在；manifest 的 `hooks` 區塊是唯一來源，測試不讀它

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 名單已經從 `skills/`、`hooks/` 目錄與 `lib/stages.js` 逐一點過，新檔全文也在這裡；抄寫、跑測試、示範一次紅。

盤點時查過的事實，寫進測試前先記下：`skills/` 底下今天有 `fankeel`、`fankeel-ask`、`fankeel-audit`、`fankeel-build`、`fankeel-design`、`fankeel-land`、`fankeel-plan`、`fankeel-survey`、`fankeel-verify` 九個目錄，每個都有 `SKILL.md`；`lib/stages.js` 的 `NAMES` 是 `survey`、`design`、`plan`、`build`、`verify`、`audit`、`land` 七個，`fankeel-<stage>` 七個目錄都在上面那九個裡；`hooks/` 底下是 `brief.js`、`carry.js`、`gate.js`、`guard.js`、`inject.js`、`leave.js`、`resume.js`、`touch.js` 八支，`.claude-plugin/plugin.json` 的 `hooks` 區塊剛好引用這八個檔名，`hooks/hooks.json` 不存在。四個斷言此刻都成立——這支測試鎖的是「現在」，不是修一個已知的錯。

步驟：

1. 新增 `tests/inventory.test.js`，全文：

   ```js
   'use strict';

   const test = require('node:test');
   const assert = require('node:assert/strict');
   const fs = require('node:fs');
   const path = require('node:path');

   const ROOT = path.join(__dirname, '..');

   // Hardcoded, not derived from the filesystem — an extra directory or an
   // extra `.md` has to fail this test by name, not slide past it because
   // the list was generated from the very thing being checked.
   const SKILLS = [
       'fankeel',
       'fankeel-ask',
       'fankeel-audit',
       'fankeel-build',
       'fankeel-design',
       'fankeel-land',
       'fankeel-plan',
       'fankeel-survey',
       'fankeel-verify',
   ];

   function setDiff(actual, expected) {
       const a = new Set(actual);
       const e = new Set(expected);
       return {
           extra: actual.filter((x) => !e.has(x)),
           missing: expected.filter((x) => !a.has(x)),
       };
   }

   test('skills/ holds exactly the known directories, sorted', () => {
       const actual = fs.readdirSync(path.join(ROOT, 'skills')).sort();
       const expected = [...SKILLS].sort();
       const { extra, missing } = setDiff(actual, expected);
       assert.deepEqual(actual, expected,
           'extra: ' + JSON.stringify(extra) + ', missing: ' + JSON.stringify(missing));
   });

   test('every listed skill directory has a SKILL.md', () => {
       for (const name of SKILLS) {
           const file = path.join(ROOT, 'skills', name, 'SKILL.md');
           assert.ok(fs.existsSync(file), name + ' has no SKILL.md');
       }
   });

   test('lib/stages.js exports NAMES, and every stage has its own skill directory', () => {
       const stages = require('../lib/stages.js');
       assert.ok(Array.isArray(stages.NAMES), 'lib/stages.js does not export NAMES as an array');
       assert.ok(stages.NAMES.length > 0, 'NAMES is empty');
       assert.ok(stages.NAMES.every((n) => typeof n === 'string'), 'NAMES holds something other than strings');
       const missing = stages.NAMES
           .map((stage) => 'fankeel-' + stage)
           .filter((dir) => !SKILLS.includes(dir));
       assert.deepEqual(missing, [], 'stage(s) with no skills/ directory: ' + JSON.stringify(missing));
   });

   test('hooks/*.js matches exactly what plugin.json references', () => {
       // plugin.json is the source of truth for which hooks run. A
       // hooks/hooks.json would be a second one, and there is none today —
       // if this ever exists, read the hook list from there instead.
       assert.ok(!fs.existsSync(path.join(ROOT, 'hooks', 'hooks.json')),
           'hooks/hooks.json exists now — read the hook list from there, not from plugin.json');

       const hookFiles = fs.readdirSync(path.join(ROOT, 'hooks'))
           .filter((f) => f.endsWith('.js'))
           .sort();

       const manifest = JSON.parse(fs.readFileSync(path.join(ROOT, '.claude-plugin', 'plugin.json'), 'utf8'));
       const referenced = new Set();
       const RE = /\$\{CLAUDE_PLUGIN_ROOT\}\/hooks\/([\w.-]+\.js)/g;
       for (const matchers of Object.values(manifest.hooks)) {
           for (const matcher of matchers) {
               for (const hook of matcher.hooks) {
                   let m;
                   RE.lastIndex = 0;
                   while ((m = RE.exec(hook.command))) referenced.add(m[1]);
               }
           }
       }
       const referencedSorted = [...referenced].sort();
       const { extra, missing } = setDiff(hookFiles, referencedSorted);
       assert.deepEqual(hookFiles, referencedSorted,
           'extra hook file(s): ' + JSON.stringify(extra) + ', missing hook file(s): ' + JSON.stringify(missing));
   });
   ```

2. `git add tests/inventory.test.js` — 未 staged 的新檔案，`tests/source.test.js` 讀 `git ls-files` 時掃不到（見 Global Constraints）。

3. `npm test` 看它綠：四個斷言此刻都對著現在的樹成立，這不是「先紅後綠」的那種測試——它鎖住的是已經真實的樣子，不是修一個已知的錯，所以第一次跑就是綠的。它真的會抓到未來的漂移得靠下一步的破壞性示範。

4. 示範它真的會紅。建立一個空目錄：

   ```
   mkdir skills/zz-probe
   ```

   跑 `node --test tests/inventory.test.js`（只跑這支，避免 `tests/skills.test.js` 也對著同一個多出來的目錄報一輪不相干的紅），看到第一個測試失敗，訊息裡點名 `"extra":["zz-probe"]`。刪掉它：

   ```
   rm -rf skills/zz-probe
   ```

5. `npm test` 看整套回到綠。

6. Commit：`test: pin skills/ and hooks/ to a named inventory`
## Task 3: 校準規則、build 的停止條件，與 reachability test

**Files:**
- Modify: `lib/stages.js` — `ALWAYS[2]` 句尾加一句；build 第三條（`:301`）從「四件事擋下來」改寫成「git 能不能撤掉」的形；`:278`（plan）與 `:300`（build）的 reviewer 措辭改成 `fankeel-reviewer`；`design`、`verify`、`audit`、`land` 各有一到兩條規則跟著改寫或壓縮字數——原因見步驟 3 的 `Ruling:`
- Modify: `skills/fankeel/SKILL.md` — `## Subagents` 之前新增 `## Calibration`：三條通則，底下一張四列表
- Modify: `skills/fankeel-build/SKILL.md` — 第 39 行引的 `` `Decide rather than stall` `` 跟著 `:301` 的新文字改成 `` `Decide` ``
- Modify: `docs/pipeline.md` — 兩份逐字引用 `build` 注入區塊的示範（`### build` 底下與 `# The answer is not a prompt` 之後那份）跟著 `ALWAYS[2]`、`:300`、`:301` 的新文字改
- Test: `tests/stages.test.js` — 新增一句 ALWAYS[2] 的目標斷言、一句 build 第三條的目標斷言、reachability test 本體
- Read: `lib/render.js` — `render()`／`rulesLines()` 怎麼把 `rulesFor()` 的結果組成注入區塊（:122-215）
- Read: `tests/render.test.js` — `sizeAtReference`／`REFERENCE_ROOT`（:441-451）與量每個 stage 用的 `PROFILES`（:536-548），Task 3 的量測照抄這一份，不是重新發明
- Read: `docs/decisions/2026-08-27-gate-rules.md` — `ALWAYS[0]`／`ALWAYS[1]` 上一次改字時，`docs/pipeline.md` 兩份示範怎麼跟著動，這次是同一個模式
- Read: `tests/source.test.js` — 新測試檔要先 `git add` 它才看得到，不改

**Interfaces:**
- Consumes: Task 1 落地後的 `skills/fankeel-build/SKILL.md`——兩個 task 都改這個檔，Task 1 先、Task 3 後，Task 3 的步驟 10 在 Task 1 改過的檔案上做（Task 3 排在節 4／節 7 對應的所有其他 TODO 條目之後執行，不消費它們的產物；`fankeel-reviewer` agent 由另一個 task 建，這裡只是把 `:278`／`:300` 的文字改成那個名字，不驗 agent 檔存不存在）
- Produces: `lib/stages.js` 的 `ALWAYS`、`STAGES[...].rules`、`STAGES[...].when` 是 Task 4 讀 `budget` 與 `rulesFor()` 的基礎——**Task 4 必須在這個 task commit 之後才量 `budget`**，量到舊文字上是量錯地方

**Dispatch:** implementer, sonnet — 每一段替換文字、每一個量測用的一行指令都在下面逐字給出；工作是照抄、跑指令、核對輸出裡的數字，不是重新推導哪一句該留哪一句該剪。

---

### 為什麼這個 task 比節 4／節 7 的字面範圍大

節 4 只點名 `lib/stages.js:301`、`:278`、`:300` 三處，節 7 只點名 `ALWAYS[2]`。但 `ALWAYS` 是七個 stage 共用的陣列——`ALWAYS[2]` 加一句，七個 block 全部一起變長。跑過 `tests/render.test.js:546` 那個 2400 上限的量法（同一份 `PROFILES`、同一個 `sizeAtReference`）會看到：

| stage | 今天（字元，59-char root） | 只加 `ALWAYS[2]` 之後 |
|---|---|---|
| survey | 2261 | 2339（過） |
| design | 2357 | 2435（**破 2400**） |
| plan | 2249 | 2328（過） |
| build | 2257 | （由這個 task 自己的改寫抵掉，見步驟 2） |
| verify | 2391 | 2469（**破 2400**） |
| audit | 2397 | 2475（**破 2400**） |
| land | 2391 | 2469（**破 2400**） |

`design`、`verify`、`audit`、`land` 四個 stage 今天離 2400 的餘裕都小於 `ALWAYS[2]` 那一句要花的字元數（約 78），設計文件的節 7 只交代了 `build` 要怎麼還債，沒提這四個。這不是「要不要做」的問題——`tests/render.test.js` 是已經存在、不在這個 task 改動範圍內的測試，`ALWAYS[2]` 一改就會讓它在這四個 stage 上變紅，不管 build 那邊怎麼處理。

`Ruling:` 四個 stage 各壓縮一到兩條規則的字數，換回 `ALWAYS[2]` 佔掉的空間 — 每一句要留的判準、每一個 pin 的子字串（`tests/stages.test.js`、`tests/pipeline-doc.test.js` 的 anchor）都先用 `node` 核對過，壓縮只動未被任何測試釘住的措辭，不動被 pin 的部分 — costs if wrong: 四段規則的用詞比今天緊一些，但語意與四個判準本身都沒有變。步驟 3 逐條給文字。

---

### 步驟

1. 先寫兩個失敗的目標斷言。在 `tests/stages.test.js`，把檔案開頭的這一行：

```js
const { ALWAYS, STAGES, NAMES, byName, nextStage, rulesFor, templateFor } = require('../lib/stages.js');
```

在 `tests/stages.test.js` 換成（多引入 `FULL_ROUTE`，並新增一行引入 `render`）：

```js
const { ALWAYS, STAGES, NAMES, FULL_ROUTE, byName, nextStage, rulesFor, templateFor } = require('../lib/stages.js');
const { render } = require('../lib/render.js');
```

然後在 `tests/stages.test.js` 檔尾加上：

```js
// Where a rule and the shape it is filed under disagree, the rule stays and
// the shape gives way — the Calibration meta-rule this same sentence carries
// into skills/fankeel/SKILL.md.
test('the always-on block says the constraint wins over the shape', () => {
  assert.match(ALWAYS[2], /the constraint wins and the shape stays/);
});

// build's third rule used to name four things that stop the loop with no
// stated reason they are the four. Reframed around what a `Ruling` actually
// has to survive: git's own ability to undo it.
test("build's stopping rule is framed around what git can revert", () => {
  const text = byName('build').rules.join(' ');
  assert.match(text, /revert/);
  assert.match(text, /irreversible/);
  assert.match(text, /Ruling:/);
});
```

2. 跑 `node --test tests/stages.test.js`，看兩個新測試都紅：`ALWAYS[2]` 今天沒有那句話；build 的規則今天沒有 `revert` 這個字。

3. 在 `lib/stages.js`，做以下逐字替換。

3a. `ALWAYS[2]`（原本整條）：

```js
    'Say what you actually did — a skipped step, a failed test, a thing you could not check — and a dispatch before it goes: how many, which model.',
```

在 `lib/stages.js` 改成：

```js
    'Say what you actually did — a skipped step, a failed test, a thing you could not check — and a dispatch before it goes: how many, which model. Where a rule and the shape conflict, the constraint wins and the shape stays.',
```

3b. `lib/stages.js:278`（`plan` 的 reviewer 措辭，字數不多不少換成同名 agent）：

```js
            'Before the gate: `ledger.js --plan <f> lint` clean, then one sonnet reviewer over design and plan, returning only promises with no task and Files blocks that disagree with their task.',
```

在 `lib/stages.js` 改成：

```js
            'Before the gate: `ledger.js --plan <f> lint` clean, then one fankeel-reviewer over design and plan, returning only promises with no task and Files blocks that disagree with their task.',
```

3c. `lib/stages.js` 的 build `:300`（reviewer 措辭）與 `:301`（停止條件，改寫成「git 能不能撤掉」的形，四個判準不變）：

```js
            'From a plan: `node {{LEDGER}} --plan <f> show` first; never redo a task it lists complete. One reviewer per task or fix, then `complete <n> "<what>"` or `fix "<what>"`.',
            'Decide rather than stall, recording `Ruling: what — why — costs if wrong`. Only four things stop the loop: irreversible, security-sensitive, a side effect outside this workspace, every path a guess.',
```

在 `lib/stages.js` 改成：

```js
            'From a plan: `node {{LEDGER}} --plan <f> show` first; never redo a task it lists complete. One fankeel-reviewer per task or fix, then `complete <n>` or `fix`.',
            'Decide, record `Ruling:`. Stop where git cannot revert: irreversible, security-sensitive, out-of-workspace, every path a guess.',
```

`skills/fankeel-build/SKILL.md` 的「## Rulings, not stalls」一節已經把 `ledger.js ruling "<what>" "<why>" "<cost if wrong>"` 與四個判準的完整版本寫過一次（進站時讀一次），注入規則不必再把 `what — why — costs if wrong` 拼一遍。

4. 跑 `node --test tests/stages.test.js`，看步驟 1 的兩個測試轉綠；其餘既有測試也仍是綠的（這一步只動了 `ALWAYS[2]`、`plan.rules[3]`、`build.rules[1]`、`build.rules[2]`，都沒有碰任何被 pin 的子字串——`never redo a task it lists complete`、`Ruling:`、`irreversible`、`reviewer` 全部還在）。

5. 跑一次全套：`npm test`。`tests/render.test.js` 的「no stage's rules cost more than a readable preamble」在 `design`、`verify`、`audit`、`land` 四個 stage 上會紅，訊息形如 `design injection is 2435 chars under a 59-character plugin root`（超過 2400）。這是本節開頭那個 `Ruling:` 要處理的地方。

6. 在 `lib/stages.js`，對這四個 stage 各做以下替換——每一句都核對過：只動未被任何測試釘住的措辭，四個 stage 各自被 pin 的子字串（下方每組替換後列出）原封不動。

`design.rules`（第 4 條，map 檢查）：

```js
            'Check the approach against .fankeel/map.md before presenting it. Name the page, or say you checked and found none.',
```

在 `lib/stages.js` 改成：

```js
            'Check the approach against .fankeel/map.md before presenting it. Name the page, or say none was found.',
```

`lib/stages.js` 的 `design.when[0].text`（mockup 規則）今天是：

```js
            { when: 'design.mockup', text: 'Frontend work gets a mockup first: one page at the model `design.mockup` names, saved under `.fankeel/build/`, its path on the `spec:` line — the gate approves the page, not the paragraph.' },
```

在 `lib/stages.js` 改成下面這段。

只動措辭，pin 住的子字串一字不改：

```js
            { when: 'design.mockup', text: "Frontend work gets a mockup first: one page at `design.mockup`'s model, under `.fankeel/build/`, path on `spec:` — the gate approves the page, not the paragraph." },
```

（`design` 被 pin 的子字串——`map.md`、`Under 200 words`、`mockup`、`design.mockup`——都還在。）

`lib/stages.js` 的 `verify.rules`（adversary 那條，第 6 條）今天是：

```js
            'Before the question, one read-only adversary over the evidence: was each command run after the last edit, on the thing claimed, by a check that could have failed. Give it paths, never a paste, and ask only for the rows it defeats: every line it returns stays here for good. Where the host opens it, the chain is one workflow.',
```

在 `lib/stages.js` 改成：

```js
            'Before the question, one read-only adversary over the evidence: was it run, on what, by a check that could have failed. Give it paths, never a paste; keep only what it defeats. Where the host opens it, the chain is one workflow.',
```

（`verify` 被 pin 的子字串——`adversary`、`could have failed`、`one workflow`、`denominator`、`no longer true`——都還在。）

`lib/stages.js` 的 `audit.rules`（第 1、2、3、4、5、7 條，六條裡動五條，第一條只動未被 anchor 釘住的前半）：

```js
            'Run `node {{DOCS_CHECK}}` and quote it — dead references, never opinions.',
            'Run `node {{RESIDUE}}` and quote it, non-git too.',
            'Every fortnight /fankeel-audit adds the deep pass: `node {{DOCS_AUDIT}}`, one reader per pair, and offers the cleanup. {{PONYTAIL}}',
            '`package.json`: `fankeel`? Its scripts, not `<plugin>`\'s.',
            'Before the question, one read-only adversary over the findings. Where the host opens it, the chain is one workflow.',
```

在 `lib/stages.js` 改成：

```js
            'Run `node {{DOCS_CHECK}}` — dead references, never opinions.',
            'Run `node {{RESIDUE}}`, quoted, non-git too.',
            'Every fortnight, `node {{DOCS_AUDIT}}` runs the deep pass, one reader per pair, then offers cleanup. {{PONYTAIL}}',
            '`package.json`: `fankeel`? Its scripts, not `<plugin>`.',
            'One read-only adversary over the findings — one workflow where the host opens it.',
```

`lib/stages.js` 的 `audit.rules` 最後一條（Output）今天是：

```js
            'Output: one line per finding, worst first, then the question. Nothing found is a finding: say so and stop.',
```

在 `lib/stages.js` 改成：

```js
            'Output: worst-first, one line per finding, then the question. None found: say so, stop.',
```

（`audit` 被 pin 的子字串——ANCHOR 用的 `dead references, never opinions`、`adversary`、`{{PONYTAIL}}`、`Read the fankeel-audit skill on entry: todo-check after a move.`——都還在；那句 `Read the fankeel-audit skill on entry: ...` 本身不在這次替換之列。）

`lib/stages.js` 的 `land.rules`（第 2、3、4、5 條）今天是：

```js
            'Close the TODO.md entries this work finished, then run `node {{TODO_CHECK}}`. A plan that just moved is a link that just changed address.',
            'Update last_verified on every page you re-read and found true, then re-run `node {{MAP}}`.',
            'shipped: is one line per thing someone can now do that they could not, from the ledger\'s completed entries where there is one.',
            'Option one stands the task down; route the notes first. `/clear` after, never before: a cleared session gets a new id and the entry is left active, unread.',
```

在 `lib/stages.js` 改成：

```js
            'Close the TODO.md entries this work finished, then run `node {{TODO_CHECK}}`. A moved plan is a changed-address link.',
            'Update last_verified on pages re-checked true, then re-run `node {{MAP}}`.',
            "shipped: is one line per thing someone can now do that they could not — the ledger's completed entries, if any.",
            "Option one stands the task down; route the notes first. `/clear` after, never before — a cleared session gets a new id, leaving the entry active, unread.",
```

`lib/stages.js` 的 `land.when`（兩條 archivePlan 文字）今天是：

```js
            { when: 'land.archivePlan', text: 'A landed plan leaves a decision record behind — what was decided and why — then is archived; the profile said so, so no question.' },
            { when: '!land.archivePlan', text: 'A landed plan leaves a decision record behind — what was decided and why — then is archived, after asking. An unarchived plan gets read as current.' },
```

在 `lib/stages.js` 改成：

```js
            { when: 'land.archivePlan', text: 'A landed plan leaves a decision record behind — what and why — then is archived; the profile said so, no question.' },
            { when: '!land.archivePlan', text: 'A landed plan leaves a decision record behind — what and why — then is archived, after asking. Unarchived, it reads as current.' },
```

（`land` 被 pin 的子字串——`last_verified`、`{{MAP}}`、`shipped: is one line per thing someone can now do`、`` `/clear` after, never before ``、`leaves a decision record behind`、以及 `COMMIT` 常數整段（`under 60 characters`、`one bullet per change`、`- <what changed> — <module>`、`one paragraph`，這條完全沒動）——都還在。）

7. 量一次全部七個 stage，確認都回到 2400 以下。在倉庫根目錄跑：

```
node -e "const s=require('./lib/stages.js');const{render}=require('./lib/render.js');const{NAMES}=s;const NOW=Date.parse('2026-08-21T12:00:00.000Z');const ago=ms=>new Date(NOW-ms).toISOString();const MINE='aaaaaaaa-0000-4000-8000-000000000001';const entry=(id,o)=>({sessionId:id,data:Object.assign({task:'x',claims:['statusline.ps1','statusline.sh'],stage:'implement',class:'architectural',route:['survey','design','plan','build','verify','audit','land'],active:true,started:ago(2*3600e3),updated:ago(60e3)},o)});const ROOT=require('./lib/render.js').PLUGIN_ROOT;const R=59;const sr=o=>{const n=o.split(ROOT).length-1;return o.length+n*(R-ROOT.length)};const P=[true,false].flatMap(a=>[false,'opus'].map(m=>({values:{'land.integration':'merge','land.push':false,'land.archivePlan':a,guard:'ask','dispatch.floor':'sonnet','judge.model':'fable','design.mockup':m}})));for(const st of NAMES){const outs=P.map(pr=>render({mine:entry(MINE,{stage:st}),others:[],now:NOW,profile:pr}));const out=outs.reduce((a,b)=>sr(b)>sr(a)?b:a);console.log(st.padEnd(7),sr(out),sr(out)<2400?'OK':'OVER')}"
```

七行都要印 `OK`；量到的今天數字（供核對，實際落地後可能因為抄寫時的空白差 1-2 個字元）：`survey 2339`、`design 2396`、`plan 2328`、`build 2255`、`verify 2372`、`audit 2384`、`land 2396`——`design` 與 `land` 餘裕只有 4 個字元，往後任何一條規則再變長都會先從這兩個 stage 開始紅，記在這裡供下一次改動的人知道去哪裡量。

8. 跑 `npm test`，確認全部轉綠，通過數不低於這個 task 開工前的基準。

9. 把 `docs/pipeline.md` 裡逐字引用 `build` 注入區塊的兩份示範（各自的 `stage rules:` 區塊）同步成新文字：`Background belongs...` 那一行不動，其下三行——reviewer 措辭、`Decide, record...`——換成步驟 3c 給的新字串，`Say what you actually did...` 那一行接上步驟 3a 新增的句子。兩份示範完全相同，一起改。

10. 在 `skills/fankeel-build/SKILL.md` 第 39 行：

```markdown
| A ruling recorded instead of a fifth stopper firing | `lib/stages.js:301` names deciding as the normal outcome (`Decide rather than stall`) — only irreversible, security-sensitive, out-of-workspace or every-path-a-guess actually stop the loop. |
```

把引號裡的 `` `Decide rather than stall` `` 換成 `` `Decide` ``（`:301` 的新文字不再有 "rather than stall" 這幾個字，這裡的引句要跟著）。

11. 跑 `npm test` 確認綠燈，`git add -A`（若有新測試檔尚未追蹤，先 `git add` 再跑一次確認 `tests/source.test.js` 也綠），commit：`fix(stages): ALWAYS[2] names the constraint-shape tie, build reframes its stop`。

12. 在 `skills/fankeel/SKILL.md`，找到 `## Output styles` 一節結束、`## Subagents` 開始之間的空行（`... copy however long the session runs.` 之後，`## Subagents` 之前），插入新的一節：

```markdown
## Calibration

Three rules sit above the ones a stage carries, because they govern how the
rules themselves are read rather than what any one stage produces.

**A gate on every stage is a treadmill.** The gate belongs at a stage's end —
see "At the end of a stage, ask" above — never partway through one. A rule
that would stop mid-stage to check in has invented a second gate nothing
asked for.

**Two rules in conflict name both.** State which one wins and why, in the
turn that hits the conflict, rather than quietly following one and dropping
the other with nothing on screen to say it happened.

**Where a rule and the shape conflict, the constraint wins and the shape
stays.** `ALWAYS[2]` carries this for every stage. The output template is the
cheaper place to spend room — it is read once by the model and never by the
user — so it is never rewritten to dodge a rule, and a rule is never cut to
keep a template line intact.

The four always-on rules exist because something specific broke without
them, not for balance:

| rule | cause | Bad → Good | exemptions |
|---|---|---|---|
| `ALWAYS[0]` — ask with `AskUserQuestion`; option one is the approval; `(Recommended)` is a label, never a position | a real design stage ended with three numbered options in a paragraph — asking, and also the failure: the options were on screen and the user still had to type one out (`lib/stages.js`, the comment above `ALWAYS`) | Bad: "Here are three options: 1)… 2)… 3)… which would you like?" typed in prose. Good: an `AskUserQuestion` call, option one wired to advance, `(Recommended)` on whichever finding backs it | none |
| `ALWAYS[1]` — background sits in the option descriptions, never in the stem | `background inside the question` was read as *inside the question stem*, and a design stage asked a 491-character question (`tests/stages.test.js`, the comment above this rule's test) | Bad: a one-line stem carrying the whole rationale ahead of the options. Good: a short stem, the rationale moved into the description of the option it is about | none |
| `ALWAYS[2]` — say what you actually did, and a dispatch before it goes: how many, which model | only `survey` said what it was sending; a fan-out nobody announced is spend the user is paying for and could not see coming (`lib/stages.js`, the comment above `ALWAYS`) | Bad: naming one model in the wrap-up, after four subagents already ran. Good: "dispatching 4 readers, sonnet" stated before they go | none — covers every stage that dispatches |
| `ALWAYS[3]` — literal characters, never `\uXXXX` escapes; a code concept named in code, not translated | two of seventeen `AskUserQuestion` calls in one real session serialised their Chinese as unicode escapes, corrupted mid-word, and did not parse (`lib/stages.js`, the comment above `ALWAYS`) | Bad: `這樣` inside a tool call's JSON string. Good: writing `這樣` directly | none |
```

`ALWAYS[1]` 的成因取自 `tests/stages.test.js`（第 277 行附近的註解），不是 `lib/stages.js` 本身或 docs/decisions 目錄——那一條規則在 `lib/stages.js` 開頭的大段註解裡沒有單獨敘述它的失敗事件，逐句核對過（`grep -n "stem" lib/stages.js docs/decisions/*.md` 只有名詞出現，沒有敘事）；其餘三條的成因都逐字出自 `lib/stages.js` 中 `ALWAYS` 上方那段既有註解。落地時如果審核者要求四條成因來源一致，這一格需要另外決定（是把敘事搬進 `lib/stages.js` 的註解，還是留著跨檔引用），不在這個 task 裡動 `lib/stages.js` 的註解本身。

13. 跑 `node --test tests/skills.test.js`，確認新一節沒有踩到既有的 frontmatter／長度檢查（`## Calibration` 不是 frontmatter 也不是 `## Output` 區塊，不影響那些測試）。

14. 在 `tests/stages.test.js` 檔尾加上 reachability test：

```js
// Reachability, not worth: a rule that is well-written and gated behind a
// `when` key nobody sets is followed by nobody. This renders every stage and
// confirms each of its own rules — ALWAYS, the stage's own list, and its
// `when`-gated ones under the profile value that turns them on — actually
// appears in the injected block, then removes exactly that rule from the
// live STAGES array (the same module lib/render.js reads, restored in a
// `finally`) and confirms only that rule's anchor is gone and every other
// stays. This is green the moment it is written — it guards an invariant,
// not a feature this task is building — and its own "remove one rule" step
// is its proof that it is not vacuous; nothing outside the test demonstrates
// that separately.
//
// A token like `{{NEXT}}` or `{{LEDGER}}` becomes a run-time value, so a rule
// carrying one is anchored on its longest token-free piece rather than its
// full text — the piece least likely to collide with another rule that
// happens to share a short prefix or suffix (two of `audit`'s rules both
// start with "Run `node ").
test('every rule reaches the injected block, and removing one drops only it', () => {
  const NOW = Date.parse('2026-08-21T12:00:00.000Z');
  const ago = (ms) => new Date(NOW - ms).toISOString();
  const MINE = 'aaaaaaaa-0000-4000-8000-000000000001';

  function entryFor(stage, values) {
    return {
      mine: { sessionId: MINE, data: { task: 'x', claims: [], stage, class: 'architectural', route: FULL_ROUTE, active: true, started: ago(3600e3), updated: ago(60e3) } },
      others: [], now: NOW, profile: values ? { values } : undefined,
    };
  }

  const anchor = (text) => text.split(/\{\{[A-Z_]+\}\}/).reduce((a, b) => (b.length > a.length ? b : a), '');

  function whenValues(when) {
    const neg = when.charAt(0) === '!';
    const key = neg ? when.slice(1) : when;
    return { [key]: !neg };
  }

  let checked = 0;
  for (const stage of NAMES) {
    const found = byName(stage);

    for (let i = 0; i < ALWAYS.length; i++) {
      const rule = ALWAYS[i];
      const out = render(entryFor(stage));
      assert.ok(out.includes(anchor(rule)), stage + ': ALWAYS[' + i + '] does not reach the block');
      const saved = ALWAYS.splice(i, 1);
      try {
        const out2 = render(entryFor(stage));
        assert.ok(!out2.includes(anchor(rule)), stage + ': ALWAYS[' + i + '] still present after removal');
        for (let j = 0; j < ALWAYS.length; j++) {
          assert.ok(out2.includes(anchor(ALWAYS[j])), stage + ': ALWAYS[' + j + '] disappeared when a different rule was removed');
        }
      } finally { ALWAYS.splice(i, 0, saved[0]); }
      checked++;
    }

    for (let i = 0; i < found.rules.length; i++) {
      const rule = found.rules[i];
      const out = render(entryFor(stage));
      assert.ok(out.includes(anchor(rule)), stage + ': rules[' + i + '] does not reach the block');
      const saved = found.rules.splice(i, 1);
      try {
        const out2 = render(entryFor(stage));
        assert.ok(!out2.includes(anchor(rule)), stage + ': rules[' + i + '] still present after removal');
      } finally { found.rules.splice(i, 0, saved[0]); }
      checked++;
    }

    for (const w of (found.when || [])) {
      const values = whenValues(w.when);
      const out = render(entryFor(stage, values));
      assert.ok(out.includes(anchor(w.text)), stage + ': when(' + w.when + ') does not reach the block');
      const idx = found.when.indexOf(w);
      const saved = found.when.splice(idx, 1);
      try {
        const out2 = render(entryFor(stage, values));
        assert.ok(!out2.includes(anchor(w.text)), stage + ': when(' + w.when + ') still present after removal');
      } finally { found.when.splice(idx, 0, saved[0]); }
      checked++;
    }
  }
  // A loop that iterated zero times would pass vacuously — NAMES or byName
  // returning nothing would look identical to every rule being reachable.
  assert.ok(checked > 50, 'the reachability loop only checked ' + checked + ' rules');
});
```

15. 跑 `node --test tests/stages.test.js`，確認新測試綠燈（`checked` today 應為 83：4 條 `ALWAYS` × 7 stage，加七個 stage 自己的規則數總和，加三條 `when`——`design.mockup` 一條、`land.archivePlan` 兩條）。

16. 跑 `npm test` 全套一次，確認仍是綠的，通過數不低於這個 task 開工前的基準。

17. commit：`docs(fankeel): Calibration section names the three meta-rules and why ALWAYS exists`。

---

## Task 4: `skills/registry.json` 與 stage 的 prompt 預算

**Files:**
- Modify: `lib/stage-registry.js` — 純邏輯：量每個 stage 注入區塊的位元數、從規則裡抽 `entry_condition`、從 SKILL.md 抽 `stop_condition`、組出整份 registry 物件
- Modify: `scripts/stage-registry.js` — 薄包裝：`--print` 印到 stdout，無旗標寫 `skills/registry.json`
- Modify: `skills/registry.json` — generated，`node scripts/stage-registry.js` 的輸出
- Modify: `lib/stages.js` — 每個 `STAGES` 元素加一個 `budget` 數字（UTF-8 bytes，59-char reference root 量法）
- Test: `tests/stage-registry.test.js` — 重新產生並 deep-equal 已 commit 的檔案；`prompt_bytes <= prompt_byte_budget` 逐 stage 斷言；一個會真的紅一次的控制組
- Modify: `docs/pipeline.md` — 三層表加一列
- Modify: `README.md` — `## Development` 段落新增一段，理由見步驟 8
- Read: `scripts/map.js` — `parseArgs`（`node:util` 的 `parseArgs`）與 `--print` 提前回傳的形
- Read: `lib/registry.js` — `resolveRoot` 是解「這個外掛正在服務哪個專案」的根，`stage-registry.js` 量的是外掛自己的 stage 定義，兩者是不同的「root」，這裡不用它
- Read: `lib/skills.js` — `REQUIRED_CORE`（12 支被 skill 或 `lib/stages.js` 點名的 script）與其上方註解裡 `eval.js`／`tmp-clean.js` 兩支「unnamed-script」的說法——`stage-registry.js` 不被任何 stage 規則或 skill 點名，走同一條路
- Read: `tests/render.test.js` — `sizeAtReference`／`REFERENCE_ROOT` 與 `PROFILES`，量位元數照它的 context，不改
- Read: `scripts/skills-check.js` — `REQUIRED_CORE` 之外「unnamed-script」的前例，不改
- Read: `tests/pipeline-doc.test.js` — 它的 ANCHOR 機制不讀三層表，確認新列不影響它，不改

**Interfaces:**
- Consumes: Task 3 落地後的 `lib/stages.js`（`ALWAYS`、七個 stage 的 `rules`、`when`）——`prompt_bytes` 量的是 Task 3 改完之後的字串，量在 Task 3 之前的數字全部作廢，必須重新跑
- Produces: `lib/stages.js` 的 `budget` 欄位、`skills/registry.json`、`lib/stage-registry.js` 的 `buildRegistry(root)`（給任何以後要讀這份 registry 的程式碼用）

**Dispatch:** implementer, sonnet — 兩個新檔案的完整程式碼都在下面；抽取用的兩個正則表達式已經對著這個倉庫的七個 stage 逐一核對過，照抄即可。

---

### 步驟

1. 量出七個 `budget` 數字。在倉庫根目錄跑（`node -e` 一行，量法與 `tests/render.test.js:441-548` 相同，只是把單位從字元換成 UTF-8 bytes）：

```
node -e "const s=require('./lib/stages.js');const{render}=require('./lib/render.js');const{NAMES}=s;const NOW=Date.parse('2026-08-21T12:00:00.000Z');const ago=ms=>new Date(NOW-ms).toISOString();const MINE='aaaaaaaa-0000-4000-8000-000000000001';const entry=(id,o)=>({sessionId:id,data:Object.assign({task:'x',claims:['statusline.ps1','statusline.sh'],stage:'implement',class:'architectural',route:['survey','design','plan','build','verify','audit','land'],active:true,started:ago(2*3600e3),updated:ago(60e3)},o)});const ROOT=require('./lib/render.js').PLUGIN_ROOT;const R=59;const br=o=>{const n=o.split(ROOT).length-1;return Buffer.byteLength(o,'utf8')+n*(R-ROOT.length)};const P=[true,false].flatMap(a=>[false,'opus'].map(m=>({values:{'land.integration':'merge','land.push':false,'land.archivePlan':a,guard:'ask','dispatch.floor':'sonnet','judge.model':'fable','design.mockup':m}})));for(const st of NAMES){const outs=P.map(pr=>render({mine:entry(MINE,{stage:st}),others:[],now:NOW,profile:pr}));const out=outs.reduce((a,b)=>br(b)>br(a)?b:a);console.log(st.padEnd(7),br(out))}"
```

這個 task 開工前（Task 3 剛落地）量到的數字：`survey 2377`、`design 2434`、`plan 2366`、`build 2291`、`verify 2406`、`audit 2424`、`land 2440`。往上取整到百位：`survey 2400`、`design 2500`、`plan 2400`、`verify 2500`、`audit 2500`、`land 2500`；`build` 不取整，直接用 `tests/render.test.js` 現有的 `2400`（設計文件節 3 明訂）。**在實作這一步之前重新跑一次上面的指令**——如果 Task 3 落地時的字數與這個 task 起草時（見 Task 3 步驟 7）量到的不完全一樣，往上取整的結果可能不同，用重新量到的數字，不要抄這裡的表。

2. 在 `lib/stages.js` 的 `STAGES` 陣列，給七個元素各加一行 `budget:`（放在 `rules:` 陣列結束、`template:` 開始之前）。例如 `survey`：

```js
    {
        name: 'survey',
        produces: 'a statement of what already exists',
        rules: [
```

在 `lib/stages.js` 的這段結構裡，`rules: [...]`（陣列本身，含收尾的 `],`）之後、`template: [` 之前插入一行：

```js
        budget: 2400,
```

七個 stage 依步驟 1 量到的數字分別填入（`build: 2400`，其餘用步驟 1 表格往上取整的值）。

3. 建立 `lib/stage-registry.js`：

```js
'use strict';

// The pure logic behind scripts/stage-registry.js: every stage's rules
// reduced to what another tool can check without parsing prose — the
// sentence that gates entry to its skill, the sentence that gates leaving
// it, and how many of its own budgeted bytes the injected block actually
// spends today.
//
// Sized at a reference plugin root rather than this checkout's, for the
// same reason tests/render.test.js does at its own REFERENCE_ROOT: the real
// output grows by wherever fankeel happens to be installed, and a number
// that moved with the install path would never deep-equal between two
// checkouts of the same commit. `budget` in lib/stages.js is set against the
// same reference, which is what makes `prompt_bytes <= prompt_byte_budget`
// mean anything.

const fs = require('node:fs');
const path = require('node:path');
const { NAMES, byName } = require('./stages.js');
const { render, PLUGIN_ROOT } = require('./render.js');

const REFERENCE_ROOT = 59;

const NOW = Date.parse('2026-08-21T12:00:00.000Z');
const ago = (ms) => new Date(NOW - ms).toISOString();
const MINE = 'aaaaaaaa-0000-4000-8000-000000000001';
const FULL_ROUTE = ['survey', 'design', 'plan', 'build', 'verify', 'audit', 'land'];

// The worst case tests/render.test.js's own cap test measures against: every
// `when` rule crossed both ways, because the profile that turns one on is
// also the one that binds against the cap.
const PROFILES = [true, false].flatMap((archive) => [false, 'opus'].map((mockup) => ({
    values: { 'land.integration': 'merge', 'land.push': false, 'land.archivePlan': archive, guard: 'ask', 'dispatch.floor': 'sonnet', 'judge.model': 'fable', 'design.mockup': mockup },
})));

function entryFor(stage) {
    return {
        mine: { sessionId: MINE, data: { task: 'rework the colour ramp', claims: ['statusline.ps1', 'statusline.sh'], stage, class: 'architectural', route: FULL_ROUTE, active: true, started: ago(2 * 3600e3), updated: ago(60e3) } },
        others: [], now: NOW,
    };
}

// `render()` names its own PLUGIN_ROOT from where lib/render.js itself sits
// on disk, not from whatever `root` this function is called with — the two
// only coincide because this registry is always about fankeel's own stages,
// never a project the plugin is pointed at. Importing the same constant
// `render()` uses, rather than recomputing one from `root`, is what keeps
// that true even if a future caller passes a `root` that does not happen to
// be this checkout.
function bytesAtReference(out) {
    const roots = out.split(PLUGIN_ROOT).length - 1;
    return Buffer.byteLength(out, 'utf8') + roots * (REFERENCE_ROOT - PLUGIN_ROOT.length);
}

// The worst of the four profile crossings, in UTF-8 bytes at a 59-character
// root — the same figure `build`'s `budget` in lib/stages.js is pinned to.
function promptBytes(stage) {
    let worst = 0;
    for (const profile of PROFILES) {
        const out = render(Object.assign({ profile }, entryFor(stage)));
        const size = bytesAtReference(out);
        if (size > worst) worst = size;
    }
    return worst;
}

// The tail of the rule that reads `Read the fankeel-<stage> skill on entry:
// ...` — every stage carries exactly one, checked by
// tests/stages.test.js's own coverage of that rule.
function entryCondition(stage) {
    const re = new RegExp('^Read the fankeel-' + stage + ' skill on entry:\\s*(.+)$');
    for (const rule of byName(stage).rules) {
        const m = re.exec(rule);
        if (m) return m[1].trim();
    }
    return null;
}

// The first sentence of the `**Done when**` paragraph, without the markdown
// bold markers. Stops at the first period followed by a capital letter,
// `*` or a backtick — every stage's inline code (`` `skipped:` ``,
// `` `TODO.md` ``) contains no period-then-space, so none of those false-
// trigger the boundary.
function stopCondition(root, stage) {
    const file = path.join(root, 'skills', 'fankeel-' + stage, 'SKILL.md');
    const text = fs.readFileSync(file, 'utf8');
    const m = /\*\*Done when\*\*\s+([\s\S]+?\.)\s+[A-Z*`]/.exec(text);
    return m ? ('Done when ' + m[1].replace(/\s+/g, ' ')) : null;
}

function buildRegistry(root) {
    const stages = NAMES.map((name) => {
        const stage = byName(name);
        const entry_condition = entryCondition(name);
        const stop_condition = stopCondition(root, name);
        if (!entry_condition) throw new Error(name + ' names no "Read the fankeel-' + name + ' skill on entry:" rule');
        if (!stop_condition) throw new Error('skills/fankeel-' + name + '/SKILL.md carries no "**Done when**" sentence');
        if (typeof stage.budget !== 'number') throw new Error(name + ' has no budget in lib/stages.js');
        return {
            name,
            entry_condition,
            stop_condition,
            prompt_bytes: promptBytes(name),
            prompt_byte_budget: stage.budget,
        };
    });
    return { generated_by: 'scripts/stage-registry.js', stages };
}

module.exports = { buildRegistry, promptBytes, entryCondition, stopCondition, REFERENCE_ROOT };
```

4. 建立 `scripts/stage-registry.js`：

```js
#!/usr/bin/env node
'use strict';

// Writes skills/registry.json — the machine-readable half of what
// lib/stages.js and each skills/fankeel-<stage>/SKILL.md already say in
// prose: which sentence gates entry to a stage's skill, which sentence says
// it is done, and how many of its own budgeted bytes it spends today.
//
// This is about fankeel's own stages, not about whatever project the plugin
// is running against — unlike scripts/map.js, there is no separate "target
// project" root to resolve, so this does not go through lib/registry.js's
// resolveRoot. The root is this plugin's own checkout unless --root says
// otherwise, which only exists for pointing a test at a fixture tree.

const fs = require('node:fs');
const path = require('node:path');
const { parseArgs: parseArgv } = require('node:util');

const { buildRegistry } = require('../lib/stage-registry.js');

const OUT_REL = 'skills/registry.json';
const DEFAULT_ROOT = path.join(__dirname, '..');

function parseArgs(argv) {
    const { values } = parseArgv({ args: argv, strict: false, allowPositionals: true, options: { root: { type: 'string' }, print: { type: 'boolean' } } });
    return {
        root: typeof values.root === 'string' ? values.root : DEFAULT_ROOT,
        print: values.print === true,
    };
}

function main(argv) {
    const { root, print } = parseArgs(argv);
    const text = JSON.stringify(buildRegistry(root), null, 2) + '\n';
    if (print) return text;
    fs.writeFileSync(path.join(root, OUT_REL), text);
    return OUT_REL + ' written.';
}

if (require.main === module) {
    process.stdout.write(main(process.argv.slice(2)) + '\n');
}

module.exports = { parseArgs, main };
```

5. 先寫一個失敗的測試。建立 `tests/stage-registry.test.js`：

```js
'use strict';

// skills/registry.json is generated, the way .fankeel/map.md is — the
// equivalent of a stale frontmatter date is a committed file that no longer
// matches what buildRegistry() produces from the current lib/stages.js and
// the current skills/fankeel-<stage>/SKILL.md files. Regenerating and
// deep-equalling the committed file is what makes that mechanical rather
// than something a reviewer has to notice by eye.

const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const { buildRegistry } = require('../lib/stage-registry.js');
const { NAMES, byName } = require('../lib/stages.js');

const ROOT = path.join(__dirname, '..');
const REGISTRY = path.join(ROOT, 'skills', 'registry.json');

test('skills/registry.json is exactly what regenerating it produces', () => {
  const committed = JSON.parse(fs.readFileSync(REGISTRY, 'utf8'));
  const fresh = buildRegistry(ROOT);
  assert.deepEqual(committed, fresh,
    'skills/registry.json is stale — a rule, a Done when sentence, or a budget changed without regenerating it (run: node scripts/stage-registry.js)');
});

test('every stage fits its own budget, and the set is the canonical seven', () => {
  const { stages } = buildRegistry(ROOT);
  assert.deepEqual(stages.map((s) => s.name), NAMES);
  for (const s of stages) {
    assert.ok(s.prompt_bytes <= s.prompt_byte_budget,
      s.name + ' spends ' + s.prompt_bytes + ' bytes against a ' + s.prompt_byte_budget + '-byte budget');
  }
});

// A budget test that cannot fail is not a test. Lower build's budget below
// what it actually measures, on the live STAGES entry lib/stage-registry.js
// itself reads, and confirm the "fits its own budget" assertion above would
// have caught it — restored in a `finally` so no later test in this file, or
// in a file that shares the process, sees the tampered value.
test('a budget lowered below the measured size actually fails the check it exists for', () => {
  const build = byName('build');
  const saved = build.budget;
  build.budget = 1;
  try {
    const { stages } = buildRegistry(ROOT);
    const b = stages.find((s) => s.name === 'build');
    assert.ok(b.prompt_bytes > b.prompt_byte_budget, 'a 1-byte budget did not register as an overflow');
  } finally {
    build.budget = saved;
  }
});
```

6. `git add tests/stage-registry.test.js`（新測試檔要先進 `git ls-files`，`tests/source.test.js` 才看得到它）。

7. 跑 `node --test tests/stage-registry.test.js`，看它紅：`skills/registry.json` 還不存在，第一個測試在 `fs.readFileSync` 就丟例外。

8. 生成 registry 檔：

```
node scripts/stage-registry.js
git add skills/registry.json
```

9. 跑 `node --test tests/stage-registry.test.js`，確認三個測試都綠：deep-equal 通過、七個 stage 的 `prompt_bytes <= prompt_byte_budget` 都成立、把 `build.budget` 暫時改成 `1` 確實讓斷言抓到溢位（測試內已還原，不留痕跡）。

10. 在 `docs/pipeline.md` 的三層表（`### Where a rule lives` 底下，`| tier | holds | why it is enough |` 那張表），在 `skill` 那一列之後加一列：

```markdown
| tier | holds | why it is enough |
|---|---|---|
| script | anything a script can check or refuse — a missing block, a stage off the route, a prose `Consumes:` | the check runs whether or not anyone read the rule |
| anchor | a step whose skipping is silent and a later stage pays for: a template slot where the step produces something the report must show, else words on the `Read the fankeel-<stage> skill on entry:` line | both are re-sent every prompt |
| skill | the procedure's detail, the format, the why | read once on entering the stage; nothing load-bearing lives only here |
| registry | `skills/registry.json` — the entry and stop condition and the byte budget, machine-readable, generated by `scripts/stage-registry.js` | a tool outside this session can read a stage's contract without parsing prose |
```

（只加最後一列；前三列原封不動地保留，`tests/pipeline-doc.test.js` 的 ANCHOR 機制不讀這張表，不受影響。）

11. 在 `README.md`，把新腳本記在 `## Development` 一節裡，跟 `scripts/skills-check.js`（同樣是 `lib/skills.js` 的 `REQUIRED_CORE` 之外的「unnamed-script」）用同樣的散文寫法，而不是 `## The three scanners` 那張表——那張表的三支腳本都是「掃描並回報問題」的 scanner，`stage-registry.js` 是產生器，性質不同，`eval.js` 與 `skills-check.js` 也都沒有列在那張表裡，而是各自一段散文。`Ruling:` 這裡照 `eval.js`／`skills-check.js` 的前例，寫法選段落而非表格列，理由記在這裡；如果落地時的審核者認為字面上「加一列」就是指那張表，這一步需要另外確認。在 `` `node scripts/skills-check.js` 是一個 fail-closed gate... `` 那一段之後、`### Behaviour evals` 之前插入新的一段：

```markdown
`node scripts/stage-registry.js` writes `skills/registry.json`: one entry per
stage with the sentence that gates entering its skill, the sentence that
says it is done, and how many of its own budgeted bytes the injected block
spends today. It follows `eval.js`'s precedent rather than joining
`REQUIRED_CORE` — nothing in a stage's rules or a skill names this script,
so nothing would go looking for it there. `tests/stage-registry.test.js`
regenerates the file and deep-equals it against what is committed: a rule
that grew without regenerating, or a budget lowered below what a stage
actually measures, fails there rather than drifting silently.
```

12. 跑 `npm test` 全套一次，確認綠燈，通過數不低於這個 task 開工前的基準（`scripts/skills-check.js` 不需要改——`stage-registry.js` 不在任何 skill 或 `lib/stages.js` 的規則裡被點名，`REQUIRED_CORE` 本來就不該收它）。

13. commit：`feat(stages): skills/registry.json makes each stage's contract machine-readable`。

---

### 沒有在這裡做的事

`TODO.md` `## Waiting` 底下「`entry_condition / stop_condition` 進 registry」與「每 stage 一欄 `prompt_byte_budget`」這兩條——由這個 task 做掉之後直接刪除——留給 **Task 8**（TODO.md 與索引收尾）處理，這裡只把功能做出來，不動 `TODO.md` 本身。
## Task 5: provenance 六條通道

**Files:**
- Modify: `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab-provenance.txt` — 檔尾補 `cwd:`／`settings:`／`always-on flag:`／`budget:` 四行
- Modify: `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab2-provenance.txt` — 同上
- Modify: `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab3-provenance.txt` — 同上
- Modify: `docs/reports/evidence/2026-09-07-join-pair/ab5-provenance.txt` — 同上
- Modify: `docs/reports/evidence/2026-09-07-style-to-subagent/provenance.txt` — 同上
- Modify: `docs/reports/evidence/2026-09-08-route-typo/provenance.txt` — 同上，但這份是唯一經過 `scripts/eval.js` 這支 harness 跑的，兩個通道的值跟其餘六份不同
- Modify: `docs/reports/evidence/2026-09-09-haiku-pair/ab-haiku-provenance.txt` — 同上
- Read: `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab.sh` — 判斷 ab-provenance.txt 的四個值
- Read: `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab2.sh` — 判斷 ab2-provenance.txt 的四個值
- Read: `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab3.sh` — 判斷 ab3-provenance.txt 的四個值
- Read: `docs/reports/evidence/2026-09-07-join-pair/ab5.sh` — 判斷 ab5-provenance.txt 的四個值
- Read: `docs/reports/evidence/2026-09-07-style-to-subagent/probe.sh` — 判斷 style-to-subagent/provenance.txt 的四個值
- Read: `docs/reports/evidence/2026-09-09-haiku-pair/ab-haiku.sh` — 判斷 ab-haiku-provenance.txt 的四個值
- Read: `scripts/eval.js` — `:102-120` 的 `runOnce`，harness 自己怎麼控 cwd 與 settings 兩個通道；`route-typo/provenance.txt` 那份的兩個值從這裡來，不是它自己的指令列
- Read: `docs/improvement-brief.md` — `:795-823`，六條污染通道的名字（i-have-adhd 的命名）與 fankeel 目前的對應表，四行要用同一套詞彙
- Read: `scripts/docs-check.js` — `:108` `isMarkdown`、`:330` `markdown = files.filter(isMarkdown)`、`:201` `role === 'archive' || role === 'report'` 早退；三處合起來是這個 task 為什麼不會讓 `docs-check` 變紅的理由

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 下面七段的四行內容都已經寫定，照抄進對應檔案即可，不需要現場判斷。

七個檔案裡，目前沒有任何一個已經寫著 `cwd:`／`settings:`／`always-on flag:`／`budget:` 這四行中的任何一行（`docs/reports/evidence/2026-09-07-join-pair/ab5-provenance.txt` 已有的 `worktree HEAD unchanged`／`worktree porcelain after` 兩行，是另一件事——它們記的是「這個 worktree 跑完有沒有被動到」，不是「cwd 是不是一個中立的空目錄」），所以七個檔案都要補齊全部四行，沒有可以跳過的通道。

步驟：

1. `ab.sh:6` 只有 `cd "$REPO"`，沒有 `git checkout` 釘 commit，也不是 `fs.mkdtempSync` 那種每次一個空目錄；沒有 `--setting-sources` 或 `--settings`；沒有 `--max-budget-usd`。在 `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab-provenance.txt` 檔尾補上：

```text
cwd: the checkout (F:/ymlab/fankeel), uncontrolled — ab.sh:6 only `cd`s into it, no fresh empty directory per run
settings: user and project, uncontrolled — no --setting-sources or --settings flag
always-on flag: n/a — fankeel has none
budget: none, uncontrolled — no --max-budget-usd
```

2. `ab2.sh:9` 在跑之前 `git checkout --quiet 86a104e`，跑完在 `:35` `git checkout --quiet main` 復原——內容釘住了，但仍是同一個工作樹，不是空目錄。settings 與 budget 兩通道跟 ab.sh 一樣沒有旗標。在 `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab2-provenance.txt` 檔尾補上：

```text
cwd: the checkout, uncontrolled — pinned to 86a104e via `git checkout` (ab2.sh:9, restored to main at :35), not a fresh empty directory
settings: user and project, uncontrolled — no --setting-sources or --settings flag
always-on flag: n/a — fankeel has none
budget: none, uncontrolled — no --max-budget-usd
```

3. `ab3.sh` 跟 `ab2.sh` 同一個形狀，只是行號不同：`git checkout --quiet 86a104e` 在 `:11`，`git checkout --quiet main` 在 `:37`。在 `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab3-provenance.txt` 檔尾補上：

```text
cwd: the checkout, uncontrolled — pinned to 86a104e via `git checkout` (ab3.sh:11, restored to main at :37), not a fresh empty directory
settings: user and project, uncontrolled — no --setting-sources or --settings flag
always-on flag: n/a — fankeel has none
budget: none, uncontrolled — no --max-budget-usd
```

4. `ab5.sh:21-23` 改用一個獨立的 worktree（`F:/ymlab/fankeel-ab`），`:11` 說明是為了不去動主 checkout 當時所在、帶著未推送 commit 的 `todo-thirteen` 分支；這個 worktree 本身在 `ab5.sh:31` 被 `git diff` 讀過、跑前跑後都沒有 `checkout`/`switch`/`stash`/`reset`（腳本註解 `:14`）。它是「跟主 checkout 隔開」，但仍是一整棵工作樹，不是空目錄。settings 與 budget 兩通道一樣沒有旗標。在 `docs/reports/evidence/2026-09-07-join-pair/ab5-provenance.txt` 檔尾補上：

```text
cwd: a dedicated worktree (F:/ymlab/fankeel-ab, ab5.sh:22-23) pinned at 86a104e, isolated from the main checkout's in-progress branch — but still a full tree, not a fresh empty directory
settings: user and project, uncontrolled — no --setting-sources or --settings flag
always-on flag: n/a — fankeel has none
budget: none, uncontrolled — no --max-budget-usd
```

5. `probe.sh:18` 只有 `cd "$REPO"`，沒有釘 commit，也不是空目錄。`probe.sh:48-54` 每個 arm 帶一個 `--settings "$OUT/settings-$arm.json"`（control 臂內容是 `{}`、styled 臂是 `{ "outputStyle": "fankeel-terse" }`，兩個檔案在 `:30-31` 生成），但沒有 `--setting-sources`——這是這次探測本身的自變數，不是「排除操作者設定」意義下的控制：操作者原本的 user／project 設定仍然疊在底下。`always-on flag` 這裡也是 n/a，並且要說清楚這次量的是一個 output style 會不會傳到 subagent，跟「持續性的 always-on flag」是兩回事。在 `docs/reports/evidence/2026-09-07-style-to-subagent/provenance.txt` 檔尾補上：

```text
cwd: the checkout (F:/ymlab/fankeel), uncontrolled — probe.sh:18 only `cd`s into it, no pinned commit and no fresh empty directory
settings: this run's own variable, not contamination-controlled — probe.sh:48-54 passes --settings per arm (control `{}`, styled `{ outputStyle: fankeel-terse }`) with no --setting-sources, so the operator's own user/project settings still apply underneath
always-on flag: n/a — fankeel has none (this probes an output style, a different mechanism from a persistent always-on flag)
budget: none, uncontrolled — no --max-budget-usd
```

6. 這一份不一樣：`provenance.txt` 記的指令是 `node scripts/eval.js evals/route-typo --model claude-sonnet-5 --json ...`，跑的是 harness 本身而不是直接 `claude -p`。`scripts/eval.js:103` 的 `fs.mkdtempSync` 給每次 run 一個全新的空目錄，`:115` 把它設成 spawn 的 `cwd`；`:109` 對每次 run 都帶 `--setting-sources project`，排除操作者的 user 設定。這兩件事都是 harness 內建行為，不是呼叫端在命令列上打的旗標，所以 `provenance.txt` 記的指令看不到它們——這一點要在檔案裡寫出來，否則讀者會以為這兩個通道跟其餘六份一樣沒被控制。`--max-budget-usd` 這次呼叫確實沒有帶（`run.json`、`run2.json` 兩次都沒有），雖然 `scripts/eval.js:111` 支援透傳。在 `docs/reports/evidence/2026-09-08-route-typo/provenance.txt` 檔尾補上：

```text
cwd: controlled by the harness, not the checkout — scripts/eval.js:103 gives every run a fresh fs.mkdtempSync directory and spawns claude with that as cwd (:115); this command line does not show a flag for it because it is not one — it is built into scripts/eval.js
settings: controlled by the harness — scripts/eval.js:109 always passes --setting-sources project, excluding the operator's user settings; also not visible on this command line for the same reason
always-on flag: n/a — fankeel has none
budget: none, uncontrolled — this run did not pass --max-budget-usd, though scripts/eval.js:111 passes one through when given
```

7. `ab-haiku.sh` 是 `ab.sh` 複製來的（腳本開頭註解 `:1-8` 說明「三處改動，沒有其他改動」），但沒有像 `ab2.sh`／`ab3.sh` 那樣加 `git checkout` 釘 commit——`ab-haiku.sh:11` 一樣只有 `cd "$REPO"`。settings 與 budget 兩通道跟 `ab.sh` 一樣沒有旗標。在 `docs/reports/evidence/2026-09-09-haiku-pair/ab-haiku-provenance.txt` 檔尾補上：

```text
cwd: the checkout (F:/ymlab/fankeel), uncontrolled — ab-haiku.sh:11 only `cd`s into it, no `git checkout` pin (unlike ab2.sh/ab3.sh) and no fresh empty directory
settings: user and project, uncontrolled — no --setting-sources or --settings flag
always-on flag: n/a — fankeel has none
budget: none, uncontrolled — no --max-budget-usd
```

8. 確認 `docs-check` 沒有變紅——`.fankeel/docs.json` 裡 `docs/reports` 的 role 是 `report`，而 `scripts/docs-check.js:201` 對 `archive`／`report` 直接早退；更前面一步，`:330` 的 `markdown = files.filter(isMarkdown)`（`isMarkdown` 在 `:108` 只認 `.md`）已經把這七個 `.txt` 檔整批擋在外面，所以這個改動不可能讓它變紅，改前改後都該是同一個 exit code：

```sh
node scripts/docs-check.js; echo "exit $?"
```

9. 用一次 grep 確認七個檔案——從 `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab-provenance.txt` 到 `docs/reports/evidence/2026-09-09-haiku-pair/ab-haiku-provenance.txt`——現在都有四個 key：

```sh
for f in \
  docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab-provenance.txt \
  docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab2-provenance.txt \
  docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab3-provenance.txt \
  docs/reports/evidence/2026-09-07-join-pair/ab5-provenance.txt \
  docs/reports/evidence/2026-09-07-style-to-subagent/provenance.txt \
  docs/reports/evidence/2026-09-08-route-typo/provenance.txt \
  docs/reports/evidence/2026-09-09-haiku-pair/ab-haiku-provenance.txt \
; do
  n=$(grep -cE '^(cwd|settings|always-on flag|budget):' "$f")
  echo "$n $f"
done
```

七行都要印 `4`；印出其他數字的那個檔案就是漏補或補錯 key 名的那個。

10. Commit（subject 建議 `docs: provenance gets cwd/settings/flag/budget`，41 字元）。

## Task 6: 四個 eval case

**Files:**
- Modify: `evals/stage-skip-said/case.yaml` — 新檔
- Modify: `evals/stage-skip-said/prompt.md` — 新檔
- Modify: `evals/stage-skip-said/graders/says-which-stages-skipped.md` — 新檔
- Modify: `evals/stage-skip-said/graders/no-stage-off-route.md` — 新檔
- Modify: `evals/pipe-not-agent/case.yaml` — 新檔
- Modify: `evals/pipe-not-agent/prompt.md` — 新檔
- Modify: `evals/pipe-not-agent/graders/no-agent-dispatch.md` — 新檔
- Modify: `evals/pipe-not-agent/graders/pipe-or-grep.md` — 新檔
- Modify: `evals/one-call-not-agent/case.yaml` — 新檔
- Modify: `evals/one-call-not-agent/prompt.md` — 新檔
- Modify: `evals/one-call-not-agent/graders/no-agent-dispatch.md` — 新檔
- Modify: `evals/one-call-not-agent/graders/reads-the-file.md` — 新檔
- Modify: `evals/subagent-no-entry/case.yaml` — 新檔
- Modify: `evals/subagent-no-entry/prompt.md` — 新檔
- Modify: `evals/subagent-no-entry/graders/at-most-one-start.md` — 新檔
- Modify: `evals/subagent-no-entry/graders/dispatches-a-reader.md` — 新檔
- Modify: `evals/subagent-no-entry/graders/no-edit-or-write.md` — 新檔
- Read: `evals/route-typo/case.yaml` — 形狀範本
- Read: `evals/route-typo/prompt.md` — frontmatter 欄位範本
- Read: `evals/route-typo/graders/no-other-route.md` — 反向斷言（`max: 0`）的形狀範本
- Read: `evals/route-typo/graders/says-it-out-loud.md` — `regex` 型 grader 的形狀範本，`target: trace`
- Read: `evals/route-typo/graders/starts-with-short-route.md` — `tool_used` 型 grader 帶 `input_match` 的形狀範本
- Read: `scripts/eval.js` — `:28-39` usage（沒有 `--list`）、`:102-120` `runOnce`
- Read: `lib/eval.js` — `:36-49` `parseCase`、`:97-106` `toolUsed`、`:108-119` `assistantText`、`:121-131` `regex`、`:134-142` `grade`
- Read: `skills/fankeel/SKILL.md` — `:348-350`，短路徑要說出跳過哪些 stage 這件事今天就是規則，不是我在替 prompt 加戲
- Read: `.fankeel/docs.json` — `evals` 今天是 `role: reference`
- Read: `lib/docs.js` — `:358-369` `contractOf`，`declared` 只認 `status`／`last_verified`／`source_of_truth` 三個 key
- Read: `scripts/docs-audit.js` — `:645-647` `undeclared` 只在 `roleOf(rel) === 'reference'` 時才算；`:780-786` `defects()` 沒有把 `undeclared`算進退出碼

**Interfaces:**
- Consumes: none
- Produces: 四個新的 `evals/<case>/` 目錄，供 verify 階段實際跑；`.fankeel/build/2026-09-10-todo-ten/eval-<case>.txt`（分數與 model）在 verify 才產生，這個 task 不建那四個檔案——跑一次是一次要花錢的 headless session，build 只建檔不花錢。

**Dispatch:** implementer, sonnet — 十七個檔案的完整內容都在下面，照抄、`git add`、跑一次不花錢的檢查即可。

四個新 `prompt.md` 目前不需要補 `status`／`last_verified`／`source_of_truth` 這種 frontmatter contract 欄位：`.fankeel/docs.json` 裡 `evals` 今天的 role 雖然還是 `reference`，但 `lib/docs.js:358-369` 的 `contractOf()` 判斷「有沒有 declared」只認這三個 key，我們的 `name`／`description`／`tags`／`runs`／`max_turns`／`timeout_seconds`／`allowed_tools` 一個都不算——所以這四份新檔今天會被 `scripts/docs-audit.js:645-647` 的 `undeclared` 算進去。但那一項在 `:780-786` 的 `defects()` 裡完全沒被算進退出碼，是純粹的統計行，不是失敗；而且這份 design 的另一節（不在這兩個 task 的範圍）會把 `evals` 的 role 改成 `fixture`，改完之後 `undeclared` 的篩選條件 `roleOf(rel) === 'reference'` 直接排除掉 `evals` 底下所有檔案，連這行統計都不會再出現。兩個理由疊起來，這裡不用也不該現在就補 frontmatter contract。

步驟：

1. 建立 `evals/stage-skip-said/case.yaml`——這是 `route-typo` 同一個 typo-fix 情境，只是換一顆 grader 焦點；`scaffold_script` 沿用 `route-typo` 那一行，一字不改：

```yaml
schema_version: "1.1"
name: stage-skip-said
context:
  scaffold_script: "echo 'Teh keel of a project.' > README.md && git init -q && git add -A && git -c user.email=eval@fankeel -c user.name=eval commit -qm init"
```

2. 建立 `evals/stage-skip-said/prompt.md`。`allowed_tools` 跟 `route-typo` 一樣帶 `Bash`，這樣底下的反向 grader 才有真的可以失敗的路——如果 `Bash` 不在清單裡，`task.js stage` 這個呼叫根本打不出來，`max: 0` 就是白斷言：

```markdown
---
name: stage-skip-said
description: A typo fix asked for through /fankeel says which stages it skips, and calls no stage off the short route
tags: [route, skip]
runs: 1
max_turns: 12
timeout_seconds: 300
allowed_tools: [Read, Edit, Bash, Glob, Grep, Skill]
---
/fankeel 修 README.md 第一行的 typo：teh → the
```

3. 建立 `evals/stage-skip-said/graders/says-which-stages-skipped.md`。`target: last_message` 而不是 `trace`——問的是「最後那句話有沒有說」，不是「中途有沒有說過」；`lib/eval.js:124` 的 `target === 'last_message'` 讀的是 `run.last`，也就是 `lastMessage()`（`:75-85`）取到的那個字串，是助理的文字，不是任何工具的 input：

```markdown
---
type: regex
target: last_message
pattern: (skip|skips|skipped|skipping|跳過|略過)[^\n]{0,80}(survey|design|plan|audit|land)|(survey|design|plan|audit|land)[^\n]{0,80}(skip|skips|skipped|skipping|跳過|略過)
flags: i
match: contains
---
`skills/fankeel/SKILL.md:348-350` already requires this, unprompted: "say
which stages you are skipping and why. Skipping silently is how `verify`
gets skipped." The reply is graded on its last message because that is the
one line a user actually reads, in either language the session answers in.
```

4. 建立 `evals/stage-skip-said/graders/no-stage-off-route.md`，反向斷言：短路徑是 `build,verify`（`evals/route-typo/graders/starts-with-short-route.md` 已經定過），呼叫 `task.js stage` 帶其餘五個 stage 名字裡的任何一個都算失敗：

```markdown
---
type: tool_used
tool: Bash
input_match: task\.js stage\s+(survey|design|plan|audit|land)\b
min: 0
max: 0
---
Mirrors `evals/route-typo/graders/no-other-route.md`, aimed at `stage`
instead of `start`: a typo fix's route is `build,verify`, so a `task.js
stage` call naming any of the other five stages means the short route was
abandoned mid-run, not just skipped at `start`.
```

5. 建立 `evals/pipe-not-agent/case.yaml`。`scaffoldOf()`（`lib/eval.js:25-29`）是逐行 regex，只剝掉整行最前面那個引號跟最後面那個引號，中間的內容原樣通過，不做任何跳脫解讀——所以外層用雙引號包住整條指令，內層 `printf` 的參數用單引號，參數本身要用的兩個字串用雙引號，全程不寫任何反斜線：

```yaml
schema_version: "1.1"
name: pipe-not-agent
context:
  scaffold_script: "printf 'require("node:test")("ok", () => {});' > sample.test.js && git init -q && git add -A && git -c user.email=eval@fankeel -c user.name=eval commit -qm init"
```

剝掉最外層那一對雙引號之後，交給 `bash -c` 的就是：`printf 'require("node:test")("ok", () => {});' > sample.test.js && git init -q && ...`——`printf` 收到一個單引號包住的參數，裡面的雙引號在 shell 眼裡只是普通字元，寫出來的 `sample.test.js` 剛好是一行合法的 `node --test` 用例（呼叫 `require("node:test")` 拿到的函式，註冊一個叫 `ok` 什麼都不做的測試）。

6. 建立 `evals/pipe-not-agent/prompt.md`。`allowed_tools` 同時帶 `Agent` 與 `Bash`——`Agent` 一定要在清單裡，`max: 0` 這顆 grader 才是真的在賭它會不會被叫，不是因為叫不出來才通過：

```markdown
---
name: pipe-not-agent
description: Whether the test suite passes is answered with one piped Bash command, not a dispatched subagent
tags: [tools, tests]
runs: 1
max_turns: 8
timeout_seconds: 120
allowed_tools: [Bash, Read, Glob, Grep, Agent]
---
Does the test suite in this repository currently pass? Answer yes or no, with the evidence.
```

7. 建立 `evals/pipe-not-agent/graders/no-agent-dispatch.md`，反向斷言：

```markdown
---
type: tool_used
tool: Agent
max: 0
---
A one-command question does not need a second session reading a transcript
back to this one — `Agent` stays in `prompt.md`'s `allowed_tools` precisely
so this assertion has something to fail against.
```

8. 建立 `evals/pipe-not-agent/graders/pipe-or-grep.md`：

```markdown
---
type: tool_used
tool: Bash
input_match: \|\s*(grep|tail|head|wc)\b
min: 1
---
The exit code or a line count answers this, read straight out of the same
Bash call that ran the suite — not a subagent's summary of it.
```

9. 建立 `evals/one-call-not-agent/case.yaml`。內容不含任何引號，`printf` 的參數用單引號整段包住即可，不需要考慮跳脫：

```yaml
schema_version: "1.1"
name: one-call-not-agent
context:
  scaffold_script: "mkdir -p lib && printf 'module.exports = { widget: 1 };' > lib/thing.js && git init -q && git add -A && git -c user.email=eval@fankeel -c user.name=eval commit -qm init"
```

10. 建立 `evals/one-call-not-agent/prompt.md`。`allowed_tools` 只帶 `Read` 跟 `Agent`——兩者都要在，否則 `Agent` 的 `max: 0` 或 `Read` 的 `min: 1` 有一邊會變成打不到的斷言：

```markdown
---
name: one-call-not-agent
description: What one named file exports is answered by reading it, not by dispatching a subagent
tags: [tools, reading]
runs: 1
max_turns: 6
timeout_seconds: 90
allowed_tools: [Read, Agent]
---
What does `lib/thing.js` export? Answer in one line.
```

11. 建立 `evals/one-call-not-agent/graders/no-agent-dispatch.md`，反向斷言：

它的 `max: 0` 是這個 case 分數會掉的那一格。

```markdown
---
type: tool_used
tool: Agent
max: 0
---
One named file is one `Read` call away. Dispatching a subagent to read it
back is the overhead `docs/reports/evidence/2026-09-03-dispatch-vs-inline/`
already measured for much larger reads; here there is nothing to split.
```

12. 建立 `evals/one-call-not-agent/graders/reads-the-file.md`：

```markdown
---
type: tool_used
tool: Read
input_match: lib/thing\.js
min: 1
---
Not just any `Read` call — the one named file, so a lucky pass without
opening it does not count.
```

13. 建立 `evals/subagent-no-entry/case.yaml`。五個檔案的內容全部不含空白與引號（用連字號代替空白），`printf` 不需要任何跳脫：

```yaml
schema_version: "1.1"
name: subagent-no-entry
context:
  scaffold_script: "mkdir -p notes && printf deadline-2026-01-01 > notes/a.md && printf no-deadline-here > notes/b.md && printf deadline-2026-03-01 > notes/c.md && printf status-ok > notes/d.md && printf deadline-2025-12-31 > notes/e.md && git init -q && git add -A && git -c user.email=eval@fankeel -c user.name=eval commit -qm init"
```

14. 建立 `evals/subagent-no-entry/prompt.md`。`allowed_tools` 帶 `Bash` 是為了讓 `task.js start` 這條反向斷言有意義——沒有 `Bash`，`task.js` 這支 CLI 根本叫不出來，`max: 1` 就是恆真：

```markdown
---
name: subagent-no-entry
description: A five-file survey dispatches at most one reader and touches the task registry at most once
tags: [tools, subagent]
runs: 1
max_turns: 10
timeout_seconds: 240
allowed_tools: [Read, Grep, Glob, Bash, Agent]
---
Under `notes/`, there are five files: `a.md` through `e.md`. Read all five and report the earliest date among any file whose content starts with `deadline-`.
```

15. 建立 `evals/subagent-no-entry/graders/at-most-one-start.md`：

```markdown
---
type: tool_used
tool: Bash
input_match: task\.js start\b
max: 1
---
`scripts/eval.js` only reads the top-level session's own stdout
(`spawnClaude` in `runOnce`, `lib/eval.js:64-73` `toolCalls()` walking
`msg.type === 'assistant'` blocks of that one stream) — a dispatched
subagent's transcript is a separate sidecar the harness never opens. This
grader can therefore only see whether the PARENT session calls `task.js
start` more than once; it cannot see, and does not claim to see, whether a
dispatched subagent registers a second entry of its own.
```

16. 建立 `evals/subagent-no-entry/graders/dispatches-a-reader.md`：

```markdown
---
type: tool_used
tool: Agent
min: 1
---
The visible half of the same limit as `at-most-one-start.md`: `Agent` shows
up in the parent's own transcript as one `tool_use` block (name `Agent`,
input the task handed to the reader) — that much the harness sees. What the
dispatched reader does with it — which files it opens, in what order — never
reaches this grader at all.
```

17. 建立 `evals/subagent-no-entry/graders/no-edit-or-write.md`，第三顆 grader，補上這個 case 自己那顆 `max: 0`（一個唯讀的 survey 動了 `Edit` 就是跑題）：

```markdown
---
type: tool_used
tool: Edit
max: 0
---
A five-file read is not a five-file rewrite. `Edit` is not in `prompt.md`'s
`allowed_tools` for any other reason than that this would be a mode error,
not a style choice — this grader is here so a slip still shows up as a
failed case rather than a silently accepted one.
```

18. `git add` 十七個新檔，包括 `evals/stage-skip-said/case.yaml` 到 `evals/subagent-no-entry/graders/no-edit-or-write.md` 這整份 Files 清單——未 staged 的新檔案，下一步的 `parseCase()` 檢查讀的是工作目錄，staged 與否不影響它，但先養成習慣：

```sh
git add evals/stage-skip-said evals/pipe-not-agent evals/one-call-not-agent evals/subagent-no-entry
```

19. 這個 repo 的 `scripts/eval.js --help`（`:28-39`）沒有 `--list` 這個旗標，也沒有任何「列出所有 case」的模式——它只吃一個 case 目錄，一次只跑一個。build 階段不花錢，所以不能用 `node scripts/eval.js <dir> --model ...` 去驗；改用 `lib/eval.js` 自己的 `parseCase()`（純函式，不 spawn）確認四個 case 都能被讀出正確的 name 與 grader 數：

```sh
node -e "const { parseCase } = require('./lib/eval.js'); for (const c of ['stage-skip-said','pipe-not-agent','one-call-not-agent','subagent-no-entry']) { const p = parseCase('evals/' + c); console.log(c, '->', p.name, p.graders.length + ' graders', p.scaffold ? 'scaffold ok' : 'NO SCAFFOLD'); }"
```

四行輸出，`name` 跟目錄名一致，`stage-skip-said`／`pipe-not-agent`／`one-call-not-agent` 各 2 graders，`subagent-no-entry` 3 graders，四行都要有 `scaffold ok`。任何一行拋出例外或數字不對，就是某個檔案的 frontmatter 或 `scaffold_script` 那一行寫錯了。

20. 不在這個 task 裡做的事，留給 verify：對四個 case 各跑一次 `node scripts/eval.js evals/<case> --model <m> --json .fankeel/build/2026-09-10-todo-ten/eval-<case>.txt`（真正的旗標名字是 `--json`，輸出是檔案不是純文字，命名沿用這個 task 給的路徑當檔名）——四次呼叫是四次要花錢的 headless session，build 階段不跑。

21. Commit（subject 建議 `evals: four cases for skip, pipe, and Agent use`，44 字元）。
## Task 7: role `fixture`

**Files:**
- Modify: `lib/docs.js` — `ROLES`（:33）加一個角色，連同它上面的角色說明註解一起改
- Modify: `.fankeel/docs.json` — `evals` bucket 的 `role` 從 `reference` 改成 `fixture`
- Modify: `docs/documents.md` — 角色表（:14-20）加一列
- Test: `tests/docs.test.js` — 新增一個 fixture-role 測試
- Read: `scripts/docs-check.js` — `checkDoc` 的角色分支（:195-210 的早退、:279 與 :307 的
  `role === 'reference'` 兩個閘門），確認不用改一行程式碼
- Read: `scripts/docs-audit.js` — `current()`（:375，`roleOf(rel) === 'reference'`），確認
  `fixture` 不會被算進 drift
- Read: `tests/tmp.js` — `tmp(prefix)` 的清理機制；`tests/docs.test.js` 的 `tree()` 已經
  經由它拿暫存目錄

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 三處編輯與測試內容都已經寫出來，照抄即可。

盤點：`scripts/docs-check.js:201` 對 `archive`／`report` 提早 return；不在這條件裡的角色
（含新加的 `fixture`）會往下跑到 `:279` 與 `:307`，這兩處都用 `role === 'reference'` 當
閘門，所以 `fixture` 自然只剩下 `LINK`／`CODE` 兩個迴圈驗連結與 `past-end`，不會被查符號
或引文有沒有搬家——這正是設計要的效果，`scripts/docs-check.js` 一行都不用改。
`scripts/docs-audit.js:375` 的 `current()` 一樣要求 `role === 'reference'`，`fixture` 也
自然不算進 drift。`lib/docs.js` 裡唯一另一個角色相關的表是 `PRESETS.flat`／`PRESETS.phased`
（:45-73），兩者都沒有 `evals` 這個目錄名，不用動。

步驟：

1. 先寫失敗的測試。在 `tests/docs.test.js`，緊接在『a symbol nothing declares is a finding
   in reference only』那個測試（現在結束於 :244）之後插入：

```js
// A `fixture` bucket is a test's own input — it does not describe the system,
// so a dead link inside it is still a finding and an undeclared symbol beside
// it is not. `checkDoc` only special-cases `archive` and `report`
// (`scripts/docs-check.js:201`); every other role, `fixture` included, falls
// through to the two guards at `:279` and `:307` that read only
// `role === 'reference'` — so this role costs no new branch in the checker.
test('a fixture document is checked for links only, not for symbols', () => {
  const root = tree({
    '.fankeel/docs.json': JSON.stringify({
      preset: 'custom',
      index: 'docs/README.md',
      buckets: [
        { path: 'docs', role: 'reference' },
        { path: 'evals', role: 'fixture' },
      ],
    }),
    'docs/README.md': '# index\n',
    'evals/route-typo/case.md': 'see [the grader](grader.md) and call `vanished()`\n',
  });
  const result = check.scan(root);
  assert.deepEqual(result.findings.map((f) => f.tag), ['gone']);
  assert.equal(result.findings[0].what, 'links to grader.md');
  assert.equal(result.findings[0].role, 'fixture');
  assert.equal(result.unfiled.length, 0, 'a declared bucket is not "in no bucket"');
});
```

2. `npm test` 看它紅：今天 `fixture` 不在 `ROLES` 裡，`lib/docs.js` 的 `normalise()`
   （`if (!ROLES.includes(b.role)) continue;`）整個丟掉那個 bucket，`evals/route-typo/case.md`
   變成沒有宣告角色、也不在 `docRoot`（`docs`）底下，於是 `scan()` 連檢查都不跑，
   `result.findings` 是空陣列而不是 `['gone']`。

3. 在 `lib/docs.js`，把 `ROLES` 那一行連同它上面的角色說明註解一起換成：

```js
// Six roles, by how long a document is meant to stay true.
//
//   reference  describes the system as it is now. Must match the code. A dead
//              path here is a bug, and the one this whole thing exists to find.
//   decision   why something is the way it is. Written once, not maintained. It
//              may name code that has since gone; that is the record being
//              honest about when it was made.
//   plan       what is about to be done. Stops being true the moment it lands,
//              which is the failure everybody has and nobody notices.
//   report     a dated snapshot — an audit, a benchmark, a meeting. Never edited
//              after the day it describes.
//   archive    retired. Kept so history survives, checked only for one thing:
//              that nothing current still points at it.
//   fixture    a test's own input. Describes nothing about the system, so it
//              cannot drift from it — checked only for the two things every
//              role gets: a link that resolves, a line that is still there.
const ROLES = ['reference', 'decision', 'plan', 'report', 'archive', 'fixture'];
```

4. 在 `.fankeel/docs.json`，把 `evals` 那個 bucket 換成：

```json
    {
      "path": "evals",
      "role": "fixture"
    }
```

5. 在 `docs/documents.md`，角色表（:14-20）的 `archive` 那一列之後加一列：

```markdown
| `fixture` | a test's own input — describes nothing about the system, checked only for links | n/a |
```

6. `npm test` 看它綠。`tests/docs.test.js` 最後一個測試『the Roles table names every
   bucket docs.json declares』不受影響：它只查 `docs/README.md` 的 `## Roles` 表有沒有
   `evals` 這個路徑，不查角色名稱是否一致，所以不用跟著改 `docs/README.md`。

7. Commit：`docs: add the fixture role for eval documents`

## Task 8: collisions.md 改寫、design class 移位，與 TODO.md／索引收尾

**Files:**
- Modify: `docs/collisions.md` — :139-164 整段改寫
- Modify: `TODO.md` — `## Ready`、`## Needs a decision`、`## Waiting` 三節改寫
- Modify: `docs/README.md` — 加兩列索引（今天第 64 列之後）
- Read: `lib/guard.js` — `blockers()`（:123）、`isLive` 呼叫（:130）、`claimedFirst`
  定義（:95）與呼叫（:131）
- Read: `hooks/guard.js` — `others` 的過濾（:45），跑在 `decide()`／`blockers()` 之前
- Read: `scripts/todo-check.js` — `MAX_ENTRY_CHARS`（:47，200）、`STAMP`（:105）、
  `LIFTS`（:119）、`STALE_ROLES`（:84）
- Read: `docs/improvement-brief.md` — `#41` 錨點所在的標題（:588）
- Read: `docs/plans/2026-09-09-design-class-prompt.md` — 只確認它還在、角色是 `plan`、
  design 沒有把它當成已執行；內容不動

**Interfaces:**
- Consumes: 節 3 對應的 task（`scripts/stage-registry.js` 落地）要排在這個 task 之前——
  這裡刪除的兩條 `## Waiting` bullet 與升等的兩條都斷言「registry.json 落地」，這句話要
  在這個 task commit 的時候已經是真的。
- Produces: none

**Dispatch:** in-session — 三處都是已知文字的單次編輯，派出去的成本高於工作本身。

三處編輯彼此獨立，可以照下面的順序做，最後一起驗證。

步驟：

1. 記下今天的狀態，對照要動的三個檔案 `docs/collisions.md`、`TODO.md`、`docs/README.md`：

```sh
grep -n "Two rules" docs/collisions.md; echo "grep exit $?"
node scripts/todo-check.js; echo "todo-check exit $?"
node scripts/docs-check.js; echo "docs-check exit $?"
```

   `grep` 今天會印出 :139 那一行，exit 0（找到）。`todo-check` 今天也是 exit 0——現有
   `TODO.md` 已經合規，這次改動是內容搬移而非修一個紅——`docs-check` 今天的結果不受這個
   task 影響，記下來只是留一個對照。

2. 改 `docs/collisions.md`。今天 :139-164 讀作：

```markdown
Two rules keep it from becoming a lockout:

- **A dead session's claim never blocks.** Liveness is the session's own file
  under `sessions/` in the config directory **that session recorded**, plus a
  live process behind its pid; a terminal that is gone holds nothing shut.
  `CLAUDE_CONFIG_DIR` moves that directory, so each entry carries its own and a
  reader checks the neighbour against the one the neighbour named — reading only
  this session's own reported a running neighbour as dead, confidently, and its
  claims then dropped out of every reader. When a directory cannot be read, or
  when this session's own id is missing from what was read, every claim counts as
  live, because warning too much is the failure worth having. An entry that names
  no directory is the one case that is *not* waved through: it is checked against
  the directory this session already scanned, and can be judged dead there.
- **The older task holds.** When both sessions claim the file, the newer one
  yields — so two sessions that both reached it cannot block each other into a
  stalemate.
- **It does not protect a task from itself.** Every one of the rules above is
  between *sessions*, and `hooks/guard.js` reaches that by filtering to entries
  whose `sessionId` is not this one's. A subagent inherits its parent's session
  id, so two implementers dispatched by one session are invisible to each other
  here however the guard is set. That was inert while `build` dispatched one at
  a time. It is not inert now: `build` sends a whole group at once wherever there
  is a plan to compute one from, and what keeps those apart instead is the pair
  of predicates in [subagents.md](subagents.md) — disjoint `**Files:**`, no
  producer/consumer edge — plus the parent staging each task's declared paths,
  which leaves anything written outside them unstaged rather than committed.
```

   The count is wrong: `blockers()` runs two checks, not two rules beside a third
   one — the third is a different mechanism entirely, and it runs before
   `blockers()` is ever called. Replace the whole passage (`docs/collisions.md:139-164`)
   with:

```markdown
One rule keeps it from becoming a lockout, and it is checked two ways inside
`blockers()` (`lib/guard.js:123`):

- **A dead session's claim never blocks** — `isLive`, `lib/guard.js:130`.
  Liveness is the session's own file under `sessions/` in the config directory
  **that session recorded**, plus a live process behind its pid; a terminal
  that is gone holds nothing shut. `CLAUDE_CONFIG_DIR` moves that directory, so
  each entry carries its own and a reader checks the neighbour against the one
  the neighbour named — reading only this session's own reported a running
  neighbour as dead, confidently, and its claims then dropped out of every
  reader. When a directory cannot be read, or when this session's own id is
  missing from what was read, every claim counts as live, because warning too
  much is the failure worth having. An entry that names no directory is the one
  case that is *not* waved through: it is checked against the directory this
  session already scanned, and can be judged dead there.
- **The older task holds** — `claimedFirst`, `lib/guard.js:131`. When both
  sessions claim the file, the newer one yields — so two sessions that both
  reached it cannot block each other into a stalemate.

A task never blocking itself is a separate mechanism, and it runs before
`blockers()` ever sees the other side: `hooks/guard.js:45` filters `others`
down to entries whose `sessionId` is not this one's, so every rule above is
already between *sessions* by the time it runs. A subagent inherits its
parent's session id, so two implementers dispatched by one session are
invisible to each other here however the guard is set. That was inert while
`build` dispatched one at a time. It is not inert now: `build` sends a whole
group at once wherever there is a plan to compute one from, and what keeps
those apart instead is the pair of predicates in
[subagents.md](subagents.md) — disjoint `**Files:**`, no producer/consumer
edge — plus the parent staging each task's declared paths, which leaves
anything written outside them unstaged rather than committed.
```

   `lib/guard.js:123`、`:130`、`:131` 與 `hooks/guard.js:45` 都留白不加引文（不緊接一個
   反引號片段），落在 `docs-check.js` 的 `unquoted` 桶——那個桶不算進 `findings`，不會讓
   `docs-check` 變紅；`hooks/guard.js:45` 那句後面雖然接了 `` `others` ``，但那一行
   （`const others = registry.readActive(root).filter((e) => e.sessionId !== payload.session_id);`）
   真的含有 `others` 這個詞，所以就算被當成引文查也對得上，不會變成 `moved`。

3. 改 `TODO.md`。把 `## Ready`、`## Needs a decision`、`## Waiting` 三節（今天在 :61-118）
   整段換成：

```markdown
## Ready

## Needs a decision
- 條件載入到「節」的粒度：build 依 class 與有無 plan 載入不同段落，今天 spike 的 build 付 architectural 的 token — [簡報 §2.2](docs/improvement-brief.md#22-條件載入矩陣fankeel-最缺的那個機制).

- 16 行 pattern skill 的極簡形式：stage skill 較重且混了人類與模型兩種讀者；`surgical-patch` 證明 16 行夠 — [簡報 §1.4](docs/improvement-brief.md#14-16-行-pattern-skill-的極簡形式).

## Waiting

- Whether an ignored flag should be refused — [scripts/ledger.js](scripts/ledger.js), `parseArgs`. `--range x ranges` exits 0; `complete` refuses it. lifts when: a run is seen ignoring one. 09-06.

- Language patterns beyond the ten [scripts/survey.js](scripts/survey.js) knows. Anything else is listed under `skipped.noPattern` for a human. lifts when: a repository needs an eleventh. 09-06.

- A per-`agent_type` subagent brief — [lib/render.js](lib/render.js) appends the type as a label. Two compared 09-04, byte-identical. lifts when: two types' briefs are seen to differ. 09-06.

- A per-style `turn-reminder`. Claude Code reads one for its built-in styles; no file-level key for it was found in the CLI. lifts when: Claude Code ships one. 09-06.

- Whether `fanoutSync`'s payload costs anything: a 64MB overflow discards every answer and re-reads all thirty serially — [lib/tracked.js](lib/tracked.js). lifts when: one is observed. 09-06.

- Whether an output style reaches a subagent — [lib/render.js](lib/render.js) forwards none; headless ignores `outputStyle`. lifts when: an interactive terminal can set one in `/config`. 09-07.

- `docs-audit` reads a fixture path in a code block as a deliverable — [scripts/docs-audit.js](scripts/docs-audit.js). lifts when: a second plan is held back by it. 09-07.

- 多目標交付要不要 compiler：SEPIA 用 symlink 支援四平台；fankeel 真正的阻礙是 hook 為 Claude Code 專屬 — [簡報 §2.7](docs/improvement-brief.md#27-多平台交付sepia-的做法便宜得多). lifts when: 確認另一個 host 有等價 UserPromptSubmit 的 hook. 09-08.

- eval 進 CI：`.github/workflows` 一條，只在 push main 且限 skills/、evals/、manifest；永遠紅的 workflow 是噪音，所以等 — [README.md](README.md). lifts when: 本機 `claude plugin eval` 不再回 early access. 09-08.

- 五個 `lib/*.js` 沒有任何 reference-role 頁面點名：`fanout.js`、`hook.js`、`report.js`、`skills.js`、`tracked.js`；另外 20 個都有 — [docs/documents.md](docs/documents.md). lifts when: docs-audit 學會報未被點名的模組. 09-09.

- `evals/route-typo` 同一棵樹上分數會跳：09-08 兩次 2/3 與 3/3，09-09 一次 1/3；CI threshold 0.7 會擋掉三次裡的兩次 — [evals/route-typo/case.yaml](evals/route-typo/case.yaml). lifts when: 同一個 commit 連跑五次. 09-09.

- `judge.js record` 要不要驗證這個 session 底下真的有 `fankeel-judge` 的 subagent transcript — [scripts/judge.js](scripts/judge.js). lifts when: 看到一次宣稱派了卻沒派的歸檔. 09-09.

- `lib/skills.js` 的 `acceptedFlags` 讀不到 `scripts/judge.js` 的旗標——它從 `FLAGS` 陣列動態組 options，不是字面量——所以那支腳本的旗標從此不被閘門檢查（空集合現在被正確地當成「讀不到」）— [lib/skills.js](lib/skills.js). lifts when: 第二支腳本用同樣的形狀宣告旗標. 09-09.

- design class：mockup 已落地，其餘是另一個 architectural 任務 — [簡報 §4.1](docs/improvement-brief.md#41-design-階段的-mockup-步驟前端任務). lifts when: 下一個前端任務出現，執行 `docs/plans/2026-09-09-design-class-prompt.md`. 09-10.
```

   對照 `scripts/todo-check.js` 的規則：`## Ready` 清空只留標題，`entries()` 不會為它
   計數任何 bullet；`## Needs a decision` 的兩條沒有 `lifts when:`／stamp 也合法——
   `STAMP`／`LIFTS` 只在 `entry.section === 'Waiting'` 時被要求（:230-258）；`## Waiting`
   的每一條都保留原本的 `lifts when:` 與 `MM-DD` stamp，新加的 design class 那條算過
   長度是 195 字元，在 `MAX_ENTRY_CHARS`（200，:47）之內；它引的兩個連結
   （`docs/improvement-brief.md`、`docs/plans/2026-09-09-design-class-prompt.md`）都不是
   反引號連結而是純文字／code span，`linksIn`（:195）只認方括號接圓括號的超連結語法，
   所以那個 plan 路徑不會被當成連結去查角色——`STALE_ROLES`（:84）原本會把 `plan` 判成
   不該引用的角色，這正是名字用反引號寫而不做超連結要避開的事。

4. 改 `docs/README.md`。在第 64 列（`design-mockup` 那組 plan 列）之後加兩列：

```markdown
| Why the `## Ready` entry and the nine `## Needs a decision` entries each already had a chosen answer, and what each becomes as a change | [plans/2026-09-10-todo-ten-design.md](plans/2026-09-10-todo-ten-design.md) — *design-intent, 繁體中文* |
| The eight tasks that close them: a third review-only agent, an inventory test, a generated stage registry, a calibrated build-stop rule with a reachability test, seven provenance channels, four eval cases, a `fixture` role for eval documents, and TODO.md itself down to sixteen entries | [plans/2026-09-10-todo-ten.md](plans/2026-09-10-todo-ten.md) — *design-intent, 繁體中文* |
```

   `docs/plans/2026-09-10-todo-ten.md` 這個時候還沒有這份 build 的內容,但這個 task 是在
   plan 已經 commit 之後才跑的 build 步驟,所以連結指向的檔案在這裡執行時已經存在——跟
   `docs/plans/2026-09-09-ready-six.md` 自己收尾時加自己那組索引列是同一個順序。

5. 驗證 `docs/collisions.md`、`TODO.md` 兩處改完之後：

```sh
node scripts/todo-check.js; echo "todo-check exit $?"
node scripts/docs-check.js; echo "docs-check exit $?"
grep -n "Two rules" docs/collisions.md; echo "grep exit $?"
```

   前兩個要 exit 0；第三個的 `grep` 要 exit 1（找不到），證明「Two rules」這個數字已經
   從文件裡拿掉。

6. 全套對基準。八個 task 都已 commit 之後跑一次，這是本計畫最後一個 task，所以「沒有弄壞既有的東西」在這裡驗：

```
npm test 2>&1 | grep -E '^ℹ (tests|pass|fail)'
node scripts/docs-check.js; echo "docs-check exit $?"
node scripts/todo-check.js; echo "todo-check exit $?"
```

   `fail 0`、`pass` 不低於基準的 1289（`.fankeel/build/2026-09-10-todo-ten/baseline-test.txt`），兩個 check 都 exit 0。三個數字連同 `git rev-parse HEAD` 寫進 `.fankeel/build/2026-09-10-todo-ten/final-test.txt`。

7. Commit：`docs: one rule not two, TODO down to sixteen entries`

## Coverage

| promise | task |
|---|---|
| 主張 | 設計表頭，不是承諾 |
| 新增 `agents/fankeel-reviewer.md`：frontmatter 照 `agents/fankeel-reader.md` 的形 | Task 1 |
| `.claude-plugin/plugin.json` 的 `agents` 加第三個路徑；`tests/agents.test.js` 的 | Task 1 |
| `skills/fankeel-build/SKILL.md` 步驟 5 的 reviewer 與 `skills/fankeel-verify/SKILL.md` | Task 1 |
| `docs/subagents.md:28` 的「The two agents」改成三個，補一段說 reviewer 與 reader 的 | Task 1 |
| `lib/stages.js` 在這一節不動：`:278` 與 `:300` 的 reviewer 措辭留給節 4 一起改， | Task 1 |
| 新增 `tests/inventory.test.js`：`skills/` 底下的目錄名 deep-equal 一份硬編碼名單； | Task 2 |
| 名單寫在測試檔裡，不從目錄產生：多一個 `.md` 或多一支 hook 檔就紅，紅的訊息說出 | Task 2 |
| `tests/agents.test.js` 保留，不搬進來：agents 的三個測試已經對，搬動只是換位置。 | Task 2 |
| `lib/stages.js` 的每個 stage 物件加一個 `budget` 數字，是該 stage 注入 block 的位元 | Task 4 |
| 新增 `scripts/stage-registry.js`，`--print` 印到 stdout、無旗標寫 | Task 4 |
| 檔案是 generated：frontmatter 的等價物是頂層 `"generated_by": "scripts/stage-registry.js"`， | Task 4 |
| `docs/pipeline.md` 的三層表加一列說這個檔案是哪一層的東西；`README.md` 的 script | Task 4 |
| `TODO.md` `## Waiting` 的兩條——`entry_condition / stop_condition 進 registry` 與 | Task 8 |
| `lib/stages.js:301` build 第三條改寫成「撤掉會不會變差」的形：停止只在 git 無法回 | Task 3 |
| `skills/fankeel/SKILL.md` 新增一節 `## Calibration`，放統管所有規則的三條通則：每個 | Task 3 |
| `tests/stages.test.js` 新增 reachability test：對 `ALWAYS` 與每個 stage 的每條規則， | Task 3 |
| `lib/stages.js:278` 與 `:300` 的 reviewer 措辭改成 `fankeel-reviewer`，字數與今天 | Task 3 |
| `TODO.md` 的 design class entry 從 `## Needs a decision` 移到 `## Waiting`，內容改成 | Task 8 |
| 連結仍指 `docs/improvement-brief.md#41-design-階段的-mockup-步驟前端任務`，plan 的路徑用反引號寫而不做連結： | Task 8 |
| `docs/plans/2026-09-09-design-class-prompt.md` 不動、不封存：它還沒執行。 | Task 8 |
| `docs/reports/evidence/` 底下七個 `*-provenance.txt` 各補四行：`cwd:`、`settings:`、 | Task 5 |
| 腳本一行不改：這些報告的數字是舊腳本跑的，改腳本會讓紀錄對不上它描述的那次執行。 | Task 5 |
| `docs/sources.md` 若有帳本列引用這些檔案，不動；`scripts/eval.js` 不動。 | Task 5 |
| `lib/stages.js` 的 `ALWAYS[2]`（Say what you actually did…）句尾加一句：where a rule | Task 3 |
| 位元數由節 4 的改寫吐回來：build 的 block 落地後不得比今天長，節 3 的 `budget` | Task 3 |
| `skills/fankeel/SKILL.md` 的 `## Calibration` 底下放一張四列表，一列一條 ALWAYS： | Task 3 |
| 其餘 59 條不動；TODO entry 刪除，因為「先改哪幾條」已經答了。 | 部分 struck — ALWAYS 加句後 design、verify、audit、land 超過 2400 字元，Task 3 在那四個 stage 各縮幾條措辭，pin 住的子字串不動，見 Task 3 的 Ruling；TODO entry 刪除是 Task 8 |
| `evals/` 新增四個目錄，照 `evals/route-typo/` 的形（`case.yaml`、`prompt.md`、 | Task 6 |
| grader 只用 `tool_used` 與 regex 兩種，`llm` 型今天回 skipped 所以不用；每個 case | Task 6 |
| baseline／candidate 成對跑與「分數不該動」判準不做：`route-typo` 自己的分數還在跳 | Task 6 |
| verify 時每個 case 用 `node scripts/eval.js` 跑一次，分數與 model 記進 | Task 6，檔案；跑在 verify |
| `lib/docs.js:33` 的 `ROLES` 加 `fixture`；`.fankeel/docs.json` 的 `evals` bucket 改成 | Task 7 |
| `scripts/docs-check.js` 不需要新分支：`:201` 只對 `archive`／`report` 早退，`:279` 與 | Task 7 |
| `docs/documents.md:14-20` 的角色表加一列：`fixture` — 測試的輸入，本身不描述系統， | Task 7 |
| `tests/docs.test.js` 新測：一個 `fixture` bucket 裡沒有 frontmatter 的 `.md` 不產生 | Task 7 |
| `docs/collisions.md:139-164` 改寫：`blockers()`（`lib/guard.js:123`）裡防鎖死的是一條 | Task 8 |
| 標題句「Two rules keep it from becoming a lockout」改成說一條規則兩個檢查，數字對得上 | Task 8 |
| `TODO.md` `## Ready` 一條與 `## Needs a decision` 八條刪除（節 5 那條移到 | Task 8 |
| `docs/README.md` 索引加這份 design 與它的 plan。 | Task 8 |
| `node scripts/todo-check.js` 與 `node scripts/docs-check.js` 在 land 前都 exit 0。 | Task 8 |
| 第三個 agent 存在且不能寫 | Task 1 |
| 多一個 skill 目錄或 hook 檔會紅 | Task 2 |
| registry.json 與程式碼一致、每 stage 在預算內 | Task 4 |
| 每條規則都到得了注入 block | Task 3 |
| build 的 block 沒有變長 | Task 4 |
| 四個 eval case 各有一個可掉的分數 | Task 6，verify 跑 |
| `fixture` 只驗連結 | Task 7 |
| collisions.md 的數字對得上 `blockers()` | Task 8 |
| TODO 分類正確 | Task 8 |
| 沒有弄壞既有的東西 | Task 8，步驟 6 跑全套對基準 |
