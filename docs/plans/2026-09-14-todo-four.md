---
status: design-intent
last_verified: 2026-09-15
---

# `## Needs a decision` 的四條 Implementation Plan

**Goal:** 關掉 `TODO.md` 的 `## Needs a decision` 四條 —— station 的不上站專案、gate 的選項標籤與彙總、開站方式，以及 `hooks/guard.js` 的 `agent_type`。

**Architecture:** 不上站的過濾只加在 `lib/station.js:375-380` 的 `flatten()` 一處，因為那是每個 session 進入頁面資料的唯一一點，頁面端十一處各自加總與渲染都在它下游；不在下游的四處（`projects` 映射、detail 檔迴圈、`--json`、文字回覆）各自呼叫同一個 `lib/station.js` 匯出的判斷函式。gate 那條把 `lib/replay.js` 已經讀出來又丟掉的 option label 留下來，clip 從 120 提到 240，並在 `serialize()` 加一個跨 session 彙總欄位給首頁一格 —— 因為 gate 事件目前只存在 per-session 的 detail 檔裡，首頁看不到。guard 那條在 `:45` 前加一個 `agent_id` 存在性檢查。開站那條是一個新技能加頁面對 `station/health` 的輪詢。

**Tech Stack:** Node.js（`node --test`，無 runtime dependencies），CommonJS，Windows 為主要開發平台。fankeel 0.67.0。

**Spec:** [2026-09-14-todo-four-design.md](2026-09-14-todo-four-design.md)

## Global Constraints

從 `CONTRIBUTING.md` 的 Scope and ownership 表：

- `lib/*.js` 是純函式、直接測。**`lib/` 不得 reach into `scripts/` 或 `hooks/`，只有反方向。**
- `scripts/*.js` 是 `lib/` 的薄包裝。station CLI 新增一個旗標要在 `docs/station.md` 有一列，否則 `tests/station-doc.test.js` 紅。**本計畫不新增任何旗標。**
- **每個 hook 在每條路徑上都 exit `0`，包含它自己出錯時。**
- 技能要薄：不要把路由表、領域規則或共用慣例從擁有它的技能複製到包裝層。
- 測試跑 `node --test`。每個 exported name 都要有 importer，而**新檔要先 `git add` 之後 `tests/source.test.js` 才看得到**。
- 文件歸檔照 `.fankeel/map.md`。**新頁或改名的頁在同一個改動裡拿到 `docs/README.md` 的索引列。**
- 絕不手改 `station.js serve` 寫出來的檔。一個名字不再被 emit 時，要手動從 committed 的 `.fankeel/.gitignore` 移除 —— 那裡是附加自動、移除不自動。
- `TODO.md` 一條一件，歸在 `## Ready`、`## Needs a decision`、`## Waiting` 之下，細節不寫在那裡。
- 版號跑 `scripts/version.js`，它讓十二個檔一致；手改任何一個就是它們開始不一致的方式。

從 `package.json`：

- `"test": "node --test"`，`"private": true`，version `0.67.0`。**沒有 `dependencies` 欄位 —— 不得新增任何依賴。**

從 `knip.json`：

- `entry` 是 `hooks/*.js`、`scripts/*.js`、`tests/**/*.test.js`；`exclude: ["exports"]`（unused exports 那格關著，見 `TODO.md` 的 `## Waiting`）。

從 `.fankeel/map.md`：

- 索引是 `docs/README.md`。歸檔：`docs` reference、`docs/plans` plan、`docs/decisions` decision、`docs/reports` 與 `docs/judgements` report、`docs/archive` archive、`skills` 與 `agents` reference、`evals` fixture。
- `planned, not built` 兩份：這份計畫引的設計檔，與 `docs/plans/2026-09-09-design-class-prompt.md`。

從測試套件與程式碼，照抄的確切值：

- `lib/profile.js:17` 的 `KEYS` 每個值都是**字串**。布林鍵的慣例是 `['true', 'false']`（`land.push`，`:19`）。`builtin` 可以是 `null`（代表問使用者）或值裡的一個字串（`guard` 是 `'ask'`，`dispatch.floor` 是 `'sonnet'`）。
- `lib/registry.js:35` `MAX_NEXT_LEN = 120` —— 與 `lib/replay.js` 的 clip **無關**，不動。
- `lib/detail.js:138` `.slice(0, 120)` —— 也無關，不動。
- `tests/inventory.test.js:13` 的 `SKILLS` 是**手寫**陣列；`:35-47` 要求 `skills/` 底下的目錄恰好是該陣列，且 `skills/` 底下的檔案恰好是 `['registry.json']`；`:49-54` 要求每個列出的目錄有 `SKILL.md`。
- `tests/station-doc.test.js:16` 從 `scripts/station.js` 以 `/a === '(--[a-z]+)'/g` 抓旗標，每一個都要出現在 `docs/station.md`。
- `tests/stages.test.js:833` 要求每個 stage 的 `when(key)` 命名的鍵存在於 `KEYS` —— 加鍵不會讓它紅，移除或改名才會。
- `lib/skills.js:63-66` 的 `REQUIRED_CORE` 含 `station.js`，已由 `skills/fankeel/SKILL.md:650` 滿足。
- `lib/skills.js:103` 的 `unknown-flag` 是 fail；`scripts/station.js:51-59` 的 `serve` 與八個旗標都是字面量，認得。
- `scripts/station.js:425` 的 `/station/health` 回 `{ station: true, pid: process.pid, started }`。

## 檔案結構

| 檔案 | 責任 | 誰動它 |
|---|---|---|
| `lib/profile.js` | profile 鍵表與唯一的驗證點 | Task 1 |
| `lib/station.js` | 蒐集、過濾、序列化 —— 頁面資料與 detail 檔的唯一來源 | Task 1（過濾）、Task 3（gate 彙總） |
| `scripts/station.js` | CLI 薄包裝：`--json` 與文字回覆 | Task 1 |
| `lib/replay.js` | 從 transcript 重建事件，含 gate 的問答 | Task 2 |
| `assets/station/station.js` | 頁面渲染 | Task 3（一格）、Task 6（輪詢） |
| `hooks/guard.js` | 寫入前的範圍守衛 | Task 4 |
| `skills/fankeel-station/SKILL.md` | 新技能，只跑一條命令 | Task 5 |
| `docs/collisions.md` | 守衛的 reference 頁 | Task 4 |
| `docs/station.md` | station 的 reference 頁 | Task 7 |
| `docs/README.md` | 手維護的索引 | Task 7 |
| `TODO.md` | 四條條目，由交付它的任務移除 | Task 1、3、4、6 |

## Task 1: 不上站的專案

**Files:**
- Modify: `lib/profile.js` — `KEYS` 加 `station.hide`
- Modify: `lib/station.js` — 匯出 `hiddenPkeys()`；`flatten()`、`projects` 映射、detail 檔迴圈、`tally()` 四處過濾；`write()` 回傳加 `hidden` 計數
- Modify: `scripts/station.js` — `--json` 呼叫 `hiddenPkeys()` 過濾，文字回覆印 `out.hidden`
- Modify: `TODO.md` — 移除 `## Needs a decision` 的「設定檔指定不上站的專案」那條
- Read: `assets/station/station.js` — `windowTotals`、`lastDays`、`kpiHtml` 三個已匯出的純函式，第 10 步的兩個數字都用它們算
- Read: `tests/brief.test.js` — `:36-42` 的 `execFileSync` 形狀，第 9 步照它寫
- Test: `tests/station-hide.test.js`
- Test: `tests/profile.test.js`

**Interfaces:**
- Consumes: 無
- Produces: `hiddenPkeys(model) -> Set<string>`，集合成員是 pkey 形狀的字串（`root + '/' + project`，或單獨的 `root`），由 `lib/station.js` 匯出

**Dispatch:** implementer, sonnet — 六處改動的位置與程式碼都在下面，抄寫加測試。

第一步不是寫程式，是確認一個會讓整條改動靜默失效的前提。`lib/station.js:332-333` 用 `path.join(root, p)` 組 `projectDirs`，Windows 上得到反斜線；`:446` 的 `pkey` 是 `s.root + '/' + s.project`，是正斜線。兩邊直接比字串在 Windows 上永遠不相等。下面的 `hiddenPkeys()` 就是為了這件事而把鍵正規化的，**不要簡化掉它**。

在 `lib/profile.js` 的 `KEYS` 物件裡，接在 `'design.mockup'` 那一行之後，加：

```js
    'station.hide': { values: ['true', 'false'], builtin: 'false' },
```

在 `lib/station.js`，緊接在 `flatten()` 之前，加：

```js
// A project whose profile says `station.hide: 'true'` does not go on the page
// at all — not as a row, not inside a total, not as a detail file on disk.
// One function, because the page, the detail writer and the two CLI surfaces
// each filter separately and a second copy of this judgement is a second
// judgement that will drift.
//
// `r.profiles` is keyed by `path.join(root, project)` — backslashes on Windows
// — while `pkey` is `root + '/' + project`. Comparing the two raw is how this
// filter silently never fires, so the key is normalised to pkey's shape here.
function hiddenPkeys(model) {
    const hidden = new Set();
    for (const r of model.registries) {
        const profiles = r.profiles || {};
        for (const dir of Object.keys(profiles)) {
            const values = profiles[dir] && profiles[dir].values;
            if (!values || values['station.hide'] !== 'true') continue;
            if (dir === r.root) { hidden.add(r.root); continue; }
            const rel = path.relative(r.root, dir).split(path.sep).join('/');
            hidden.add(r.root + '/' + rel);
        }
    }
    return hidden;
}

function pkeyOf(session) {
    return session.project ? session.root + '/' + session.project : session.root;
}
```

在 `lib/station.js` 的 `flatten()` 裡，把迴圈主體換成：

```js
function flatten(model) {
    const sessions = [];
    const hidden = hiddenPkeys(model);
    for (const r of model.registries) {
        for (const s of r.sessions) {
            const row = Object.assign({ root: r.root }, s);
            if (hidden.has(pkeyOf(row))) continue;
            sessions.push(row);
        }
    }
    return sessions;
}
```

在 `lib/station.js` 的 `serialize()` 裡，`projects:` 那一行（`:412`）換成：

```js
        projects: Object.assign({}, ...model.registries.map((r) => {
            const keep = {};
            for (const dir of Object.keys(r.profiles || {})) {
                const values = r.profiles[dir] && r.profiles[dir].values;
                if (values && values['station.hide'] === 'true') continue;
                keep[dir] = r.profiles[dir];
            }
            return keep;
        })),
```

在 `lib/station.js` 寫 detail 檔的迴圈裡（`:533-537`），`if (!s.detail) continue;` 之後加一行：

```js
                if (hidden.has(pkeyOf(Object.assign({ root: r.root }, s)))) continue;
```

那個 `hidden` 要在迴圈之前取得一次。同一個檔案裡把 `hiddenPkeys` 加進 `module.exports`。

在 `scripts/station.js` 的 `--json` 分支（`:677`）裡，`process.stdout.write` 之前加：

```js
            const hidden = station.hiddenPkeys(model);
            for (const r of model.registries) {
                r.sessions = r.sessions.filter((s) => !hidden.has(s.project ? r.root + '/' + s.project : r.root));
                const keep = {};
                for (const dir of Object.keys(r.profiles || {})) {
                    const values = r.profiles[dir] && r.profiles[dir].values;
                    if (!values || values['station.hide'] !== 'true') keep[dir] = r.profiles[dir];
                }
                r.profiles = keep;
            }
```

文字回覆那一半不能在 `scripts/station.js` 裡算，因為 `:743` 的 `const out = station.write({...})` 之後 `model` 不在那個 scope 裡 —— `write()` 是自己建 model 的。而 CONTRIBUTING 的 Scope 表要求 CLI 入口是核心邏輯的薄包裝，所以判斷留在核心，CLI 只負責印。

在 `lib/station.js` 的 `tally()`（`:479-483`）裡，把迴圈換成過濾過的版本：

```js
function tally(model) {
    const counts = { live: 0, stale: 0, down: 0 };
    const hidden = hiddenPkeys(model);
    for (const r of model.registries) {
        for (const s of r.sessions) {
            if (hidden.has(s.project ? r.root + '/' + s.project : r.root)) continue;
            counts[s.state]++;
        }
    }
    return counts;
}
```

在 `lib/station.js` 的 `write()` 回傳物件裡，加一個欄位：

```js
        hidden: hiddenPkeys(model).size,
```

在 `scripts/station.js`，把 `:768` 那個 `process.stdout.write` 的最後一段換成：

```js
    process.stdout.write('fankeel station — ' + out.file + '\n'
        + '  ' + out.registries + ' registries · ' + out.live + ' live, ' + out.stale + ' stale, ' + out.down + ' down'
        + (out.copy ? '  ·  copy at ' + out.copy : '') + '\n'
        + (out.hidden ? '  ' + out.hidden + ' project' + (out.hidden === 1 ? '' : 's') + ' hidden by station.hide\n' : ''));
```

名字不印，只印數量。頁面是客戶看的，終端是操作者自己的 —— 而設錯這個鍵在頁面上沒有任何痕跡，終端是唯一能讓它浮出來的地方。

**Steps:**

1. 在 `tests/station-hide.test.js` 寫第一個失敗測試：一個假 model，兩個 registry，其中一個專案的 profile 帶 `station.hide: 'true'`，斷言 `hiddenPkeys(model)` 回的集合含那個 pkey 且**用的是正斜線**。跑它，看它紅（函式還不存在）。
2. 寫 `hiddenPkeys()` 與 `pkeyOf()`，匯出前者。跑，看它綠。
3. 加第二個失敗測試：同一個 model，斷言 `serialize(model).sessions` 不含那個專案的任何 session，且 `serialize(model).roots.projects` 不含那個目錄鍵。跑，看它紅。
4. 改 `flatten()` 與 `projects:`。跑，看它綠。
5. 在 `tests/profile.test.js` 加一個失敗測試（鍵表的測試屬於那一個檔）：`station.hide` 設成非法值（例如 `'maybe'`）時 `profile.write` 回 `{ok: false}`，而設成 `'true'` 時回成功。跑，看它紅 —— 現在 `parseValue` 回 `unknown key`。
6. 加 `KEYS` 那一行。跑，看它綠。
7. 加第四個失敗測試：被藏的 session 不產生 detail 檔。跑、改、跑。
8. 加第五、六個失敗測試：`tally(model)` 不數被藏的 session，`write()` 的回傳帶 `hidden` 且等於被藏的 pkey 數。兩者都在 `lib/`，CONTRIBUTING 說那是純函式、直接測。跑、改、跑。
9. 加第七個測試，跑真的 CLI：`tests/brief.test.js:36-42` 已經用 `execFileSync` 這樣做，照它的形狀。跑 `node scripts/station.js --json --root <夾具 registry>`，parse stdout，斷言被藏專案的 session 不在任何 `registries[].sessions` 裡、也不在任何 `registries[].profiles` 的鍵裡。`--json` 繞過 `serialize()`，所以前面那些測試一個都覆蓋不到它。跑、改、跑。
10. **成品自身一致性，機械斷言。** 單元測試全綠而頁面上兩個數字差三倍的事在 2026-09-06 發生過，所以這一步不是可選的。不要把窗口改寫成「近 30 天」—— `windowTotals`、`lastDays` 與 `kpiHtml` 三個都已經在 `assets/station/station.js:846-847` 匯出，所以兩個數字都能用頁面自己的常數在 node 裡算出來。`NOW` 要是固定值，夾具的 `days[].day` 用 `V.localDay(NOW)` 之類建出來，否則測試會隨當天日期飄。

在 `tests/station-hide.test.js` 裡加：

```js
global.window = { STATION: {} };
const V = require('../assets/station/station.js');

const NOW = Date.parse('2026-09-15T12:00:00Z');
const DAYS = V.lastDays(NOW, 30);
const inside = {};
DAYS.forEach((d) => { inside[d] = true; });

function summed(sessions) {
    let usd = 0;
    sessions.forEach((s) => (s.days || []).forEach((r) => { if (inside[r.day]) usd += r.usd || 0; }));
    return usd;
}

// Two figures the artefact derives from one source: what the page's own
// arithmetic totals, and an independent sum over the same day window. They
// have to agree with a project shown and with it hidden, and the hidden run
// has to be smaller — equal-and-unchanged would pass while the filter never
// fired at all.
const shown = V.windowTotals(serialize(modelWithProjectShown).sessions, DAYS).usd;
assert.equal(shown, summed(serialize(modelWithProjectShown).sessions));
const hid = V.windowTotals(serialize(modelWithProjectHidden).sessions, DAYS).usd;
assert.equal(hid, summed(serialize(modelWithProjectHidden).sessions));
assert.ok(hid < shown, 'hiding a project with spend must lower the total');
assert.ok(V.kpiHtml(V.windowTotals(serialize(modelWithProjectHidden).sessions, DAYS),
    V.windowTotals([], DAYS)).includes(V.usd(hid)), 'the rendered cell carries that figure');
```

11. `git add tests/station-hide.test.js`，否則 `tests/source.test.js` 看不到它。跑 `node --test`，全綠。
12. 開一次真的服務頁面，看「30 天花費」那一格既不是空的也不是 `NaN`。純半全綠不保證渲染出來的東西對 —— 這是 `assets/station/station.js:6` 寫的那個分工的另一半，一行記進 commit message。
13. 從 `TODO.md` 的 `## Needs a decision` 移除第一條。跑 `node scripts/todo-check.js`，exit 0。
14. commit。

## Task 2: gate 留下 option label，clip 提到 240

**Files:**
- Modify: `lib/replay.js` — 檔頂加 `GATE_CLIP`，`qs` 條目加 `labels`
- Read: `lib/detail.js` — `:613` 是唯一的非測試呼叫者，確認回傳形狀沒有別的消費者
- Read: `lib/registry.js` — `:35` 的 `MAX_NEXT_LEN = 120`，step 5 的斷言讀它，不改
- Test: `tests/replay.test.js`

**Interfaces:**
- Consumes: 無
- Produces: gate 事件的 `qs[i]` 多一個 `labels: string[]`，順序與 `AskUserQuestion` 的 `options` 相同，所以 `labels[0]` 是選項一；`q` 與 `a` 的 clip 寬度變成 240

**Dispatch:** implementer, sonnet — 一個檔案、一個常數、一個欄位。

在 `lib/replay.js` 檔頂的 require 之後，加：

```js
// A gate's question and answer are clipped so one event cannot dominate the
// page. 120 held one sentence and cut the half of an option description that
// says what accepting it accepts, which is the half worth reading.
const GATE_CLIP = 240;
```

在 `lib/replay.js` 的 gate 事件組裝處（`:117-118`），把那兩行換成：

```js
            const labels = (Array.isArray(q.options) ? q.options : []).map((o) => o && o.label);
            return {
                q: clip(q.question, GATE_CLIP),
                a: a === null ? null : clip(a, GATE_CLIP),
                own: a !== null && !labels.includes(a),
                labels: labels.filter((l) => typeof l === 'string'),
            };
```

只存 `label`，不存 `description`。`description` 從來沒有被讀過，而且是最長的欄位。

**Steps:**

1. 在 `tests/replay.test.js` 加一個失敗測試：一段帶 `AskUserQuestion` 的假 transcript，其 `options` 有三個 label，斷言回來的 gate 事件 `qs[0].labels` 等於那三個字串且順序相同。跑，看它紅。
2. 加 `labels` 欄位。跑，看它綠。
3. 加第二個失敗測試：一個 240 字的問句原封不動、一個 241 字的問句被截在 240。跑，看它紅（現在截在 120）。
4. 加 `GATE_CLIP` 並換掉兩個字面量。跑，看它綠。
5. 加第三個測試守住不回歸：`lib/registry.js` 的 `MAX_NEXT_LEN` 仍是 120。跑，看它綠 —— 這一列是在證明改動沒有波及另外兩個同數字但無關的常數。
6. 跑 `node --test`，全綠。commit。

## Task 3: gate 彙總進 serialize，首頁加一格

**Files:**
- Modify: `lib/station.js` — `serialize()` 加 `gates` 彙總欄位
- Modify: `assets/station/station.js` — `kpiHtml()` 加一格 `roHtml`
- Modify: `TODO.md` — 移除 `## Needs a decision` 的「gate 只留問題與答案各 120 字」那條
- Read: `lib/replay.js` — `qs[i].labels` 與 `qs[i].a` 的形狀
- Read: `lib/detail.js` — `:646` 把 `events` 掛上 `extract()` 的回傳
- Test: `tests/station-gate.test.js`
- Test: `tests/station-view.test.js`

**Interfaces:**
- Consumes: Task 2 的 `qs[i].labels: string[]`
- Produces: `serialize(model).gates` 是 `{ swapped: [{ label: string, lost: number, total: number }] }`，`swapped` 依 `lost` 遞減排序

**Dispatch:** implementer, sonnet — 彙總規則與格子形狀都在下面。

彙總量的是「選項一輸掉幾次」：對每個 gate 的每個問題，若 `a` 不等於 `labels[0]` 就記 `labels[0]` 輸一次。選項一是批准本身，所以它常輸等於那句批准寫得不對。**不是** `own` —— `own` 為真代表答案不在任何選項裡，也就是使用者打了 Other，那是另一個量。

在 `lib/station.js`，`serialize()` 之前，加：

```js
// Which option-one wording most often loses. `labels` is in the order the
// question declared, so `labels[0]` is option one — the one whose description
// carries the approval. A wording that keeps losing is a wording to rewrite.
function gateSummary(model) {
    const lost = new Map();
    const total = new Map();
    for (const r of model.registries) {
        for (const s of r.sessions) {
            const events = s.detail && Array.isArray(s.detail.events) ? s.detail.events : [];
            for (const e of events) {
                if (e.kind !== 'gate' || !Array.isArray(e.qs)) continue;
                for (const q of e.qs) {
                    const first = Array.isArray(q.labels) ? q.labels[0] : null;
                    if (typeof first !== 'string' || q.a === null) continue;
                    total.set(first, (total.get(first) || 0) + 1);
                    if (q.a !== first) lost.set(first, (lost.get(first) || 0) + 1);
                }
            }
        }
    }
    const swapped = [...lost.keys()]
        .map((label) => ({ label, lost: lost.get(label), total: total.get(label) || 0 }))
        .sort((a, b) => b.lost - a.lost);
    return { swapped };
}
```

在 `lib/station.js` 的 `serialize()` 回傳物件裡，`profileKeys:` 那一行之後，加：

```js
        gates: gateSummary(model),
```

在 `assets/station/station.js` 的 `kpiHtml()` 裡，接在既有那幾個 `roHtml` 之後，加：

```js
    var top = S.gates && S.gates.swapped && S.gates.swapped.length ? S.gates.swapped[0] : null;
    out += roHtml('最常被換掉', top ? esc(top.label) : '—', top ? top.lost + ' / ' + top.total : '');
```

新格子放進既有那一列，不另開一列。它會讓讀數面板長高約 130px，而 `.hero-top` 的 `align-items: flex-end` 會把 hero 標題壓到底部；接受那個位移，`align-items` 不改。只做這一個數字 —— mockup 另外畫了「換成什麼」與「最常被選中」兩行排行，那超出這條 TODO 問的事，不要實作。

彙總的來源與 `windowTotals`（`assets/station/station.js:265`）同層：`homePage`（`:1028`）在 `:1037` 呼叫 `kpiHtml(...)`，新格子讀的是序列化好的 `S.gates`，不必接 `R`。

**Steps:**

1. 在 `tests/station-gate.test.js` 寫失敗測試：一個假 model，兩個 session 的 `detail.events` 各帶一個 gate 事件，其中一個選了 `labels[0]`、另一個選了 `labels[1]`，斷言 `serialize(model).gates.swapped` 是一列、`lost` 為 1、`total` 為 2。跑，看它紅。
2. 寫 `gateSummary()` 並掛上 `serialize()`。跑，看它綠。
3. 加第二個失敗測試：`q.a === null`（沒答的 gate）不進任何計數。跑、改、跑。
4. 加第三個失敗測試：`own` 為真（答案不在選項裡）時仍計入 `lost`，因為使用者確實沒選選項一。跑，看它綠或改到綠 —— 這一列是在把那個容易搞反的語意釘住。
5. `git add tests/station-gate.test.js`。跑 `node --test`，全綠。
6. 在 `tests/station-view.test.js` 加一個失敗測試：把 `global.window.STATION.gates` 設成一筆 `{ label: '進 build', lost: 3, total: 5 }`，斷言 `V.kpiHtml(...)` 的輸出含 `最常被換掉`、含那個 label、含 `3 / 5`。`kpiHtml` 在 `assets/station/station.js:367`、純半、`:847` 已匯出，所以這一格是可以斷言的而不是只能眼看。跑，看它紅。
7. 加 `kpiHtml` 那一格。跑，看它綠。
8. 手動開一次頁面，確認格子在既有那一列裡而不是自己一列。純半綠不保證版面對，這是 `assets/station/station.js:6` 那個分工的另一半。
9. 從 `TODO.md` 移除第二條。跑 `node scripts/todo-check.js`，exit 0。commit。

## Task 4: guard 同時看 agent_id 與 agent_type

**Files:**
- Modify: `hooks/guard.js` — `:45` 之前加 `agent_id` 存在性檢查，`:52` 的 reason 改口
- Modify: `docs/collisions.md` — `:181` 與 `:230` 從兩個條件改成三個
- Modify: `TODO.md` — 移除 `## Needs a decision` 的「`--agent` 起的主 session」那條
- Read: `hooks/carry.js` — `:51` 的 `if (payload.agent_id) return;`，同一個形狀
- Read: `docs/subagents.md` — `:430-440`，這一頁**不改**
- Test: `tests/guard.test.js`

**Interfaces:**
- Consumes: 無
- Produces: 無 —— 行為改動，沒有新的名字

**Dispatch:** implementer, sonnet — 一行插入、一句 reason、一個測試夾具欄位、一個新測試。

在 `hooks/guard.js`，第 44-45 行現在讀：

```js
    if (payload.tool_name === 'Bash' || payload.tool_name === 'PowerShell') {
        if (!readOnlyAgentType(payload.agent_type)) return;
```

在 `hooks/guard.js` 的那個 `if (!readOnlyAgentType(...))` **之前**插一行：

```js
        // `agent_type` is set inside a subagent AND on the main thread of a
        // session started with `--agent` — and that second one is a real
        // session that owns tasks and must be able to write. `agent_id` is
        // present only inside a subagent, so both are needed: the id says
        // whether this is a subagent at all, the type says whether it is a
        // read-only one. docs/subagents.md:430-440 quotes Claude Code's own
        // wording on the field to use.
        if (!payload.agent_id) return;
```

`hooks/guard.js:52` 的 reason 字串要把兩個條件都說出來，而不只說型別。這個 hook 每條路徑都 exit `0`，插入的 `return` 沿用同一個出口，不要改成 throw 或非零。

`docs/collisions.md:181` 現在引 `hooks/guard.js:45` 那一行；改成引新的兩行並說明為什麼是兩個欄位。`:230` 說「hook denies a command only when both are true」，改成三個。`docs/subagents.md:437` 不改 —— 它已經把 `agent_type` 判定叫做 trap，這個改動是讓程式碼追上那一頁。

**Steps:**

1. 在 `tests/guard.test.js` 加一個失敗測試：`tool_name` 為 `Bash`、`agent_type` 是唯讀型別、**沒有** `agent_id`、`tool_input.command` 是一個會寫檔的命令（例如 `echo x > f.txt`），斷言結果是 `''`（放行）。跑，看它紅 —— 現在 `:45` 會擋。
2. 加那一行 `if (!payload.agent_id) return;`。跑，看它綠。
3. 在 `bashCall()`（`:361`）加 `agent_id: 'agt_01'`。跑 `node --test`，看用它的八個測試（`:366` `:374` `:381` `:387` `:393` `:399` `:406` `:421`）仍綠 —— 不加這個欄位它們會全部變成在量「主 session 被放行」而不是「唯讀 agent 被擋」，八個一起綠而什麼都沒測到。
4. 改 `:52` 的 reason。跑，全綠。
5. 改 `docs/collisions.md` 的兩處。跑 `node scripts/docs-check.js`，exit 0。
6. 從 `TODO.md` 移除第三條。跑 `node scripts/todo-check.js`，exit 0。commit。

## Task 5: `/fankeel-station` 技能

**Files:**
- Modify: `skills/fankeel-station/SKILL.md` — 新檔
- Modify: `tests/inventory.test.js` — `SKILLS` 陣列（`:13`）加一筆
- Read: `skills/fankeel-explain/SKILL.md` — frontmatter 的六個欄位，照抄欄位名
- Read: `scripts/station.js` — `:51-59`，`serve` 與八個旗標的拼法

**Interfaces:**
- Consumes: 無
- Produces: 一個 slash command `/fankeel-station`

**Dispatch:** implementer, sonnet — 一個新檔加一筆陣列條目。

`skills/registry.json` **不動**。它由 `scripts/stage-registry.js` 產生（`skills/registry.json:2`），而 `lib/stage-registry.js:91-92` 只走 `lib/stages.js:447` 的七個階段名；`fankeel-station` 不是階段，`prompt_byte_budget` 不適用。

技能只做一件事：跑 `node <plugin>/scripts/station.js serve --open`，然後把它印的 URL 說出來。不要把 station 的路由表、狀態定義或欄位說明複製進來 —— 那些屬於 `docs/station.md`，而 CONTRIBUTING 的 Scope 表要求技能保持薄。

frontmatter 用 `skills/fankeel-explain/SKILL.md:1-8` 的六個欄位：`name`、`description`、`version`、`status`、`last_verified`、`source_of_truth`。

**Steps:**

1. 先讀 `skills/fankeel-explain/SKILL.md` 的前八行，照抄欄位名與格式。
2. 建 `skills/fankeel-station/SKILL.md`。跑 `node --test`，看 `tests/inventory.test.js` 的「skills/ holds exactly the known directories」紅 —— 那是它在做它的工作。
3. 在 `tests/inventory.test.js:13` 的 `SKILLS` 加 `'fankeel-station'`。跑，看它綠。
4. 跑 `node scripts/skills-check.js`，看它對新技能沒有 fail 級的話要說 —— 特別是 `unknown-flag`（`lib/skills.js:103`）。
5. `git add skills/fankeel-station/SKILL.md`。跑 `node --test`，全綠。commit。

## Task 6: 頁面自己偵測 serve 死了

**Files:**
- Modify: `assets/station/station.js` — 加輪詢與凍結橫條
- Modify: `TODO.md` — 移除 `## Needs a decision` 的「只能用 `--open` 或 `serve --open` 開」那條
- Read: `scripts/station.js` — `:425` 的 `/station/health` 回 `{ station: true, pid, started }`
- Read: `assets/station/index.html` — `:15` 的 `<span id="gen">`
- Read: `.fankeel/build/2026-09-14-todo-four/mockup.html` — 螢幕 3 的降級樣式
- Test: `tests/station-view.test.js`

**Interfaces:**
- Consumes: Task 3 已經改過 `assets/station/station.js`，本任務接在它之後
- Produces: 無

**Dispatch:** implementer, sonnet — 輪詢與降級樣式 mockup 的螢幕 3 已經畫出來了。

輪詢打 `station/health`（相對路徑，和 `station/station.js` 同層）。失敗時：masthead 下面一條整頁寬的 stale 色橫條，內容降 `opacity .72` 加 `saturate(.3)` 但仍可讀可點，不清掉任何東西。凍結時刻用頁面自己的 `gen`（`assets/station/index.html:15` 的 `<span id="gen">`），**不用** health 回的 `started` —— `started` 是伺服器開始跑的時間，不是資料寫成的時間。

這一條只在 `serve` 下有意義。`--open` 直接開檔（`scripts/station.js:770`）時沒有伺服器，輪詢必須自己關掉而不是永遠顯示死亡：判斷方式是 `location.protocol === 'file:'` 就不啟動輪詢。

死活判斷要是一個純函式，放在 `assets/station/station.js:833` 那個 `module.exports` 守衛**之上**並匯出。`:6` 的註解把這件事寫成規則：守衛之上是純函式且有單元測試，之下碰 document、靠服務出來的頁面檢查。輪詢與橫條的插入屬於之下那半，判斷屬於之上那半。

在 `assets/station/station.js` 的守衛之上，加：

```js
    // Whether the served page has lost its server, and what to say. Pure, so
    // it is unit tested; the fetch that feeds it and the banner it fills are
    // the document half below the guard.
    function serveLost(lastOkMs, nowMs, genText) {
        if (lastOkMs === null || lastOkMs === undefined) return null;
        if (nowMs - lastOkMs < 15000) return null;
        return '伺服器已離線 — 畫面上的數字凍結於 ' + genText;
    }
```

**Steps:**

1. 讀 mockup 的螢幕 3，取它的橫條標記與 CSS。
2. 在 `tests/station-view.test.js` 加一個失敗測試：`serveLost(null, ...)` 回 `null`、剛回應過回 `null`、超過門檻回含 `凍結於` 與那個 `gen` 字串的一行。跑，看它紅。
3. 加 `serveLost()` 並匯出。跑，看它綠。
4. 加輪詢與橫條（守衛之下）。`location.protocol === 'file:'` 時不啟動輪詢 —— `--open` 直接開檔時沒有伺服器，永遠顯示死亡是錯的。
5. 手動驗證三種情況：`serve` 下正常（無橫條）、`serve` 起來後殺掉 node（橫條出現、內容仍可讀可點）、`--open` 直接開檔（無橫條、不報死）。三次的結果寫進 commit message。
6. 跑 `node --test`，全綠。
7. 從 `TODO.md` 移除第四條。跑 `node scripts/todo-check.js`，exit 0。commit。

## Task 7: 文件

**Files:**
- Modify: `docs/station.md` — `:90`、`:312`、`:411`、`:510` 四節
- Modify: `docs/README.md` — 設計檔與本計畫各一列索引
- Read: `assets/station/station.js` — Task 3 的那一格與 Task 6 的輪詢，記錄前要讀實作
- Read: `docs/plans/2026-09-14-todo-four-design.md` — 四節規格
- Read: `.fankeel/map.md` — 歸檔與索引慣例

**Interfaces:**
- Consumes: Task 1 的 `station.hide` 與 `hiddenPkeys()`、Task 3 的 `gates` 欄位、Task 5 的 `/fankeel-station`、Task 6 的輪詢
- Produces: 無

**Dispatch:** implementer, sonnet — 四節與兩列索引，內容都在規格裡。

`docs/station.md` 的四節各要寫什麼：

- `## What each row holds`（`:90`）—— 被藏的專案不產生列，這一節要說清楚頁面上沒有任何痕跡。
- `## Filtering, and the two views`（`:312`）—— `station.hide` 的過濾與既有的 facet 過濾是兩件不同的事：前者在 `lib/station.js` 的 `flatten()`，資料根本沒進頁面；後者在頁面端。
- `## When it is written, and where`（`:411`）—— 被藏的 session 不產生 `station/detail/<id>.js`。
- `## Setting a profile from the page`（`:510`）—— 兩個代價：藏起來之後 `POST /profile` 會 404（`scripts/station.js:545` 要求專案已被跑著的 model 認識），解除只能走 `node scripts/task.js profile set station.hide false`；以及 `assets/station/station.css:15` 的 `--p-0` 到 `--p-5` 是按順序指派的，藏掉一個會讓其餘專案換色，這是接受的代價而不是缺陷。

還要加 `/fankeel-station` 與首頁那一格的說明。`docs/README.md` 兩列索引照既有 plan 列的格式（例如 `:52`、`:121`）。

**Steps:**

1. 改 `docs/station.md` 四節。
2. 加 `docs/README.md` 兩列。
3. 跑 `node scripts/docs-check.js`，exit 0。
4. 跑 `node scripts/docs-audit.js`，看 index 兩個方向都乾淨。
5. 跑 `node --test`，全綠 —— 這是整個計畫最後一次全套。
6. commit。

## Coverage

| promise | task |
|---|---|
| `station.hide` 加進 `lib/profile.js:17` 的 `KEYS`，值照 `land.push`（`:19`）的布林 | Task 1 |
| `lib/station.js:375-380` 的 `flatten` 過濾：`station.hide` 為 `'true'` 的 pkey 之下 | Task 1 |
| 判斷「這個 pkey 被藏了嗎」寫成 `lib/station.js` 匯出的一個函式，所有過濾點都呼叫 | Task 1 |
| `lib/station.js:412` 的 `projects: Object.assign({}, ...model.registries.map((r) => r.profiles \|\| {}))` | Task 1 |
| `lib/station.js:533-537` 的 detail 檔迴圈跳過同一批 session。它直接走 | Task 1 |
| `scripts/station.js:677` 的 `--json` 自己過濾一次。它寫的是 `station.gather()` | Task 1 |
| `scripts/station.js:768` 的文字回覆與它的來源 `lib/station.js:479-483` 的 `tally()` | Task 1 |
| 頁面上不加任何指示。`assets/station/station.js` 為這一條一行不改。 | Task 1 |
| 代價寫下來：藏起來之後 `POST /profile`（`scripts/station.js:545`）會 404，因為 | Task 7 |
| 第二個代價：`assets/station/station.css:15` 的 `--p-0` 到 `--p-5` 是按順序指派給 | Task 7 |
| `lib/replay.js:118` 的 `qs` 條目多一個 `labels` 欄位，存每個選項的 `label`。 | Task 2 |
| `lib/replay.js:118` 的兩個 `clip(..., 120)` 改成一個具名常數 `GATE_CLIP = 240`， | Task 2 |
| `lib/station.js` 的 `serialize()`（398-448）多一個跨 session 的 gate 彙總欄位。 | Task 3 |
| `assets/station/station.js:367` 的 `kpiHtml` 多一個 `roHtml`（`:364`， | Task 3 |
| 彙總量的是 `a !== labels[0]`，按 `labels[0]` 分組：哪一句「選項一」最常輸掉。 | Task 3 |
| 彙總在 `serialize()` 算完，所以新格子讀的是序列化好的欄位而**不是** `R`。 | Task 3 |
| 新格子放進既有那一列，不另開一列。它會讓讀數面板長高約 130px，而 `.hero-top` 的 | Task 3 |
| 只做「被換掉最多的 label」一個數字。mockup 另外加了「換成什麼」與「最常被選中」 | Task 3 |
| `hooks/guard.js:45` 之前加 `if (!payload.agent_id) return;`。沒有 `agent_id` 就是 | Task 4 |
| `hooks/guard.js:52` 的 reason 字串跟著改，把兩個條件都說出來。 | Task 4 |
| `tests/guard.test.js:361` 的 `bashCall()` 加 `agent_id`。它現在只設 `agent_type`， | Task 4 |
| 新增一個測試：`agent_type` 是唯讀型別、**沒有** `agent_id`、指令會寫檔，斷言放行。 | Task 4 |
| `docs/collisions.md:181` 與 `:230` 要改。它們是 current 的 reference 頁，`:181` | Task 4 |
| `docs/subagents.md:437` 不改。它已經把 `agent_type` 判定叫做 trap，`:430-435` 還引 | Task 4 |
| `skills/fankeel-station/SKILL.md`，frontmatter 用 `skills/fankeel-explain/SKILL.md:1-8` | Task 5 |
| `tests/inventory.test.js:13` 手寫的 `SKILLS` 陣列加一筆。不加就是 | Task 5 |
| 技能內容只做一件事：`node <plugin>/scripts/station.js serve --open`。 | Task 5 |
| `skills/registry.json` 不動。它由 `scripts/stage-registry.js` 產生 | Task 5 |
| `assets/station/station.js` 加一個輕量輪詢打 `station/health` | Task 6 |
| 失敗時的畫面：說數字凍結於何時，不清掉內容。時間戳用頁面自己的 `gen` | Task 6 |
| 這一條只在 `serve` 下有意義。`--open` 直接開檔（`scripts/station.js:770`）時沒有 | Task 6 |
| `tests/guard.test.js` 新測試：`agent_type` 唯讀、無 `agent_id`、指令寫檔 | Task 4 |
| `tests/profile.test.js` 新測試：`station.hide` 設 `'true'` 後 `flatten()` 不回那個 pkey 的 session | Task 1 |
| `tests/replay.test.js` 新測試：gate 事件帶 `labels`，`q` 在 121 到 240 字之間不被截 | Task 2 |
| `tests/inventory.test.js` 既有的「skills/ holds exactly the known directories」 | Task 5 |
| 整個 `node --test` 套件 | Task 7 |
| **頁面自身一致性**：算出頁面上「30 天花費」那一格的數字，再把 `station-data.js` | Task 1 |
