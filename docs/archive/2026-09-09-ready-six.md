---
status: current
---

# TODO.md `## Ready` 六條 Implementation Plan

**Goal:** 清掉 `TODO.md` 的 `## Ready` 六條，外加盤點時發現的一個 `docs-check` 差一行。

**Architecture:** 七個互不相干的小改動。四個是「一個檔加一個測試」，可以各自派人；三個是搬檔、改句子與跑量測，留在會期內做。共用的檔只有 `docs/README.md`，Task 5 與 Task 6 都改它，因此那兩個會被排成前後。

**Tech Stack:** Node.js，`node --test`，零執行期相依。`node:util` 的 `parseArgs`、`node:vm`、`node:child_process`。

**Spec:** [2026-09-09-ready-six-design.md](2026-09-09-ready-six-design.md)

## Global Constraints

從 `CONTRIBUTING.md`、`package.json`、`.fankeel/map.md` 與測試套件取得，值照抄：

- 測試指令是 `npm test`，展開為 `node --test`。`package.json` 沒有 `dependencies` 也沒有 `devDependencies`，**不得新增任何相依**。
- **新的測試檔必須先 `git add` 才會被 `tests/source.test.js` 看見。** 未 staged 的新檔案，`source.test.js` 讀 `git ls-files` 時掃不到。
- 測試要暫存目錄一律經由 `tests/tmp.js` 的 `tmp(prefix)`，不得自行 `mkdtempSync`。該檔的 exit handler 是唯一會清掉它們的東西 — 2026-09-07 曾累積 957,746 個未清目錄。
- `lib/*.js` 是純函式，**不得反向引用 `scripts/` 或 `hooks/`**；只能由 `scripts/` 與 `hooks/` 引用 `lib/`。
- `scripts/*.js` 是 `lib/` 的薄包裝。station CLI 新增 flag 需在 `docs/station.md` 補一列否則 `tests/station-doc.test.js` 紅 — 本計畫不動 station CLI 的 flag，`map.js` 沒有這層耦合（唯一的 doc-test 是 `tests/station-doc.test.js`）。
- 文件歸檔照 `.fankeel/map.md`：`docs` 是 reference、`docs/plans` 是 plan、`docs/decisions` 是 decision、`docs/reports` 是 report、`docs/archive` 是 archive。**新增或改名的頁面要在同一個 change 補上 `docs/README.md` 的索引列**，那份索引是手動維護的。
- `docs/archive/`、`docs/plans/`、`docs/reports/` 是有日期的紀錄，**不得事後編輯內容**；Task 5 只搬檔不改字。
- 基準是 `b0e68a6`，`ℹ pass 1223 / fail 0`，存於 `.fankeel/build/ready-six/baseline-test.txt`。收尾時通過數不得低於 1223。
- `node scripts/docs-check.js` 今天 exit 1，10 條 `past-end`。
- 版號一律跑 `scripts/version.js`，不手改 — 本計畫不動版號。

## File structure

| 檔案 | 負責什麼 |
|---|---|
| `assets/station/index.html` | 靜態站台外殼。第 30 行決定 data script 的 `src` |
| `tests/station.test.js` | station 的既有測試，已 `require('node:vm')` |
| `scripts/map.js` | map CLI。`parseArgs` 與 `main`，`main` 回傳要印的字串 |
| `tests/map-cli.test.js` | map CLI 的既有測試，有 `root()` 與 `run(dir)` |
| `skills/fankeel-survey/SKILL.md` | survey 步驟 2 是 `map.js` 的說明所在 |
| `scripts/docs-check.js` | 文件檢查。`linesOf`/`lineCount` 在 :136–:148，匯出 `{ scan, report, parseArgs, ... }` |
| `tests/docs-check.test.js` | 既有測試都在測 `report` 的排版；本計畫加第一個 `scan` 的 `past-end` 案例 |
| `docs/pipeline.md` | 七個 `### <stage>` 各帶 mermaid 圖 |
| `tests/pipeline-doc.test.js` | 新檔。守著圖與 `lib/stages.js` 規則的對應 |
| `docs/README.md` | 手動維護的文件索引 |
| `docs/improvement-brief.md` | 改進簡報，`undeclared` |
| `docs/reports/2026-09-09-haiku-pair.md` | 新報告 |
| `docs/sources.md` | 量測帳本，新增一列 |

## Task 1: `cleared` 在靜態外殼也只收數字

**Files:**
- Modify: `assets/station/index.html` — 第 30 行改成先驗證再串接
- Test: `tests/station.test.js`
- Read: `scripts/station.js` — `:347`–`:360` 是伺服器端已有的同一條規則，不改

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 計畫已帶著程式碼；抄寫加測試。

步驟：

1. 先寫失敗的測試。在 `tests/station.test.js` 檔尾加上：

```js
// The static shell concatenates the page's query into the data script's src.
// A crafted query could close the src attribute and inject markup, so only a
// digits-only `cleared` is passed through — the same rule scripts/station.js
// already applies at :355, now applied at both ends.
const INDEX = path.join(__dirname, '..', 'assets', 'station', 'index.html');

function writtenSrc(search) {
    const html = fs.readFileSync(INDEX, 'utf8');
    const open = html.indexOf('<script>');
    const inline = html.slice(open + 8, html.indexOf('</script>', open));
    assert.ok(inline.includes('station-data.js'), 'the first inline script no longer writes the data tag');
    let out = null;
    vm.runInNewContext(inline, {
        location: { search },
        document: { write: (s) => { out = s; } },
        URLSearchParams,
    });
    return out;
}

test('the data script src takes a digits-only cleared and nothing else', () => {
    assert.match(writtenSrc('?cleared=3'), /station-data\.js\?cleared=3"/);
    assert.match(writtenSrc(''), /station-data\.js"/);

    const crafted = writtenSrc('?cleared=1"></' + 'script><script>alert(1)</' + 'script>');
    assert.ok(!crafted.includes('alert(1)'), 'a crafted query reached the src: ' + crafted);
    assert.match(crafted, /station-data\.js"/);
});
```

2. `npm test` 看它紅。今天 `crafted` 那三行會失敗，因為整個 `location.search` 被原樣串上去。

3. 在 `assets/station/index.html`，把第 30 行整行換成：

```html
<script>(function(){var v=new URLSearchParams(location.search).get('cleared');var q=v!==null&&/^\d+$/.test(v)?'?cleared='+v:'';document.write('<script src="station/station-data.js'+q+'"><\/script>')})()</script>
```

`document.write` 保留：動態插入的 script 預設 `async`，而下一行的 `station.js` 是 parser-inserted，換成 `createElement` 會失去「資料先於 `station.js`」的順序保證。`<\/script>` 的反斜線必須留著，否則 HTML 解析器會在字串中間結束這個 script。

4. `npm test` 看它綠。

5. Commit。

## Task 2: `map.js --print`

**Files:**
- Modify: `scripts/map.js` — `parseArgs` 加 `print`，`main` 提前回傳
- Modify: `skills/fankeel-survey/SKILL.md` — 步驟 2 的指令列加上 flag 與一句說明
- Test: `tests/map-cli.test.js`
- Read: `lib/map.js` — `buildMap(root)` 在 :277，是純函式，不改

**Interfaces:**
- Consumes: none
- Produces: `scripts/map.js` 的 `--print` flag

**Dispatch:** implementer, sonnet — 計畫已帶著程式碼；抄寫加測試。

步驟：

1. 先寫失敗的測試。在 `tests/map-cli.test.js` 檔尾加上：

```js
// A reviewer told to touch nothing still needs to read the map. Without a
// print-only mode the only way to read it is to write it: a build reviewer
// wrote .fankeel/map.md twice on 2026-09-08 doing exactly that.
test('--print writes nothing and puts the map on stdout', () => {
  const dir = root();
  fs.writeFileSync(path.join(dir, 'CLAUDE.md'), '| a | b |\n|---|---|\n| 1 | 2 |\n');
  const out = execFileSync(process.execPath, [SCRIPT, '--root', dir, '--print'], { encoding: 'utf8' });
  assert.match(out, /status: generated/);
  assert.ok(!fs.existsSync(path.join(dir, '.fankeel', 'map.md')), 'a map was written');
  assert.ok(!fs.existsSync(path.join(dir, '.fankeel', '.gitignore')), 'a .gitignore was written');
});

test('without --print the map is still written', () => {
  const dir = root();
  fs.writeFileSync(path.join(dir, 'CLAUDE.md'), '| a | b |\n|---|---|\n| 1 | 2 |\n');
  run(dir);
  assert.ok(fs.existsSync(path.join(dir, '.fankeel', 'map.md')), 'no map written');
});
```

2. `npm test` 看第一個紅：今天 `--print` 是未宣告的 flag，`strict: false` 讓它靜默通過，於是照常寫檔。

3. 在 `scripts/map.js`，把 `parseArgs` 整個函式換成：

```js
function parseArgs(argv) {
    const { values } = parseArgv({ args: argv, strict: false, allowPositionals: true, options: { root: { type: 'string' }, print: { type: 'boolean' } } });
    return {
        root: registry.resolveRoot(typeof values.root === 'string' ? values.root : undefined),
        print: values.print === true,
    };
}
```

4. 在 `scripts/map.js`，把 `main` 的頭兩行——`const { root } = parseArgs(argv);` 與其下的 `const stateDir = ...`——換成：

```js
function main(argv) {
    const { root, print } = parseArgs(argv);

    // Read without writing. A reviewer told to change nothing can still ask
    // what the project's own documents say about itself.
    if (print) return buildMap(root);

    const stateDir = path.join(root, '.fankeel');
```

其餘不動：`main` 原本就回傳字串，由 `require.main` 那段印出，所以 `--print` 走的是同一個出口。

5. 在 `skills/fankeel-survey/SKILL.md` 的 `### 2. Read the map`，把那個指令區塊換成：

```markdown
```
node <plugin>/scripts/map.js [--root <dir>]
node <plugin>/scripts/map.js --print          # read it without writing it
```
```

並在其下 `It writes .fankeel/map.md and prints a summary.` 那句之後補一句：`--print` puts the map itself on stdout and writes nothing — which is the one a reviewer told to touch nothing can run.

6. `npm test` 看它綠。

7. Commit。

## Task 3: `linesOf` 不把檔尾換行算成一行

**Files:**
- Modify: `scripts/docs-check.js` — `linesOf` 在 :136
- Test: `tests/docs-check.test.js`

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 計畫已帶著程式碼；抄寫加測試。

步驟：

1. 先寫失敗的測試。在 `tests/docs-check.test.js` 檔尾加上：

```js
// `linesOf` split on newline, so a file ending in one counted a phantom last
// line: a 490-line file reported "ends at 491", and a reference to line 491
// was allowed. Scanned across the whole repository with the fix applied, no
// existing reference changed verdict — only the number in the message — so
// this is pinned on a fixture rather than on a document.
test('a reference one line past the end is past-end', () => {
  const dir = tmp('fankeel-pastend-');
  fs.mkdirSync(path.join(dir, '.fankeel'), { recursive: true });
  fs.mkdirSync(path.join(dir, 'docs'), { recursive: true });
  fs.mkdirSync(path.join(dir, 'lib'), { recursive: true });
  fs.writeFileSync(path.join(dir, '.fankeel', 'docs.json'),
    JSON.stringify({ buckets: [{ path: 'docs', role: 'reference' }] }));
  fs.writeFileSync(path.join(dir, 'lib', 'thing.js'), 'a\nb\nc\n');
  fs.writeFileSync(path.join(dir, 'docs', 'page.md'), 'see `lib/thing.js:4`\n');

  const out = scan(dir);
  const past = out.findings.filter((f) => f.tag === 'past-end');
  assert.equal(past.length, 1, 'line 4 of a three-line file was not flagged');
  assert.match(past[0].what, /ends at 3$/);
});
```

2. `npm test` 看它紅：今天那個三行的 fixture 被算成四行，所以第四行的引用放行，`past.length` 是 0。

3. 在 `scripts/docs-check.js`，把 `linesOf` 的快取那行換成：

```js
function linesOf(root, rel) {
    const key = root + '\0' + rel;
    if (!LINES.has(key)) {
        const text = readFile(root, rel);
        // A file ending in a newline splits into a trailing empty element that
        // is not a line. Counting it let a reference to the line after the last
        // one pass, and made every "ends at N" one too many.
        const l = text === null ? null : text.split('\n');
        if (l !== null && l.length && l[l.length - 1] === '') l.pop();
        LINES.set(key, l);
    }
    return LINES.get(key);
}
```

4. `npm test` 看它綠。

5. Commit。

## Task 4: 七個 stage 的圖各引一句自己的規則

**Files:**
- Modify: `docs/pipeline.md` — `verify` 的 `D2`（:570）與 `audit` 的 `A`（:607）
- Test: `tests/pipeline-doc.test.js`
- Read: `lib/stages.js` — `STAGES` 在 :192，每個元素是 `{ name, rules }`，不改

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 計畫已帶著程式碼；抄寫加測試。

盤點時逐句比對過七個 stage，兩格是紅的：`verify` 的圖漏掉規則裡的 `the chain is one workflow`，`audit` 的圖把規則每一條都改寫過、沒有一句原文。錨句每個 stage 一句，都是該 stage 規則的連續子字串。

步驟：

1. 先寫失敗的測試。新建 `tests/pipeline-doc.test.js`：

```js
'use strict';

// docs/pipeline.md draws each stage; lib/stages.js holds the rules it draws.
// Nothing kept the two together — the verify node had lost "the chain is one
// workflow" and the audit node had paraphrased every line of its own rule, and
// both read as current. One anchor phrase per stage is what is pinned, not the
// whole wording: a diagram label is meant to be shorter than the rule.

const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const { byName } = require('../lib/stages.js');

const PIPELINE = path.join(__dirname, '..', 'docs', 'pipeline.md');

const ANCHOR = {
  survey: 'Nothing matched is a finding',
  design: 'the test that fails now and passes after',
  plan: 'the smallest unit carrying its own test cycle',
  build: 'do not improve adjacent code',
  verify: 'the chain is one workflow',
  audit: 'dead references, never opinions',
  land: 'Option one stands the task down',
};

// A `<br/>` inside a mermaid label breaks a phrase across two lines, and the
// labels carry HTML entities where the rules carry the characters themselves.
const flat = (s) => s
  .replace(/<br\s*\/?>/g, ' ')
  .replace(/&quot;/g, '"')
  .replace(/&#39;/g, "'")
  .replace(/&lt;/g, '<')
  .replace(/&gt;/g, '>')
  .replace(/&amp;/g, '&')
  .replace(/\s+/g, ' ')
  .toLowerCase();

function section(text, stage) {
  const start = text.indexOf('\n### ' + stage + '\n');
  assert.notEqual(start, -1, 'no `### ' + stage + '` heading in docs/pipeline.md');
  const after = text.indexOf('\n### ', start + 1);
  return text.slice(start, after === -1 ? text.length : after);
}

test('every anchor is a contiguous substring of its own stage rule', () => {
  for (const stage of Object.keys(ANCHOR)) {
    const rules = flat(byName(stage).rules.join('\n'));
    assert.ok(rules.includes(flat(ANCHOR[stage])),
      stage + ': the anchor is not in that stage\'s rules in lib/stages.js');
  }
});

test('every stage diagram quotes one line of its own rule', () => {
  const doc = fs.readFileSync(PIPELINE, 'utf8');
  for (const stage of Object.keys(ANCHOR)) {
    assert.ok(flat(section(doc, stage)).includes(flat(ANCHOR[stage])),
      stage + ': docs/pipeline.md does not carry "' + ANCHOR[stage] + '"');
  }
});
```

2. `git add tests/pipeline-doc.test.js` — 未 staged 的新檔案，來源清單那支測試看不見（見 `## Global Constraints`）。

3. `npm test` 看它紅：第二個測試的 `verify` 與 `audit` 兩格會失敗，其餘五格通過。

4. 在 `docs/pipeline.md` 第 570 行，把 `D2` 那一行的結尾 `<i>paths never a paste — only the rows it defeats</i>"]` 換成：

```markdown
    D2["<b>the adversary</b><br/>read-only, over the evidence table<br/><i>was it run · on what · could it have<br/>failed · out of what</i><br/><i>paths never a paste — only the rows it defeats</i><br/><i>where the host opens it, the chain is one workflow</i>"]
```

5. 在 `docs/pipeline.md` 第 607 行，把 `audit` 的 `A` 節點換成：

```markdown
    A["docs-check<br/><i>dead references, never opinions</i>"]
```

原本寫的是 `every reference still resolves`，那是規則前半的改寫，掉的是「never opinions」這後半 — 而那半才是這個掃描器與人工判斷的分界。

6. `npm test` 看它綠。

7. Commit。

## Task 5: 四份已落地的 plan 封存

**Files:**
- Modify: `docs/README.md` — 第 51、52、78、79 列的連結改指 `archive/`
- Modify: `docs/plans/2026-09-07-ready-fourteen-design.md` — `git mv` 到 `docs/archive/`
- Modify: `docs/plans/2026-09-07-ready-fourteen.md` — `git mv` 到 `docs/archive/`
- Modify: `docs/plans/2026-09-08-station-shell-design.md` — `git mv` 到 `docs/archive/`
- Modify: `docs/plans/2026-09-08-station-shell.md` — `git mv` 到 `docs/archive/`
- Read: `.fankeel/docs.json` — `docs/archive` 的 role 是 `archive`，不改

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** in-session — 四個 `git mv` 與四列索引改寫，工具呼叫本身就是全部的工作。

步驟：

1. 記下今天的紅：

```sh
node scripts/docs-check.js; echo "exit $?"
```

十條 `past-end`，exit 1。

2. 搬四份：

```sh
git mv docs/plans/2026-09-07-ready-fourteen-design.md docs/archive/
git mv docs/plans/2026-09-07-ready-fourteen.md docs/archive/
git mv docs/plans/2026-09-08-station-shell-design.md docs/archive/
git mv docs/plans/2026-09-08-station-shell.md docs/archive/
```

3. 在 `docs/README.md`，把第 51、52、78、79 列連結的目錄段從 plans 改成 archive。每一列的連結文字與連結目標寫的是同一個相對路徑，兩處都要改。四列的敘述文字與結尾的 built 標記都不動。四列分別是 `2026-09-08-station-shell-design.md`、`2026-09-08-station-shell.md`、`2026-09-07-ready-fourteen-design.md`、`2026-09-07-ready-fourteen.md`。

4. 確認轉綠，並確認索引沒有殘留死連結：

```sh
node scripts/docs-check.js; echo "exit $?"
node scripts/todo-check.js; echo "exit $?"
```

`docs-check` 要是 exit 0。`scripts/docs-check.js:196` 對 `archive` 與 `report` 直接 return，所以那十條一次全清。

5. Commit。

## Task 6: 兩頁補上 Scope

**Files:**
- Modify: `docs/improvement-brief.md` — 第 171 行的項目補一句
- Modify: `docs/README.md` — 第 65 列補一句
- Read: `docs/sources.md` — `:30` 起的帳本是這些數字的出處，不改

**Interfaces:**
- Consumes: `docs/README.md` 已由 Task 5 改過第 51、52、78、79 列
- Produces: none

**Dispatch:** in-session — 兩處各一行。

步驟：

1. 在 `docs/improvement-brief.md` 第 171 行，把 `（9.2×、1.5×、2.55×、1.85×）` 改成 `（9.2×、1.5×、2.55×、1.85×——9.2× 是檔名未知的那一組，1.5× 是同題但檔名已命名的那一組，2.55× 是命名了但保留跨檔 join 的中間點）`，句子其餘不動。

2. 在 `docs/README.md` 第 65 列，把敘述 `Why the middle between those two is 2.55× and the rule is a gradient rather than a step, with a pre-registered prediction that failed` 改成 `Why the middle between those two is 2.55× — eight files named but the cross-file join kept — and the rule is a gradient rather than a step, with a pre-registered prediction that failed`。

3. 確認索引與檢查都還是綠的：

```sh
node scripts/docs-check.js; echo "exit $?"
```

4. Commit。

## Task 7: `haiku` 兩臂

**Files:**
- Modify: `docs/reports/2026-09-09-haiku-pair.md` — 新檔，這一組兩臂的報告
- Modify: `docs/reports/evidence/2026-09-09-haiku-pair/ab-haiku.sh` — 新檔，由 `ab.sh` 複製後只改模型與輸出路徑
- Modify: `docs/reports/evidence/2026-09-09-haiku-pair/ab-haiku-provenance.txt` — 新檔，HEAD、porcelain 與 CLI 版本
- Modify: `docs/reports/evidence/2026-09-09-haiku-pair/arm-haiku-dispatch.json` — 新檔，dispatch 那一臂的原始輸出
- Modify: `docs/reports/evidence/2026-09-09-haiku-pair/arm-haiku-inline.json` — 新檔，inline 那一臂的原始輸出
- Modify: `docs/sources.md` — 帳本加一列
- Modify: `docs/README.md` — 補這份報告的索引列
- Modify: `TODO.md` — 這一條的 bullet 在同一個 change 移除
- Read: `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab.sh` — 兩臂腳本的形狀，不改
- Read: `docs/reports/evidence/2026-09-03-dispatch-vs-inline/extract.js` — 從 arm JSON 取數字的既有做法，不改

**Interfaces:**
- Consumes: none
- Produces: `docs/reports/2026-09-09-haiku-pair.md`，`docs/reports/evidence/2026-09-09-haiku-pair/`

**Dispatch:** in-session — 這是量測不是實作。兩臂要在同一個 HEAD、同一個 CLI 版本下跑，provenance 與判讀必須在拿得到基準的地方做。

步驟：

1. 讀 `ab.sh`，複製成 `docs/reports/evidence/2026-09-09-haiku-pair/ab-haiku.sh`，**只改三處**：兩臂的 `--model opus` 改成 `--model haiku`、輸出路徑改成本目錄、provenance 檔名改成 `ab-haiku-provenance.txt`。問題文字（`$QUESTION`）與兩個尾綴（`$M_DISPATCH`、`$M_INLINE`）一字不改 — 這是「同題」的意思，也是唯一被改動的變因為模型的保證。

2. 跑之前先釘 provenance：`git rev-parse HEAD`、`git status --porcelain`、`claude --version`，寫進 `ab-haiku-provenance.txt`。工作樹必須是乾淨的。

3. 跑兩臂。

4. 用 `extract.js` 取數字，寫 `docs/reports/2026-09-09-haiku-pair.md`。報告必須寫明：這不是 pair 1 的嚴格重現，pair 1 釘的是 HEAD `86a104e1bc87f7e82eec45cb84b6e32459a32402` 與 `claude 2.1.259`，今天兩者都不同；因此可比的是同日兩臂之間的比值，不是跨日的絕對值。

5. 在 `docs/sources.md` 的帳本加一列，`Scope` 欄寫明上一段那個限制。

6. 在 `docs/README.md` 補這份報告的索引列。

7. 在 `TODO.md` 移除這一條 bullet。

8. Commit。

## Coverage

| promise | task |
|---|---|
| `assets/station/index.html:30` 只把通過 `/^\d+$/` 的 `cleared` 串進 data script 的 `src`，其餘一律丟掉 | Task 1 |
| 保留 `document.write`：動態插入的 script 預設 `async`，而 `station.js` 是 parser-inserted | Task 1 |
| serve 模式不受影響：`scripts/station.js:453` 轉址到 `/?cleared=N`，`:354` 仍讀得到 | Task 1 |
| `parseArgs` 多宣告一個 boolean flag `print` | Task 2 |
| `--print` 時 `main()` 直接回傳 `buildMap(root)`，不 `mkdirSync`、不 `ensureIgnored`、不 `writeFileSync` | Task 2 |
| 沒有 `--print` 時的行為一字不動，包含它印的那段摘要 | Task 2 |
| `linesOf`（`scripts/docs-check.js:136`）在最後一個元素是空字串時去掉它；今天 `text.split('\n')` 讓以換行結尾的檔案多算一行。 | Task 3 |
| 這是訊息修正而不是新判定：以修好的複本掃過全庫，唯一差異是 `ends at 491` 變 `ends at 490` | Task 3 |
| 因此新測必須用 fixture 造一個指向 N+1 行的引用，不能靠現有文件 | Task 3 |
| `docs/pipeline.md:570` 的 `D2` 補回 `lib/stages.js:301` 有而它沒有的措辭 | Task 4 |
| 新增一支測試守著七個 stage：從 `### <stage>` 取到下一個 `### `，斷言一句取自該 stage 規則的錨句出現在該段內。 | Task 4 |
| `audit` 是同一種缺陷的第二個實例，寫計畫時逐句比對才發現：它的圖把規則每一條都改寫過，沒有一句原文。`docs/pipeline.md:607` 的 `docs-check` 節點寫 `every reference still resolves`，掉的是規則 `dead references, never opinions` 的後半。 | Task 4 |
| 四份 `git mv` 進 `docs/archive/` | Task 5 |
| `docs/README.md` 的四列索引改指 `archive/`；索引是手動維護的，移檔不改索引就是製造死連結。 | Task 5 |
| `scripts/docs-check.js:196` 對 `archive` 直接 return，所以十個 `past-end` 一次全清 | Task 5 |
| `docs/improvement-brief.md:171` 與 `docs/README.md:65` 各補一句 | Task 6 |
| 其餘八處經查已經寫了，不動：`docs/README.md:64`、`docs/subagents.md`、`skills/fankeel/SKILL.md`、`skills/fankeel-verify/SKILL.md`、`skills/fankeel-audit/rationale.md`、`TODO.md:69`、`docs/improvement-brief.md:297,520,824`。 | Task 6 |
| `docs/archive/`、`docs/plans/`、`docs/reports/` 與 `docs/sources.md` 都不動 | Task 6 |
| 照 `docs/reports/evidence/2026-09-03-dispatch-vs-inline/ab.sh` 的形狀跑一組新的兩臂，兩臂都用 `haiku`，同一個 HEAD、同一個 CLI 版本。 | Task 7 |
| 主張 | struck — 那是 `## What proves it done` 的表頭，不是承諾 |
| 出 `docs/reports/2026-09-09-haiku-pair.md`，並在 `docs/sources.md` 的帳本加一列 | Task 7 |
| 不改任何規則。規則要不要改是另一條 TODO | Task 7 |
| 構造過的查詢字串進不了 data script 的 `src` | Task 1 |
| `--print` 不寫檔 | Task 2 |
| 指向 N+1 行的引用會被判 `past-end` | Task 3 |
| 七個 stage 的圖都帶著自己規則的錨句 | Task 4 |
| 四份 plan 封存後檢查是乾淨的 | Task 5 |
| 沒有弄壞既有的東西：`npm test` 仍是 `ℹ fail 0`，且通過數不低於 1223 | Task 7 — 最後一個落地的 task 跑完整套 |
