---
status: current
---

# TODO.md `## Ready` 十條 Implementation Plan

**Goal:** 清掉 `TODO.md` 的 `## Ready` 十條，八個改動各帶自己的測試，最後一個 task 把十行條目一起刪掉。

**Architecture:** 八個改動單元加一個收尾。改文件的三個（Task 1、2、4）和改程式的五個（Task 3、5、6、7、8）互不相依，只有共用檔案時才排成前後：`docs/documents.md`（Task 1、6）、`docs/pipeline.md`（Task 2、6）、`tests/docs-audit.test.js`（Task 3、5）、`skills/fankeel/SKILL.md`（Task 4、7）。Task 9 讀其餘八個改過的檔，所以排在最後。

**Tech Stack:** Node.js，`node --test`，零執行期相依。`node:child_process` 的 `execFileSync` 呼叫 `git`。

**Spec:** [2026-09-11-ready-ten-design.md](2026-09-11-ready-ten-design.md)

## Global Constraints

從 `CONTRIBUTING.md`、`package.json`、`.fankeel/map.md` 與測試套件取得，值照抄：

- 測試指令是 `npm test`，展開為 `node --test`。`package.json` 沒有 `dependencies` 也沒有 `devDependencies`，**不得新增任何相依**。
- 本計畫不新增測試檔。若新增，必須先 `git add`，`tests/source.test.js` 讀 `git ls-files` 才看得見。
- **每個 export 都要有 importer**（`tests/source.test.js`）。本計畫新增的 export 只有 `MAX_MOVES`，由 `tests/registry.test.js` 引用；`deletedPaths` 不 export。
- 測試要暫存目錄一律經由 `tests/tmp.js` 的 `tmp(prefix)`，不得自行 `mkdtempSync`。
- `lib/*.js` 是純函式，**不得引用 `scripts/` 或 `hooks/`**；只能反過來。
- `hooks/*.js` 每條路徑都 exit `0`。本計畫不改 hook。
- 文件歸檔照 `.fankeel/map.md`：`docs` 是 reference、`docs/plans` 是 plan、`docs/decisions` 是 decision、`docs/reports` 與 `docs/judgements` 是 report、`docs/archive` 是 archive、`skills` 是 reference、`evals` 是 fixture。新增或改名的頁要在同一個 change 補 `docs/README.md` 索引列 — 本計畫與它的 design 兩列已經補上。
- `docs/archive/`、`docs/decisions/`、`docs/reports/`、`docs/judgements/` 是有日期的紀錄，**不得編輯內容**。
- reference 頁裡寫下的每個 `path:line`，同一行、citation 之後要帶一個反引號引文，內容取自被引那一行（`quoteBeside`，`scripts/docs-check.js:182`，`function quoteBeside(text, from) {`）。
- 暫存與證據放 `.fankeel/build/2026-09-11-ready-ten/`（git-ignored），不放 scratchpad。
- 基準是 `62a65d8`，`ℹ pass 1310 / fail 0`，存於 `.fankeel/build/2026-09-11-ready-ten/baseline-test.txt`。收尾時通過數不得低於 1310。
- `node scripts/docs-check.js` 今天 exit 0，印 9 條 `carries no quote`：4 條在 `docs/collisions.md`、`docs/documents.md`，5 條在 `TODO.md`。
- 版號一律跑 `scripts/version.js`，不手改 — 本計畫不動版號。
- `MAX_CLAIMS = 60`（`lib/registry.js:45`，`const MAX_CLAIMS = 60;`）用 `slice(-MAX_CLAIMS)` 丟最舊的；`MAX_MOVES` 照同一個寫法。
- `npm test` 的輸出用 `grep -E '^ℹ (pass|fail)'` 讀；spec reporter 沒有 `ok`／`not ok` 行。
- Commit 訊息結尾加：

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_016HmYMtMQysv9TUwwLJbvRk
```

## File structure

| 檔案 | 負責什麼 |
|---|---|
| `docs/collisions.md` | 碰撞守衛的 reference 頁；`:140`、`:143`、`:157` 引 `lib/guard.js` |
| `docs/documents.md` | 文件樹與掃描器的 reference 頁；`:326` 引 `scripts/docs-check.js:31`，`:291`–`:300` 講 `pagesByStatus` |
| `docs/pipeline.md` | 七個 stage 的圖；audit 圖在 `:662`–`:706`，map 範例在 `:795`–`:805` |
| `tests/docs-audit.test.js` | `sweep()` 的測試；`:532`–`:601` 是類別表守衛，`:304`–`:366` 是 landed |
| `skills/fankeel/SKILL.md` | 主 skill；`:7` 是 frontmatter 的 `source_of_truth`，`:111`–`:140` 是 registry 欄位計數 |
| `scripts/docs-audit.js` | sweep；`commitTimes` 在 `:110`，landed 迴圈在 `:518`–`:526` |
| `lib/map.js` | `pagesByStatus` 在 `:233`，`buildMap` 在 `:286` |
| `tests/map.test.js` | map 的測試，有 `root()`、`write(dir, rel, text)` |
| `skills/fankeel-survey/SKILL.md` | survey 步驟 3 講 map 的三段 |
| `lib/registry.js` | `touch()` 在 `:430`，常數在 `:33`–`:45`，exports 在 `:691` |
| `scripts/task.js` | 改名在 `:640`–`:674`，adopt 在 `:819` 起 |
| `tests/registry.test.js` | `task(over)`、`tmpRoot()`、`SID`；clock 測試在 `:734`–`:783` |
| `tests/task.test.js` | `run`、`started`、`entry`、`A`、`B`、`DAY`；adopt 在 `:536`，改名在 `:1227` |
| `docs/registry.md` | registry 的 reference 頁；What a stage cost 在 `:108` 起，adopt 在 `:383`–`:399` |
| `scripts/judge.js` | `record()` 在 `:68`，`fail()` 寫 stdout 並 exit 1 |
| `tests/judge.test.js` | `seed()`、`run(root, args, input)`、`A` |
| `skills/fankeel-ask/SKILL.md` | 第 3 步在 `:75`–`:84` |
| `TODO.md` | `## Ready` 十行在 `:63`–`:81` |

## Task 1: 四條引文補在同一行

**Files:**
- Modify: `docs/collisions.md` — `:140`、`:143`、`:157` 三條 citation 後面補引文
- Modify: `docs/documents.md` — `:326` 的 citation 後面補引文
- Read: `lib/guard.js` — `:123`、`:130`、`:131` 是被引的三行，不改

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 計畫帶著每一行的新文字；抄寫加一次 docs-check。

步驟：

1. 看它今天是「紅」的：

```
node scripts/docs-check.js > .fankeel/build/2026-09-11-ready-ten/t1-before.txt; grep -E "docs/(collisions|documents)\.md" .fankeel/build/2026-09-11-ready-ten/t1-before.txt
```

   預期四行，每行結尾 `carries no quote, so nothing checks the line`。

2. 在 `docs/collisions.md`，把這三行：

```md
Two rules keep it from becoming a lockout, both inside `blockers()`
(`lib/guard.js:123`) — one asked of every holder, one only when this session
holds the file too:
```

   換成（`docs/collisions.md`）：

```md
Two rules keep it from becoming a lockout, both inside `blockers()`
(`lib/guard.js:123`, `function blockers(`) — one asked of every holder, one
only when this session holds the file too:
```

3. 在 `docs/collisions.md`，把

```md
- **A dead session's claim never blocks** — `isLive`, `lib/guard.js:130`.
```

   換成（`docs/collisions.md`）：

```md
- **A dead session's claim never blocks** — `isLive`, `lib/guard.js:130` (`if (!isLive(`).
```

4. 在 `docs/collisions.md`，把 `- **The older task holds** — \`claimedFirst\`, \`lib/guard.js:131\`. When both` 那一行的 citation 後面補 ` (\`!claimedFirst(data, mine)\`)`，也就是那一行開頭變成：

```md
- **The older task holds** — `claimedFirst`, `lib/guard.js:131` (`!claimedFirst(data, mine)`). When
```

   該條目其餘的字一個不改，只把後續兩行重新折行，讓每行不超過 80 字元、且沒有任何一對反引號被換行拆開。

5. 在 `docs/documents.md`，把這三行：

```md
counts at the top are in addition to the list, not instead of it. The list is
capped at `MAX_FINDINGS = 200` (`scripts/docs-check.js:31`) and says so when it
bites, so a run past that cap is the one case where the printed list is not the
```

   換成（`docs/documents.md`；citation 與引文必須同在一行，`quoteBeside` 只看 citation 那一行，所以換行點落在 citation 之前）：

```md
counts at the top are in addition to the list, not instead of it. The list is
capped at `MAX_FINDINGS = 200`
(`scripts/docs-check.js:31`, `const MAX_FINDINGS = 200`) and says so when it
bites, so a run past that cap is the one case where the printed list is not the
```

6. 看它綠：

```
node scripts/docs-check.js > .fankeel/build/2026-09-11-ready-ten/t1-after.txt; echo "exit $?"; ! grep -E "docs/(collisions|documents)\.md" .fankeel/build/2026-09-11-ready-ten/t1-after.txt
```

   預期 `exit 0`，且 grep 什麼都沒印（`!` 讓沒找到成為成功）。若出現 `no longer finds` 之類的新行，代表引文和被引那一行對不上，照那一行的原文修正引文。

7. Commit 這兩個檔：`docs: four citations quote the line they point at`。

## Task 2: 稽核那張圖

**Files:**
- Modify: `docs/pipeline.md` — audit 圖的 E2 標籤（`:674`）與 E2 的邊（`:695`）

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** in-session — 兩行替換，派工比工作本身貴。

步驟：

1. 看它今天是「紅」的：

```
awk 'NR>=662&&NR<=706' docs/pipeline.md | grep -cE 'E2 --> F2|directories no page names'
```

   預期 `0`（grep 在 0 時 exit 1，這裡要的就是那個 0）。

2. 在 `docs/pipeline.md`，把

```md
    E2["<b>unfiled · undeclared ·<br/>linked from nowhere</b>"]
```

   換成（`docs/pipeline.md`）：

```md
    E2["<b>unfiled · undeclared ·<br/>linked from nowhere ·<br/>directories no page names</b>"]
```

3. 在 `docs/pipeline.md` 的 audit 圖裡，把 `E3 --> F2` 上一行的

```md
    E2 --> F
```

   換成（`docs/pipeline.md`）：

```md
    E2 --> F2
```

   class 那張圖（`:418` 的 `E2 --> F`）不動：它的 E1 是 spike，那裡的 F 是別的節點。

4. 看它綠：

```
awk 'NR>=662&&NR<=706' docs/pipeline.md | grep -cE 'E2 --> F2|directories no page names'; node --test tests/pipeline-doc.test.js 2>&1 | grep -E '^ℹ (pass|fail)'
```

   預期 `2`，以及 `ℹ fail 0`。

5. Commit：`docs: the audit diagram sends E2 to the adversary and names uncovered directories`。

## Task 3: sweep 類別表的守衛讀兩頁

**Files:**
- Modify: `tests/docs-audit.test.js` — `:585`–`:600` 改成對兩頁各跑一次
- Read: `skills/fankeel-audit/SKILL.md` — `:102`–`:113` 是第二張表，不改

**Interfaces:**
- Consumes: `sweep()` 回傳物件的鍵，測試在 `:538`–`:549` 自己解析，不改
- Produces: none

**Dispatch:** implementer, sonnet — 計畫帶著程式碼；抄寫加一次紅綠對照。

照設計的原話：每頁一張自己的 `ROW` 對照與自己的錨點，因為兩頁措辭不同。前者錨在 `| | |` 到 `Only the first four fail the run.`；後者錨在 `| Section | Defect | What it means |` 到 `## The part only reading finds`。

新測今天就綠，所以證明它的是一次突變，要做兩次：改測試之前一次（仍然綠，這是缺口），改之後一次（變紅）。

步驟：

1. 突變，看今天的測試抓不到：

```
node -e "const fs=require('fs'),f='skills/fankeel-audit/SKILL.md';fs.writeFileSync(f,fs.readFileSync(f,'utf8').split('\n').filter(l=>!l.startsWith('| **directories with no reference document**')).join('\n'))"
node --test tests/docs-audit.test.js 2>&1 | grep -E '^ℹ (pass|fail)'
git checkout -- skills/fankeel-audit/SKILL.md
```

   預期 `ℹ fail 0` — 第二頁少了一列，今天沒有東西發現。

2. 在 `tests/docs-audit.test.js`，把 `:585` 的 `const skillText = ...` 到 `:600` 的 `}`（`for (const key of keys)` 迴圈的結尾，測試的 `});` 之前）整段換成：

```js
  // The same table lives on two pages — the core skill's and the audit skill's —
  // and a guard over one let the other fall behind with nothing to say so. Each
  // page words its rows its own way, so each carries its own row map and its own
  // anchors; the keys and the bookkeeping above are shared.
  const PAGES = [
    {
      file: 'skills/fankeel/SKILL.md',
      start: '| | |',
      end: 'Only the first four fail the run.',
      row: ROW,
    },
    {
      file: 'skills/fankeel-audit/SKILL.md',
      start: '| Section | Defect | What it means |',
      end: '## The part only reading finds',
      row: {
        drift: '**fallen behind the code they describe**',
        landed: '**plans look landed**',
        index: '**index**',
        diagrams: '**diagrams behind their directory**',
        overlaps: '**pairs describing the same code**',
        unresolved: '**unresolved source_of_truth**',
        orphans: '**linked from nowhere**',
        uncovered: '**directories with no reference document**',
        unfiled: '**unfiled**',
        undeclared: '**undeclared**',
      },
    },
  ];

  for (const page of PAGES) {
    const text = fs.readFileSync(path.join(__dirname, '..', ...page.file.split('/')), 'utf8');
    // Slice the table out rather than searching the whole page — a bold phrase in
    // unrelated prose elsewhere would pass a whole-file search.
    const endAt = text.indexOf(page.end);
    assert.notEqual(endAt, -1, page.file + ' has no ' + JSON.stringify(page.end));
    const start = text.lastIndexOf(page.start, endAt);
    assert.notEqual(start, -1, page.file + ' has no table above ' + JSON.stringify(page.end));
    const table = text.slice(start, endAt);

    for (const key of keys) {
      if (BOOKKEEPING.has(key)) continue;
      assert.ok(page.row[key], 'sweep() returns r.' + key + ' and ' + page.file + ' has no row mapped for it — classify it');
      assert.ok(table.includes(page.row[key]),
        'the sweep table in ' + page.file + ' has no ' + page.row[key] + ' row for r.' + key);
    }
  }
```

3. 看它綠：`node --test tests/docs-audit.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 0`。

4. 再做一次步驟 1 的突變，這次預期 `ℹ fail 1`，失敗訊息是 `the sweep table in skills/fankeel-audit/SKILL.md has no **directories with no reference document** row for r.uncovered`。然後 `git checkout -- skills/fankeel-audit/SKILL.md`，再跑一次看回到 `ℹ fail 0`。

5. Commit `tests/docs-audit.test.js`：`test: the sweep table guard reads both pages that carry it`。

## Task 4: SKILL.md 宣告它延後給的四頁

**Files:**
- Modify: `skills/fankeel/SKILL.md` — `:7` 的 `source_of_truth`
- Read: `scripts/docs-audit.js` — `:448` 的 `defers()` 判斷、`:271`–`:274` 說明 `.md` 條目不進 drift，不改

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** in-session — 一次 Edit，前後各一次 live sweep。

步驟：

1. 在同一棵樹上量「前」：

```
node -e "const a=require('./scripts/docs-audit.js');const r=a.sweep('.',a.DEFAULT_SINCE,Date.now());const me='skills/fankeel/SKILL.md';console.log(r.overlaps.length+' | '+r.overlaps.filter(p=>p.a===me||p.b===me).map(p=>p.a===me?p.b:p.a).sort().join(' '))" > .fankeel/build/2026-09-11-ready-ten/t4-before.txt; cat .fankeel/build/2026-09-11-ready-ten/t4-before.txt
```

   預期那一列列出 8 頁，其中有 collisions、registry、station、statusline 這四份 `docs/` 下的頁。

2. 在 `skills/fankeel/SKILL.md`，把

```md
source_of_truth: lib/stages.js, lib/registry.js, lib/live.js, scripts/task.js, lib/guard.js
```

   換成（`skills/fankeel/SKILL.md`）：

```md
source_of_truth: lib/stages.js, lib/registry.js, lib/live.js, scripts/task.js, lib/guard.js, docs/collisions.md, docs/registry.md, docs/station.md, docs/statusline.md
```

   `docs/documents.md` 不加：它自己的 `source_of_truth`（`docs/documents.md:4`）已經宣告 SKILL.md，`defers()` 兩向都認。

3. 用步驟 1 的同一行指令量「後」，存成 `t4-after.txt`。預期：`overlaps` 比「前」少 4，SKILL.md 那一列剩 `docs/improvement-brief.md docs/pipeline.md docs/subagents.md skills/fankeel-survey/SKILL.md`。

4. `node scripts/docs-check.js` 仍 exit 0，且沒有提到 `skills/fankeel/SKILL.md` 的 `unresolved` 新行。

5. Commit：`docs: the core skill declares the four pages it defers to`。

## Task 5: 刪掉的檔不算未建

**Files:**
- Modify: `scripts/docs-audit.js` — `commitTimes` 之後加 `deletedPaths(root)`；landed 迴圈（`:518`–`:526`）讀它
- Test: `tests/docs-audit.test.js`

**Interfaces:**
- Consumes: `HISTORY`（`scripts/docs-audit.js:49`，`const HISTORY = 4000;`）與 `execFileSync`（`:27`），都已存在
- Produces: `deletedPaths(root)` → `Set<string>`，模組內部用，不 export

**Dispatch:** implementer, sonnet — 計畫帶著程式碼與 git fixture；抄寫加測試。

步驟：

1. 在 `tests/docs-audit.test.js`，`const path = require('node:path');` 下一行加：

```js
const { execFileSync } = require('node:child_process');
```

2. 在 `tests/docs-audit.test.js`，`test('a landed plan touched today is held back by the settle period', ...)` 那支測試之後、`// --- the index ---` 之前，加上：

```js
// A plan whose work was a deletion names a path that is gone, and `pointsAt()`
// files a gone path as unbuilt — so the plans that retired the station skill
// stayed open for good. A path git has seen deleted is the plan's work done;
// one that never existed still holds its plan open. The commits are back-dated
// because with a repository the dates come from the log, not from mtime.
test('a plan naming a file git has seen deleted can land; one never built still cannot', () => {
  const root = withTree(tree({
    'docs/plans/2026-01-01-x.md': { body: 'keep `lib/badge.js`, delete `lib/old.js`\n', age: 60 },
    'docs/plans/2026-01-02-y.md': { body: 'keep `lib/badge.js`, add `lib/future.js`\n', age: 60 },
    'lib/badge.js': 'x\n',
    'lib/old.js': 'x\n',
  }), 'flat');
  const at = new Date(daysAgo(60)).toISOString();
  const env = Object.assign({}, process.env, { GIT_AUTHOR_DATE: at, GIT_COMMITTER_DATE: at });
  const git = (args) => execFileSync('git', args, { cwd: root, env, stdio: 'ignore' });
  git(['init', '-q']);
  git(['config', 'user.email', 'test@example.invalid']);
  git(['config', 'user.name', 'test']);
  git(['config', 'commit.gpgsign', 'false']);
  git(['add', '-A']);
  git(['commit', '-q', '-m', 'one']);
  git(['rm', '-q', 'lib/old.js']);
  git(['commit', '-q', '-m', 'two']);
  assert.deepEqual(sweep(root).landed.map((l) => l.file), ['docs/plans/2026-01-01-x.md']);
});
```

3. 看它紅：`node --test tests/docs-audit.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 1`，實際值是 `[]`。

4. 在 `scripts/docs-audit.js`，`commitTimes` 函式結束的 `}` 之後、`// Modification time, for a directory that is not a repository.` 之前，加上：

```js
// Every path a commit has deleted. `pointsAt()` files a path that is not on
// disk as unbuilt, and a plan whose work was a deletion names exactly such a
// path — so without this the plans that retired a file stayed open for good,
// the four that retired the station skill among them. One `git log` for the
// whole tree, for the reason `commitTimes` gives; outside a repository, or when
// git fails, nothing counts as deleted and landed reads as it always did.
function deletedPaths(root) {
    let out;
    try {
        out = execFileSync('git', ['log', '--diff-filter=D', '--format=', '--name-only', '--no-renames', '-n', String(HISTORY)], {
            cwd: root, encoding: 'utf8', maxBuffer: 64 * 1024 * 1024, stdio: ['ignore', 'pipe', 'ignore'],
        });
    } catch (e) {
        return new Set();
    }
    return new Set(out.split('\n').map((l) => l.trim()).filter(Boolean));
}

```

5. 在 `scripts/docs-audit.js` 的 landed 段，把

```js
    const landed = [];
    for (const rel of markdown) {
        if (docs.roleOf(tree, rel) !== 'plan') continue;
        const at = dates.at(rel);
        if (!at || daysBetween(now, at) < settled) continue;
        const { code: named, unbuilt } = points.get(rel);
        if (!named.length || unbuilt.length) continue;
```

   換成（`scripts/docs-audit.js`）：

```js
    const landed = [];
    const gone = deletedPaths(root);
    for (const rel of markdown) {
        if (docs.roleOf(tree, rel) !== 'plan') continue;
        const at = dates.at(rel);
        if (!at || daysBetween(now, at) < settled) continue;
        const { code: named, unbuilt } = points.get(rel);
        // `unbuilt` still means "not on disk now". What changes is how this reads
        // it: a path git has seen deleted is the plan's work done, not its work
        // waiting, so only a path that never existed holds the plan open.
        if (!named.length || unbuilt.some((p) => !gone.has(p))) continue;
```

6. 看它綠：`node --test tests/docs-audit.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 0`。既有的 `a plan still naming something unbuilt has not landed`（`:317`）也必須仍然綠 — 它沒有 git，`gone` 是空 Set。

7. 記下它在本庫的效果，不設門檻：`node -e "const a=require('./scripts/docs-audit.js');console.log(a.sweep('.',a.DEFAULT_SINCE,Date.now()).landed.map(l=>l.file).join('\n'))" > .fankeel/build/2026-09-11-ready-ten/t5-landed.txt`。今天是 0 份；這份清單交給 audit 階段，不在這裡歸檔任何東西。

8. Commit 兩個檔：`fix: a path git has seen deleted no longer holds a plan open`。

## Task 6: archive 桶一律 retired，retired 按桶列

**Files:**
- Modify: `lib/map.js` — `pagesByStatus()` 加 archive 分支；`buildMap()` 的 `documents:` 行與 retired 段
- Modify: `skills/fankeel-survey/SKILL.md` — `:101`–`:102` 的 retired 段說明
- Modify: `docs/pipeline.md` — `:795`–`:805` 的 map 說明與範例
- Modify: `docs/documents.md` — `:291`–`:300` 那段之後加一段
- Test: `tests/map.test.js`
- Read: `lib/docs.js` — `roleOf(tree, rel)`（`:177`）在 `tree` 為 null 時回傳 null，不改

**Interfaces:**
- Consumes: `docs.roleOf(tree, rel)` → `string | null`，已存在
- Produces: map 的 `documents: <n> markdown files — <k> current, <k> planned, <k> retired, ...` 一行；retired 段每個 archive 桶一行 `  <bucket> — <n>, the whole archive bucket`

**Dispatch:** implementer, sonnet — 計畫帶著程式碼與三段文字；抄寫加測試。

步驟：

1. 在 `tests/map.test.js`，`test('the map names what was planned but not built, because nothing else does', ...)` 之後加上：

```js
// Archiving is a move, not an edit, so a page in an archive bucket can still say
// `current` or `design-intent`. On 2026-09-11 twenty-four did, and the four
// saying the second were listed as planned, not built. Where it sits decides.
test('every page in an archive bucket is retired, whatever its frontmatter says', () => {
  const dir = root();
  write(dir, '.fankeel/docs.json', JSON.stringify({ preset: 'flat', index: 'docs/README.md', buckets: [{ path: 'docs', role: 'reference', depth: 1 }, { path: 'docs/archive', role: 'archive' }] }));
  write(dir, 'docs/now.md', '---\nstatus: current\n---\n# Now\n');
  write(dir, 'docs/archive/old-plan.md', '---\nstatus: design-intent\n---\n# Old plan\n');
  write(dir, 'docs/archive/old-page.md', '---\nstatus: current\n---\n# Old page\n');
  write(dir, 'docs/archive/bare.md', '# Bare\n');
  const by = map.pagesByStatus(dir);
  assert.deepEqual(by.intent, []);
  assert.deepEqual(by.undeclared, []);
  assert.deepEqual(by.retired, ['docs/archive/bare.md', 'docs/archive/old-page.md', 'docs/archive/old-plan.md']);
  assert.ok(by.current.includes('docs/now.md'));
});

// The retired section names a bucket per line rather than a page per line, so
// `MAX_PAGES` never cuts it — on 2026-09-11 it held 59 and showed 30 — and the
// counts on `documents:` add up to the total, which is what lets a page named
// nowhere be read as current. With a list cut, that sentence would lie.
test('the map counts every status, files an archive bucket as one line, and says when an unnamed page is current', () => {
  const dir = root();
  write(dir, '.fankeel/docs.json', JSON.stringify({ preset: 'flat', index: 'docs/README.md', buckets: [{ path: 'docs', role: 'reference', depth: 1 }, { path: 'docs/archive', role: 'archive' }] }));
  write(dir, 'docs/now.md', '---\nstatus: current\n---\n# Now\n');
  write(dir, 'docs/roadmap.md', '---\nstatus: design-intent\n---\n# Roadmap\n');
  write(dir, 'docs/gone.md', '---\nstatus: archived\n---\n# Gone\n');
  for (let i = 0; i < 40; i++) write(dir, 'docs/archive/p' + String(i).padStart(2, '0') + '.md', '---\nstatus: current\n---\n# P\n');
  const text = map.buildMap(dir);
  assert.match(text, /documents: 43 markdown files — 1 current, 1 planned, 41 retired\n/);
  assert.match(text, /a page named nowhere below is current\./);
  assert.match(text, /retired, do not follow — 41:\n  docs\/archive — 40, the whole archive bucket\n  docs\/gone\.md\n/);
  assert.doesNotMatch(text, /docs\/archive\/p\d\d\.md/);
  assert.doesNotMatch(text, /\.\.\. and \d+ more/);

  const many = root();
  for (let i = 0; i < 31; i++) write(many, 'docs/bare' + String(i).padStart(2, '0') + '.md', '# Bare\n');
  assert.doesNotMatch(map.buildMap(many), /a page named nowhere below is current/);
});
```

2. 看它紅：`node --test tests/map.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 2`。

3. 在 `lib/map.js` 的 `pagesByStatus()`，signpost 分支（`if (docs.isSignpost(rel)) { ... continue; }`）之後、`const contract = docs.contractOf(text);` 之前，加上：

```js
        // An archive is retired by where it sits, not by what its frontmatter
        // still says. Archiving is a move rather than an edit, so on 2026-09-11
        // twenty-four pages under `docs/archive/` still read `current` or
        // `design-intent` — and the four of the second kind were listed as
        // planned, not built, the section survey is told to read first.
        if (docs.roleOf(tree, rel) === 'archive') {
            out.retired.push(rel);
            continue;
        }
```

4. 在 `lib/map.js` 的 `buildMap()`，把從 `const by = pagesByStatus(root);` 到 retired 段結尾的這一塊：

```js
    const by = pagesByStatus(root);
    lines.push('');
    lines.push('documents: ' + Object.keys(by).reduce((n, k) => n + by[k].length, 0) + ' markdown files');
```

   換成（`lib/map.js`）：

```js
    const by = pagesByStatus(root);
    const total = Object.keys(by).reduce((n, k) => n + by[k].length, 0);
    // Every status counted, so the figures add up to the line's own total and a
    // page's status is never inferred from which list it failed to appear in.
    const counts = [['current', by.current], ['planned', by.intent], ['retired', by.retired],
        ['generated', by.generated], ['undeclared', by.undeclared]]
        .filter(([, list]) => list.length)
        .map(([word, list]) => list.length + ' ' + word);
    lines.push('');
    lines.push('documents: ' + total + ' markdown files' + (counts.length ? ' — ' + counts.join(', ') : ''));

    // An archive bucket is retired whole, so it is one line rather than a page a
    // line — which is also what stops `MAX_PAGES` cutting the section: on
    // 2026-09-11 it held 59 pages and showed 30. A retired page outside every
    // archive bucket is still named.
    const tree = declared.tree;
    const archives = tree ? tree.buckets.filter((b) => b.role === 'archive').map((b) => b.path) : [];
    const whole = new Map();
    const loose = [];
    for (const rel of by.retired) {
        const bucket = docs.roleOf(tree, rel) === 'archive'
            ? archives.filter((p) => rel.startsWith(p + '/')).sort((a, b) => b.length - a.length)[0]
            : undefined;
        if (bucket) whole.set(bucket, (whole.get(bucket) || 0) + 1);
        else loose.push(rel);
    }

    // True only when no list below is cut and nothing counted goes unlisted:
    // `generated` is counted above and never named, so it rules the sentence
    // out as surely as a cut list does.
    const cut = [by.intent, loose, by.undeclared].some((list) => list.length > MAX_PAGES);
    if (!cut && !by.generated.length) lines.push('a page named nowhere below is current.');
```

   再把 `lib/map.js` 的 retired 段：

```js
    if (by.retired.length) {
        lines.push('');
        lines.push('retired, do not follow — ' + by.retired.length + ':');
        for (const l of listing(by.retired)) lines.push(l);
    }
```

   換成（`lib/map.js`）：

```js
    if (by.retired.length) {
        lines.push('');
        lines.push('retired, do not follow — ' + by.retired.length + ':');
        for (const [bucket, n] of whole) lines.push('  ' + bucket + ' — ' + n + ', the whole archive bucket');
        for (const l of listing(loose)) lines.push(l);
    }
```

   intent 段與 undeclared 段不動。

5. 看它綠：`node --test tests/map.test.js tests/map-cli.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 0`。

6. 在 `skills/fankeel-survey/SKILL.md`，把

```md
**retired, do not follow** is the opposite error: a page that was true once and
is being read as though it still were.
```

   換成（`skills/fankeel-survey/SKILL.md`）：

```md
**retired, do not follow** is the opposite error: a page that was true once and
is being read as though it still were. Every page in an `archive` bucket is
filed there whatever its frontmatter says, one line per bucket; a retired page
outside one is named. `documents:` counts every status, and when no list is cut
it says so: a page named nowhere below is current.
```

7. 在 `docs/pipeline.md`，把

````md
the part nothing else reports, **every page's declared status**. That last
section is the one the rest was built for:

```
planned, not built — 2:
  docs/archive/2026-08-22-seven-stage-pipeline.md
  docs/roadmap.md
```
````

   換成（`docs/pipeline.md`）：

````md
the part nothing else reports, **every page's declared status** — each status
counted, the planned, retired and undeclared pages named, and an `archive`
bucket filed as retired on one line whatever its pages declare. That last
section is the one the rest was built for:

```
planned, not built — 1:
  docs/roadmap.md
```
````

8. 在 `docs/documents.md`，`from a line that claims to be every markdown file.`（講 `pagesByStatus` 那段的最後一行）之後，空一行再加上：

```md
A page in an `archive` bucket is filed as `retired` before its frontmatter is
read, the way a bare page under a `fixture` bucket is filed as `current`.
Archiving is a move rather than an edit: on 2026-09-11 twenty-four pages under
`docs/archive/` still said `current` or `design-intent`, and the four saying the
second were listed as planned, not built. The retired section names a bucket
per line rather than a page, so `MAX_PAGES` no longer cuts it, and `documents:`
counts every status, so a page named nowhere below reads as current.
```

9. 產出物自查：

```
node -e "const t=require('./lib/map.js').buildMap('.');const m=/documents: (\d+) markdown files — (.*)/.exec(t);console.log('total',m[1],'sum',m[2].split(', ').reduce((n,s)=>n+parseInt(s,10),0));console.log('archive line',(/docs\/archive — (\d+), the whole archive bucket/.exec(t)||[])[1])"; git ls-files docs/archive | grep -c '\.md$'
```

   預期 `total` 等於 `sum`，`archive line` 等於最後那個數字。另外 `node scripts/map.js --print | grep -A3 '^planned, not built'` 只剩 `docs/plans/2026-09-09-design-class-prompt.md` 與本計畫的兩份 design-intent 頁。

10. Commit 五個檔：`fix: an archive bucket is retired whole, and the map counts every status`。

## Task 7: 階段移動的紀錄 `moves`

**Files:**
- Modify: `lib/registry.js` — 新常數 `MAX_MOVES`；`touch()` 追加 `moves`；export `MAX_MOVES`
- Modify: `scripts/task.js` — 改名刪 `moves`；adopt 平移 `moves`
- Modify: `docs/registry.md` — What a stage cost 加一段；adopt 段加一句
- Modify: `skills/fankeel/SKILL.md` — `:111`、`:114`、`:138` 的計數，`:128` 之後加一段
- Test: `tests/registry.test.js`
- Test: `tests/task.test.js`

**Interfaces:**
- Consumes: none
- Produces: `registry.MAX_MOVES`（`60`）；紀錄欄位 `moves: Array<[string, number]>`，每筆是 stage 名稱與 epoch 毫秒

**Dispatch:** implementer, sonnet — 計畫帶著程式碼與文件文字；抄寫加測試。

步驟：

1. 在 `tests/registry.test.js`，`test('a clock that is not a readable pair is replaced', ...)` 之後加上：

```js
// The clock keeps one pair per stage, so a verify that went back to build and
// returned reads as one long verify. `moves` keeps the order: one entry per
// change of stage, stamped with the sighting the clock also took.
test('touch appends a move on each change of stage and none while it stays', () => {
  const root = tmpRoot();
  registry.writeSession(root, SID, task({ stage: 'build' }));
  registry.touch(root, SID);
  registry.touch(root, SID);
  const go = (stage) => {
    const d = registry.readSession(root, SID);
    d.stage = stage;
    registry.writeSession(root, SID, d);
    registry.touch(root, SID);
  };
  go('verify');
  go('build');
  go('verify');
  const after = registry.readSession(root, SID);
  assert.deepEqual(after.moves.map((m) => m[0]), ['build', 'verify', 'build', 'verify']);
  assert.equal(after.moves[0][1], after.clock.build[0]);
  assert.equal(after.moves[1][1], after.clock.verify[0]);
});

test('moves keeps the latest MAX_MOVES and drops the oldest', () => {
  const root = tmpRoot();
  const moves = [];
  for (let i = 0; i < registry.MAX_MOVES; i++) moves.push([i % 2 ? 'build' : 'verify', 1000 + i]);
  registry.writeSession(root, SID, task({ stage: 'verify', moves }));
  registry.touch(root, SID);
  const after = registry.readSession(root, SID);
  assert.equal(after.moves.length, registry.MAX_MOVES);
  assert.deepEqual(after.moves[0], moves[1]);
  assert.equal(after.moves[after.moves.length - 1][0], 'verify');
});
```

   （`moves` 的最後一筆 `i = 59` 是 `build`，所以 `verify` 的 touch 追加一筆，61 筆丟掉最舊的一筆。）

2. 在 `tests/task.test.js`，`test('adopt carries the wall-clock cost and leaves the token count behind', ...)` 之後加上：

```js
// `moves` is a sequence of wall-clock stamps, so it goes over the way `clock`
// does: shifted by the gap between the source falling quiet and the adopt, so
// the order and the spacing survive and the fortnight nobody was on it does not.
test('adopt carries the moves, shifted to end where the source fell quiet', () => {
  const dir = root();
  started(dir, A, 'tidy the project cards', 'Waypoint');
  const source = entry(dir, A);
  const quiet = Date.now() - 16 * DAY;
  source.updated = new Date(quiet).toISOString();
  source.moves = [['survey', quiet - 50 * 60e3], ['design', quiet - 30 * 60e3]];
  registry.writeSession(dir, A, source);

  assert.equal(run(dir, ['adopt', A, '--session', B]).code, 0);
  const mine = entry(dir, B);
  assert.deepEqual(mine.moves.map((m) => m[0]), ['survey', 'design']);
  assert.equal(mine.moves[1][1] - mine.moves[0][1], 20 * 60e3);
  assert.ok(Date.now() - (mine.moves[1][1] + 30 * 60e3) < 30e3);
});
```

   並在 `tests/task.test.js` 的 `test('renaming the task forgets the clock, the wait, the per-stage spend and any open gate', ...)` 之後加上：

```js
test('renaming the task forgets the moves, as it forgets the clock', () => {
  const dir = root();
  started(dir, A, 'rework the colour ramp');
  const data = entry(dir, A);
  data.moves = [['survey', 1000], ['design', 61000]];
  registry.writeSession(dir, A, data);
  run(dir, ['task', 'something else entirely', '--session', A]);
  assert.equal(entry(dir, A).moves, undefined);
});
```

3. 看它紅：`node --test tests/registry.test.js tests/task.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 4`。

4. 在 `lib/registry.js`，`const MAX_CLAIMS = 60;` 之後加上：

```js

// The order a task's stages came in, one entry per change of stage. Oldest
// evicted, as claims are: the cap bounds the file rather than claiming the
// sixty-first move is the least interesting one.
const MAX_MOVES = 60;
```

5. 在 `lib/registry.js` 的 `touch()`，`data.clock = clock;` 之後（同一個 `if (data.stage) { ... }` 裡）加上：

```js
            // `clock` keeps one pair per stage, so a verify that went back to
            // build and returned reads as one long verify. The order is kept
            // beside it: one entry per change of stage, stamped with this same
            // sighting, so the entry opening a stage's first visit carries that
            // stage's clock first.
            const moves = Array.isArray(data.moves) ? data.moves : [];
            const last = moves[moves.length - 1];
            if (!Array.isArray(last) || last[0] !== data.stage) {
                moves.push([data.stage, at]);
                data.moves = moves.slice(-MAX_MOVES);
            }
```

6. 在 `lib/registry.js` 的 `module.exports`，`MAX_CLAIMS,` 之後加一行 `MAX_MOVES,`。

7. 在 `scripts/task.js` 的改名處理，`delete d.waited;` 之後加上：

```js
        // The order of stages, for the same reason: the names come round again,
        // and a move left here draws the old task's visits into the new one.
        delete d.moves;
```

8. 在 `scripts/task.js` 的 adopt，`if (Object.keys(waited).length) data.waited = waited;` 之後加上：

```js
    // `moves` is wall-clock too, and goes over the way `clock` does: shifted by
    // the gap between the source's last sighting and this adopt, so the order
    // and the spacing survive and the fortnight nobody was on it does not.
    const quietAt = Date.parse(source.updated);
    if (Array.isArray(source.moves) && Number.isFinite(quietAt)) {
        const moves = source.moves
            .filter((m) => Array.isArray(m) && m.length === 2 && typeof m[0] === 'string' && Number.isFinite(m[1]))
            .map((m) => [m[0], m[1] + (at - quietAt)]);
        if (moves.length) data.moves = moves;
    }
```

9. 看它綠：`node --test tests/registry.test.js tests/task.test.js tests/inject.test.js tests/resume.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 0`。

10. 在 `docs/registry.md`，What a stage cost 的 JSON 區塊（`"waited": { "survey": 240000 }` 那行之後的結束 fence）之後，空一行加上：

```md
`moves` sits beside them and is not a cost. It is one `[stage, at]` for each
change of stage, appended by the same `touch` that writes `clock` and stamped
with the same sighting, so the entry opening a stage's first visit carries that
stage's `clock` first. `clock` keeps one pair per stage, which makes a verify
that went back to build and returned read as one long verify; `moves` keeps the
order. Sixty at most, oldest dropped — `MAX_MOVES` in `lib/registry.js`. `task`
clears it with the four above.
```

11. 在 `docs/registry.md` 的 adopt 段，`next answer in the adopting session would close it against a stamp from another` 之後那行 `one.` 的後面，接一句（同一段落，照 80 字元重新折行）：

```md
one. `moves` goes over the way `clock` does, every stamp shifted by the gap
between the source's `updated` and the adopt, so the order and the spacing
survive and the fortnight does not.
```

12. 在 `skills/fankeel/SKILL.md`：
    - `Ten more are written without anyone typing them.` 改成 `Eleven more are written without anyone typing them.`
    - `the six below` 改成 `the seven below`
    - `An eleventh, \`gateAt\`, is deliberately not below.` 改成 `A twelfth, \`gateAt\`, is deliberately not below.`
    - 在 `skills/fankeel/SKILL.md` 的 `has that run, and what the older process looked like from inside.` 那一段之後，空一行加上：

```md
`moves` is the order those stages came in: one `[stage, at]` for each change of
stage, stamped with the sighting `clock` takes, so a verify that went back to
build and returned reads as two visits rather than one long one. Sixty at most,
oldest dropped.
```

13. `npm test 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 0`。`node scripts/docs-check.js` 仍 exit 0。

14. Commit 六個檔：`feat: the registry keeps the order of stages beside the clock`。

## Task 8: `judge.js record` 拒收空答案

**Files:**
- Modify: `scripts/judge.js` — `record()` 在讀完答案後檢查空白
- Modify: `skills/fankeel-ask/SKILL.md` — 第 3 步加一段
- Test: `tests/judge.test.js`

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** implementer, sonnet — 計畫帶著程式碼；抄寫加測試。

步驟：

1. 在 `tests/judge.test.js`，`test('no session, a stood-down session, or a bad slug exits 1', ...)` 之後加上：

```js
// Filed on 2026-09-11 with nothing under `## Answer`: the answer was drawn from a
// background agent's tasks/<id>.output, which is 0 bytes, and record exited 0.
test('a blank answer exits 1, says where the answer lives, and files nothing', () => {
  const root = seed();
  const args = ['record', '--session', A, '--brief', path.join(root, 'brief.md'), '--slug', 'ramp', '--answer', '-'];
  for (const input of ['', ' \n\t\n']) {
    const out = run(root, args, input);
    assert.equal(out.code, 1, JSON.stringify(input));
    assert.match(out.out, /agent-<id>\.jsonl/);
  }
  assert.equal(fs.existsSync(path.join(root, 'docs', 'judgements')), false);
});
```

2. 看它紅：`node --test tests/judge.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 1`。

3. 在 `scripts/judge.js` 的 `record()`，`const answer = readAnswer(opts.answer);` 之後加上：

```js
    // Filed on 2026-09-11 with nothing under `## Answer`: the answer came from a
    // background agent's `tasks/<id>.output`, which is 0 bytes, and this exited 0.
    if (!answer.trim()) fail('The answer is empty, so there is nothing to record. A background judge\'s answer is the last assistant message in <session>/subagents/agent-<id>.jsonl; its tasks/<id>.output is 0 bytes.');
```

4. 看它綠：`node --test tests/judge.test.js 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 0`。

5. 在 `skills/fankeel-ask/SKILL.md` 第 3 步，`project has a \`## Judgements\` table.` 那行之後，空一行加上（三格縮排，留在第 3 項裡）：

```md
   The answer is the judge's last assistant message. For a background
   dispatch that is in `<session>/subagents/agent-<id>.jsonl` — its
   `tasks/<id>.output` is 0 bytes, and `record` refuses the blank answer
   piping that file would give it.
```

6. `node scripts/docs-check.js` 仍 exit 0。

7. Commit 三個檔：`fix: judge.js record refuses a blank answer and says where the real one is`。

## Task 9: 刪掉 `TODO.md` 的十條

**Files:**
- Modify: `TODO.md` — `## Ready` 底下十個 bullet
- Read: `docs/collisions.md` — Task 1 已落地
- Read: `docs/pipeline.md` — Task 2、6 已落地
- Read: `tests/docs-audit.test.js` — Task 3、5 已落地
- Read: `skills/fankeel/SKILL.md` — Task 4、7 已落地
- Read: `scripts/docs-audit.js` — Task 5 已落地
- Read: `lib/map.js` — Task 6 已落地
- Read: `lib/registry.js` — Task 7 已落地
- Read: `scripts/judge.js` — Task 8 已落地

**Interfaces:**
- Consumes: none
- Produces: none

**Dispatch:** in-session — 刪十行加一次 todo-check，派工比工作本身貴。

步驟：

1. 確認前八個 task 都已 commit：`git log --oneline 62a65d8..HEAD` 列得出八個對應的 commit。再逐檔確認每條的改動確實在檔裡，一條沒到就不刪它的 TODO 行：

```
for pair in "docs/collisions.md|function blockers(" "docs/pipeline.md|E2 --> F2" "tests/docs-audit.test.js|skills/fankeel-audit/SKILL.md" "skills/fankeel/SKILL.md|docs/statusline.md" "scripts/docs-audit.js|function deletedPaths" "lib/map.js|the whole archive bucket" "lib/registry.js|const MAX_MOVES" "scripts/judge.js|agent-<id>.jsonl"; do f=${pair%%|*}; s=${pair#*|}; grep -qF "$s" "$f" && echo "ok  $f" || echo "MISSING  $f: $s"; done
```

   預期八行 `ok`。

2. 在 `TODO.md`，刪掉 `## Ready` 與 `## Needs a decision` 之間的十個 bullet 與它們之間的空行，只留：

```md
## Ready

## Needs a decision
```

3. 看它綠：

```
node scripts/todo-check.js; echo "todo-check exit $?"; node scripts/docs-check.js > .fankeel/build/2026-09-11-ready-ten/t9-docs-check.txt; echo "docs-check exit $?"; grep -c "carries no quote" .fankeel/build/2026-09-11-ready-ten/t9-docs-check.txt
```

   預期 `todo-check exit 0`、`docs-check exit 0`，`carries no quote` 從 9 變 1（剩 `## Waiting` 裡引 `skills/fankeel-audit/SKILL.md:113` 的那條）。

4. `npm test 2>&1 | grep -E '^ℹ (pass|fail)'`，預期 `ℹ fail 0`，pass 不低於 1310。

5. Commit：`chore: the ten Ready entries are done`。

## Coverage

| promise | task |
|---|---|
| `docs/collisions.md:140`、`:143`、`:157` 與 `docs/documents.md:326` 各在 citation **之後**、同一行內補一個反引號引文，內容取自被引那一行的原文。 | Task 1 |
| 引文與 citation 不得被硬換行拆開；補完後段落要重排時，換行點落在它們之外。 | Task 1 |
| `TODO.md` 裡另有 5 條同類。4 條就在本任務的 Ready 條目裡，隨條目刪除消失；第 5 條在 `## Waiting`，不動。 | Task 9 |
| `docs/pipeline.md:695` 的 `E2 --> F` 改成 `E2 --> F2`。 | Task 2 |
| E2 的標籤（`:674`）補上 uncovered directories：`unfiled · undeclared ·<br/>linked from nowhere ·<br/>directories no page names`。 | Task 2 |
| class 那張圖（`:394`、`:418`）不動。 | Task 2 |
| `tests/docs-audit.test.js:532` 那支測試對兩頁各跑一次同一組斷言：`skills/fankeel/SKILL.md` 與 `skills/fankeel-audit/SKILL.md`。 | Task 3 |
| 每頁一張自己的 `ROW` 對照與自己的錨點，因為兩頁措辭不同。 | Task 3 |
| 兩張表今天都列全了十個類別，不改表。新測今天就綠，它證明的是守衛的範圍：刪掉第二頁任一列會變紅，今天同樣的刪除仍然綠。 | Task 3 |
| `skills/fankeel/SKILL.md:7` 的 `source_of_truth` 加上 `docs/collisions.md, docs/registry.md, docs/station.md, docs/statusline.md`。 | Task 4 |
| `docs/documents.md` 不加：它自己的 `source_of_truth`（`docs/documents.md:4`）已經宣告 SKILL.md，`defers()` 兩個方向都認（`scripts/docs-audit.js:448`）。 | Task 4 |
| `.md` 條目不會變成 drift 的主題（`scripts/docs-audit.js:271-274`），所以這個改動不會多出 drift。 | Task 4 |
| 另外四組（`docs/improvement-brief.md`、`docs/pipeline.md`、`skills/fankeel-survey/SKILL.md`、`docs/subagents.md`）不是延後關係，不宣告，照舊留在 pairs。 | Task 4 |
| `sweep()` 在跑 landed 之前，用與 `dateSource`（`scripts/docs-audit.js:113`）同一種呼叫方式取一次 git 的刪檔紀錄 | Task 5 |
| landed 的判斷（`scripts/docs-audit.js:524`）改成：`unbuilt` 扣掉這個 Set 之後仍非空才跳過。 | Task 5 |
| 不在 git 裡的專案、或 git 呼叫失敗，一律得到空 Set，行為與今天相同。 | Task 5 |
| 取捨：一個被刪掉、之後又被新 plan 計畫重建的檔，會被當成已刪而不是未建，那份 plan 可能提早被提議歸檔。 | Task 5 |
| `pagesByStatus()`（`lib/map.js:233`）在讀 frontmatter 之前，把 role 為 `archive` 的頁直接放進 `retired`，寫法比照 `:263` 的 fixture 分支。 | Task 6 |
| `buildMap()` 的 retired 段改成：每個 archive 桶印一行（`docs/archive — 59, the whole archive bucket`），只有不在 archive 桶裡的 retired 頁才逐頁列出（今天 0 頁）。 | Task 6 |
| `documents:` 那一行補上每個狀態的計數，加總等於總數。只有在沒有任何清單被截斷時，它才多印一句「a page named nowhere below is current」；有清單被截斷時這句不成立，所以不印。 | Task 6 |
| 文件跟著改：`skills/fankeel-survey/SKILL.md:95-104`（map 持有什麼、retired 段）、`docs/pipeline.md:798-802`（map 範例）、`docs/documents.md` 講 `pagesByStatus` 的那段。 | Task 6 |
| `touch()`（`lib/registry.js:430`）在寫 `clock` 的同一處，當 `moves` 最後一筆的 stage 不等於 `data.stage` 時，追加 `[data.stage, at]`。 | Task 7 |
| 上限 `MAX_MOVES = 60`，超過時丟最舊的，寫法與 `MAX_CLAIMS`（`lib/registry.js:640`）相同。 | Task 7 |
| `task.js task`（改名）把 `moves` 加進刪除清單（`scripts/task.js:655-668`），理由同 `clock`：stage 名稱會再出現。 | Task 7 |
| `task.js adopt` 把每一筆時間平移 `at − Date.parse(source.updated)`，保留順序與間隔，去掉來源閒置的那一段，理由同 `clock` 的「距離，新原點」（`scripts/task.js:867-871`）。 | Task 7 |
| 既有欄位一個都不動，station 也不讀 `moves`；怎麼畫是 `## Needs a decision` 那條〔station〕的事。 | Task 7 |
| 文件跟著改：`docs/registry.md` 的 What a stage cost 加一段 `moves`；`skills/fankeel/SKILL.md:111` 的 `Ten more` 改 `Eleven more`、「the six below」改 seven，`:138` 的 `An eleventh` 改 `A twelfth`。 | Task 7 |
| `record()`（`scripts/judge.js:68`）讀完答案後，`answer.trim()` 為空就 `fail(...)` 並 exit 1，而且在 `mkdirSync` 之前發生：不建目錄、不寫檔、不動索引。 | Task 8 |
| 錯誤訊息指出答案該去哪裡找：背景 judge 的答案在 `<session>/subagents/agent-<id>.jsonl` 的最後一則 assistant 訊息；`tasks/<id>.output` 是 0 bytes（`docs/decisions/2026-09-11-todo-split.md:37-43`）。 | Task 8 |
| `skills/fankeel-ask/SKILL.md` 第 3 步加上同一句。 | Task 8 |
| 主張 | struck — 這是 design 證據表的表頭，不是一條承諾 |
| 四條引文有人檢查 | Task 1 |
| 稽核圖的 E2 走 F2，且有 uncovered | Task 2 |
| 第二頁的表也被守著 | Task 3 |
| SKILL.md 不再和延後的四頁成對 | Task 4 |
| 刪掉的檔不擋 landed | Task 5 |
| archive 頁不進 planned | Task 6 |
| map 的每一頁都有狀態 | Task 6 |
| `moves` 記下轉換 | Task 7 |
| 空答案被拒 | Task 8 |
| 沒弄壞既有的東西 | Task 9 |
