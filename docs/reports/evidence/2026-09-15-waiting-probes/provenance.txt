# provenance — 2026-09-15 waiting probes

date:        2026-09-15T06:38:24Z
HEAD:        1ab583061fa651e63df9df097818c61091547292
branch:      main
claude:      2.1.272 (Claude Code)
node:        v24.9.0
os:          MINGW64_NT-10.0-26200 3.4.10-87d57229.x86_64

## git status --porcelain at the time of the runs
   M TODO.md
  ?? docs/reports/evidence/2026-09-15-waiting-probes/

## what is here

probe-deny.sh / grade-deny.js
  permissions.deny under two permission modes. out-{auto,bypassPermissions}-{nodeny,deny}.jsonl
  are its four cells. The nodeny pair is the control.

probe-dispatch.sh / grade-dispatch.js
  which flag and which spelling gates a dispatch. dispatch-out-{agent,task,none,dis-agent,dis-task}.jsonl
  are its five arms. dis-agent is the control.
  Rounds 1 and 2 of this probe both failed their control and are not kept: round 1 ran under
  bypassPermissions, where --allowedTools is a no-op by definition, and round 2 under
  manual + --permission-prompts none, where it is not and the dispatch happened anyway.
  Both are why the arms moved to --disallowedTools.

probe-link.sh / probe-link-sonnet.sh / grade-link.js
  whether a model follows the relative link out of a SKILL.md. link-control-{1,2}.jsonl,
  link-linked-{1,2}.jsonl (haiku), link-sonnet-{1,2}.jsonl (sonnet).
  The control needle is a word only in SKILL.md; the question needle is a word only in
  rationale.md.

  grade-link.js counts text blocks from assistant messages ONLY. Its first version counted
  every text block, and the injected SKILL.md body is a `user` text block carrying the control
  needle -- so control/1 passed on the injection rather than on the model's answer, and flipped
  to a miss when that was fixed. The control still holds, on control/2.

  These six transcripts are kept whole, at about 44K each, against a convention where the
  largest previous evidence directory is 141K. The 26K `user` text block in each is the
  fankeel-build SKILL.md body, and it is not duplication that can be elided: it is the
  evidence that the skill body arrived in the context WITHOUT any file being read, which is
  the whole of what the control proves. A re-grade has to run against exactly what the model
  saw.

eval-route-typo.{json,txt} / eval-stage-skip-said.{json,txt}
  five runs each, per the two TODO entries asking for n=5 on one commit.
  A first attempt at both died in scripts/eval.js:133, `fs.rmSync` of the scaffold temp
  directory, EPERM on Windows; --keep-temp skips that line and is what the kept runs used.

## one elision, named

eval-route-typo.json and eval-stage-skip-said.json had their per-run `raw` key removed
before committing: it held the whole stream-json transcript, about 520K across the two
files, against a convention where the largest previous evidence directory is 141K. Each
run now carries a `rawElided` string saying how many lines went and the exact command
that produced the run. What is kept is everything the report cites -- `graders` with the
regex or tool predicate behind each verdict, `cost` per run, `toolCalls`, and
`lastMessage`, which is the exact text the regex graders matched against, so the failing
grader's own input is still here. A re-run makes different transcripts, so those lines
are gone rather than reproducible; that is the cost of the elision and it is why it is
written down here rather than left for a reader to notice.

## what the review changed, and what it did not

A fankeel-reviewer read 1ab5830..eaf5844 and found six things. Four were fixed in the
follow-up commit; two were ruled on and left alone.

Fixed:
  - the probe cost row said $0.3, then $0.7670, and is $0.6763. The first was an estimate
    presented in a table where every other row was measured. The second summed every
    `result` line, and dispatch-out-{agent,none,task}.jsonl each log the same
    total_cost_usd twice, so it double-counted $0.0906. The figure is now the max per
    file. The reviewer caught the second error; the first was caught here before it did.
  - grade-dispatch.js collected its `refusal` text from every message role, the same
    defect grade-link.js was fixed for. Re-graded against the saved transcripts: every
    verdict unchanged, because both NO DISPATCH TOOL matches were in real assistant
    blocks. A hardening, not a correction -- but the report quotes that field.
  - the report said four link runs "never opened the file", which undersold what
    link-sonnet-1.jsonl shows: the model grepped, found rationale.md holds the needle,
    said the skill links it, and still would not treat it as material it had been given.
  - the report promised a TODO entry for each of the last three falsified rows and filed
    two. The tests/skills.test.js row has no decision of its own and now says so.

Ruled on, not fixed:
  - Two TODO entries in eaf5844 -- the todo-check threshold one and the main-agent cost
    one -- are outside the five-probe brief. They are there because the user asked for
    them mid-task, in as many words. The reviewer could not see that and was right to
    flag them. What was wrong is the commit message: it says "Needs a decision 9 到 12"
    where the commit takes that section from 7 to 12, because those two are in the same
    commit.
  - The cuts, both in this directory: a single-call-site `blocks()` helper in
    grade-deny.js, and probe-link-sonnet.sh duplicating probe-link.sh's one invocation.
    Declined. These scripts are the evidence of how the runs were made; refactoring them
    afterwards leaves a committed script that is not the one that produced the
    transcripts beside it. The cuts are real and would be right in source. Not here.

## round two

A second fankeel-reviewer read 1ab5830..a78df99 with one named target: whether each of the
five deleted `## Waiting` entries corresponds to a question the evidence actually answers.

It found all five justified, and made the argument the report does not: two of the five had
events that the probes did not satisfy as worded -- the --allowedTools entry's event
presupposed that one spelling opens the tool, and the rationale-link entry's event was
phrased as "confirms the link IS followed". Neither literal proposition came true. Deleting
both was still right, because `## Waiting` means waiting on something that is not a person.
Once a probe returns a firm mechanism -- including one that falsifies the entry's own
premise -- the thing being waited on is a person's choice among named options, which is
`## Needs a decision`. Rewording and keeping them in Waiting would have left settled
questions labelled as pending.

Its one defect finding: $2.8749 for stage-skip-said should be $2.8748. The five costUsd
values sum to 2.8747925; the report had summed the five four-decimal figures the .txt
prints instead of the raw ones. Fixed, along with the total ($5.1716 -> $5.1714) and an
internal inconsistency it did not catch: the prose still said $6.5 and "超支 $2.5" after the
table had been corrected to $6.4.

The reviewer's own correction repeated the same mistake in the other direction -- it gave
the total as $5.1715, which is 1.6204 + 2.8748 + 0.6763, three already-rounded numbers. The
raw sum is $5.1714. Both errors have one cause, and the report now states the method next to
the table: every figure comes from cost.costUsd and total_cost_usd, never from the .txt.

Not re-run for this round: npm test. The round-two fix changes one markdown file and this
one; no source under test was touched, and the suite was green twice earlier on this branch
(1475/1475, both times).

Round three -- what it checked, and what it found
-------------------------------------------------

One fankeel-reviewer over 1ab5830..40cd3fc, sent at exactly what round two said it did not
open: the three probe tables checked cell by cell against the raw transcripts. Round two
had read only the two eval JSONs, so the five rows of section 2 had never been verified
against their transcripts by anyone.

Sections 1, 2 and 3 all held, cell for cell. Two things it added that the report did not
claim: dis-task also says NO DISPATCH TOOL verbatim (the table quotes the phrase only on
the dis-agent row, so that row is under-credited rather than wrong), and control/1 misses
the sentence because its copy of the needle is wrapped as **predating**, which breaks the
contiguous substring grade-link.js looks for -- so the 1 of 2 is a property of the answer,
not of the grader.

Two defects, both in the report and both fixed here.

First, the quoted Grep tool_result was not verbatim. The transcript records

    Found 1 file
    skills\fankeel-build\rationale.md

and the report had printed "skills" joined to "ankeel-build", then "ationale.md" on the
next line: the backslashes of the Windows path were eaten as escapes when the quote was
first written, \f becoming a form feed and \r a newline, both of them real control
characters in the file. The claim the quote supports was never wrong -- the real text does
name that file -- but section 3 promises a verbatim record, and this was the one place the
page did not keep it. The two lines now sit in a fenced block, and the tool_use input is
quoted as its pattern and path values rather than as a JSON blob, because the JSON on disk
doubles every backslash and the decoded value is what the model actually searched. Both are
now checked against the transcript by string comparison rather than by eye, and no form
feed or carriage return survives anywhere in the page.

Second, the needle-uniqueness sentence was self-falsifying. It claimed predating occurs
only in SKILL.md and triples only in rationale.md within skills/, docs/ and lib/ -- true of
the tree the probe ran on, and false of the commit the sentence was read at, because the
report and its evidence directory both live under docs/ and both carry the words. The
sentence now pins the tree:

    git grep -n -E "predating|triples" 1ab5830 -- skills/ docs/ lib/

returns exactly one line each, and the page says so, along with the fact that its own
landing ended that uniqueness. The experiment never depended on the repository-wide
property, only on each needle being unique between the injected SKILL.md and rationale.md,
which is unchanged.

Not re-run for this round: npm test. This round changes two markdown files and this one; no
source under test was touched, and the suite was green twice earlier on this branch
(1475/1475, both times).
