---
status: current
last_verified: 2026-09-13
source_of_truth: docs/reports/*.md — each row transcribes that report's headline figure and its own frontmatter's provenance; this page is the index onto them, not a re-measurement. The reverse index (Cited by) is filled by hand from grep, not a script.
---

# Sources

The evidence ledger. `docs/improvement-brief.md` §2.4 D1 described SEPIA's
own evidence ledger — one stable ID per measurement, seven columns, and a
reverse-index column so that changing a figure says which pages have to
change with it. This page is that mechanism for fankeel's own dated reports.

Every row below is one of the dated reports at the top level of
`docs/reports/`. Twenty-five sit there and twenty-five have a row; the heading
counts rows across both tables, not files. A row is still added by hand, and
`tests/sources-doc.test.js` fails while a report there has none — the heading
is the one number left to keep in step by hand.
The files under `docs/reports/evidence/` are the raw evidence behind a
report, and what that is varies with the measurement: mostly
`claude -p --output-format json` output beside the shell script that
produced it, with one directory carrying a diff and the newest holding node
probes that no longer run: `2026-09-12-intent-plan-signal/` names two control
plans by their `docs/plans/` paths, and both were archived on 2026-09-13. None of these directories are reports
of their own, so they carry no row — a row's
Link column points at the report, and the report itself points at its
evidence directory when it has one. Two of the directories there stand behind no report at all:
`2026-09-08-route-typo` behind an eval case and `2026-09-10-exception-cases`
behind a judgement, so this table does not reach either one from either end.

**Read the Scope column before quoting a figure.** It says what the
measurement does not cover, which is what stops one row from reading as
contradicting another — the clearest case is the three-way split on
dispatch-vs-inline residue (9.2× / 2.55× / 1.5×), which is one gradient, not
three disagreeing numbers, once each row's Scope says which variable it held
fixed.

## The twenty-five reports

| ID | What it measured | Link | Checked | Evidence level | Scope | Cited by |
|---|---|---|---|---|---|---|
| `PROCESS-REVIEW-260902` | How well the seven-stage process design holds up: which rules are mechanically enforced and which are prose-only | [reports/2026-09-02-process-state-review.md](reports/2026-09-02-process-state-review.md) | 2026-09-02 | review (174 agents, sonnet readers, opus-adjudicated) | 97 raw findings from 18 sonnet readers, reduced to 63 survivors by opus rebuttal. The two heaviest survivors sit outside this repository's own boundary (a neighbour project's registry, a post-clone ledger) and would not have surfaced from thirteen days of dogfooding inside this repo alone. | `docs/archive/2026-09-04-ready-and-decisions.md`, `docs/README.md`, `docs/registry.md`, `skills/fankeel-build/SKILL.md` |
| `DISPATCH-VS-INLINE-260903` | Dispatching four `sonnet` readers vs reading the same files in-session, on fankeel's own `lib/stages.js` plus its seven stage skills | [reports/2026-09-03-dispatch-vs-inline.md](reports/2026-09-03-dispatch-vs-inline.md) | 2026-09-03 | measured (paired A/B, `claude -p --output-format json`, n=1 per arm) | 9.2× main-thread residue win (57,652 vs 532,322 tokens) costs 1.85× money, 4.7× total tokens, 1.75× wall-clock. The target files were **not** named in the prompt — only a glob — so this figure bundles the cost of searching with the cost of reading. See `DISPATCH-NAMED-260903` (1.5×) for reading-cost with search removed before quoting either number as the general price of dispatch. | `docs/archive/2026-09-04-ready-and-decisions.md`, `docs/improvement-brief.md`, `docs/archive/2026-09-07-todo-thirteen.md`, `docs/README.md`, `docs/reports/2026-09-03-dispatch-vs-inline-named.md`, `docs/reports/2026-09-04-agent-wakeups.md`, `docs/reports/2026-09-05-stage-division-measurements.md`, `docs/subagents.md`, `skills/fankeel/SKILL.md`, `docs/reports/2026-09-18-seventeen-items.md` |
| `DISPATCH-NAMED-260903` | The same question and the same repository, but the seven target files are named in the prompt so neither arm has to search for them | [reports/2026-09-03-dispatch-vs-inline-named.md](reports/2026-09-03-dispatch-vs-inline-named.md) | 2026-09-03 | measured (paired A/B, n=1 per arm) | Residue advantage falls from 9.2× to 1.5× (74,603 vs 113,518 tokens) once search is removed — most of the first pair's win was search, not reading. Money (1.59×) and wall-clock (2.77×, worse than the first pair) do not shrink with it, so this does not show dispatch getting cheaper, only residue narrowing. | `docs/archive/2026-09-04-ready-and-decisions.md`, `docs/archive/2026-09-07-todo-thirteen-design.md`, `docs/archive/2026-09-07-todo-thirteen.md`, `docs/subagents.md`, `skills/fankeel/SKILL.md`, `docs/README.md` |
| `DISPATCH-JOIN3-260903` | The point between the first two pairs: eight files named individually, but the cross-file join question kept | [reports/2026-09-03-dispatch-vs-inline-join.md](reports/2026-09-03-dispatch-vs-inline-join.md) | 2026-09-03 | measured (paired A/B, n=1 per arm, pre-registered prediction) | 62,937 vs 160,728 tokens, 2.55× — below the report's own pre-registered 3.0× threshold. This refutes the report writer's own prior bet toward "join depth decides it" and instead confirms "whether you have to search decides it" (`docs/subagents.md`'s original claim). Same single commit as the other three dispatch-vs-inline pairs; n=1. | `docs/archive/2026-09-04-ready-and-decisions.md`, `docs/README.md`, `docs/subagents.md`, `skills/fankeel/SKILL.md` |
| `AGENT-WAKEUPS-260904` | What a dispatch wakes the parent session on, and whether foreground vs background subagents differ | [reports/2026-09-04-agent-wakeups.md](reports/2026-09-04-agent-wakeups.md) | 2026-09-04 | measured (host session logs, 2 sessions, no repeated trials) | Found the foreground/background contrast does not exist for the `Agent` tool at all — 31 of 31 calls in one session were async. The wakeup-follows-dispatch-count table (4 agents→4 wakeups, 5→1, 8→1) is three single observations, one fan-out each, not a repeated series. | `docs/archive/2026-09-04-ready-and-decisions.md`, `docs/README.md`, `docs/subagents.md` |
| `CHAINS-AS-WORKFLOWS-260904` | What a build chain (implementer→reviewer, two tasks) and a verify chain (verifier→adversary ×3 plus parallel doc readers) cost run as one `Workflow` | [reports/2026-09-04-chains-as-workflows.md](reports/2026-09-04-chains-as-workflows.md) | 2026-09-04 | measured (host-recorded usage, single run per chain, no control arm) | Build: 4 agents, 319,526 subagent tokens, 814,181 ms, 135 tool calls. Verify: 10 agents, 697,949 tokens, 482,484 ms, 180 tool calls. Each chain ran once — no arm ran the same chain as four separate `Agent` dispatches instead, so these numbers are "what it cost," not "cheaper than." | `docs/archive/2026-09-04-ready-and-decisions.md`, `docs/archive/2026-09-07-backlog-eight.md`, `docs/subagents.md`, `skills/fankeel/SKILL.md`, `skills/fankeel-build/rationale.md`, `docs/README.md`, `docs/judgements/2026-09-10-verifier-agent.md` |
| `BRIEF-PROBE-260904` | Whether the `SubagentStart` hook's brief actually reaches a subagent's context, across three dispatch surfaces | [reports/2026-09-04-subagent-brief-probe.md](reports/2026-09-04-subagent-brief-probe.md) | 2026-09-04 | measured (probe instrumentation, `tool_uses` as the control) | Cell A (a custom `brief-probe` agent type) never launched — "Agent type not found," the fixture was built minutes too late for that process. Only cell B (`Explore`, n=1) and cell C (`Workflow` `agent()`, n=2) produced readings, and those two are byte-identical. Output-style delivery is only half-settled: no style text appeared in the brief, but whether Claude Code forwards a configured style into a subagent's own system prompt is untested here — see `STYLE-TO-SUBAGENT-260907`. | `docs/reports/2026-09-07-brief-probe.md`, `docs/subagents.md`, `docs/README.md` |
| `FIELD-REPORT-044-260905` | A full run on plugin 0.44.0, reported by another session (`084994f1`, `F:/ymlab/Latex/production`): sessions never stood down, registries the station cannot see, leaked test directories, tool defects | [reports/2026-09-05-field-report-0.44.0.md](reports/2026-09-05-field-report-0.44.0.md) | 2026-09-05 | field report (external session, self-reported, quoted verbatim) | Search bounded to `C:\Users\Owner` and `F:\ymlab` at `-maxdepth 6`; other drives and deeper levels were not scanned, so eleven sessions, six registries and 297,088 test directories are a lower bound, not a census. Measured on 0.44.0, several point-releases before this ledger. | `docs/decisions/fankeel-shell.md`, `docs/archive/2026-09-05-station-at-hand-design.md`, `docs/archive/2026-09-05-station-at-hand.md`, `docs/README.md` |
| `STAGE-DIVISION-MEAS-260905` | Where four figures quoted with no citation in the stage-division design and in the `fankeel-build` skill actually came from | [reports/2026-09-05-stage-division-measurements.md](reports/2026-09-05-stage-division-measurements.md) | 2026-09-05 | measured (mixed provenance: one reproducible probe, three per-machine records) | Only the probe (21 checks, 20 `no`) is reproducible from this repository. The other three — 7 agents for one return on a group of three; 1,169,842 subagent tokens over two runs, about 37 minutes; eleven alternative rewrites of one parenthetical — come from Claude Code's own workflow run records under the session directory, which is per-machine and not version-controlled: checkable today on the machine that ran them, and nowhere else, ever again. | `docs/archive/2026-09-05-anchor-remaining.md`, `docs/archive/2026-09-05-anchor-tiers.md`, `docs/decisions/2026-09-04-stage-division-design.md`, `docs/decisions/2026-09-05-anchor-remaining-design.md`, `docs/decisions/2026-09-05-anchor-tiers-design.md`, `docs/archive/2026-09-08-ready-and-station-serve.md`, `docs/README.md` |
| `AUDIT-CONSTANTS-260907` | Re-measures whether `LANDED_QUIET = 3` and `LANDMARK` still hold, against plans landed since the constants were set | [reports/2026-09-07-audit-constants.md](reports/2026-09-07-audit-constants.md) | 2026-09-07 | measured (script re-run against the current tree) | `LANDED_QUIET`: the same 0–4 day range and mode of 3 across six newly-landed plans, with one method trap on record — `git log -1` on an archived plan returns the archiving commit, collapsing every gap to 0 unless read from the path's second-newest commit. `LANDMARK`: 102 pairs with the constant disabled vs 31 at the current threshold; of 8 pairs sampled by hand, 0 held a factual contradiction and 3 only restated a neighbouring page — not a census of all 102. | `docs/archive/2026-09-07-backlog-eight-design.md`, `docs/archive/2026-09-07-backlog-eight.md`, `docs/README.md`, `docs/reports/2026-09-12-intent-plan-signal.md` |
| `BRIEF-PROBE-260907` | The `brief-probe` fixture's first successful run, after 2026-09-04 could not launch it — confirms the current task/stage and the map rule reach a subagent, and reports the `workflow-subagent` agent type | [reports/2026-09-07-brief-probe.md](reports/2026-09-07-brief-probe.md) | 2026-09-07 | measured (probe instrumentation, `tool_uses: 0` as the control) | One run, four lines returned, the harness's own `tool_uses: 0` confirms the fixture's no-tool-calls precondition held. Settles that `agent_type` is written into the brief, but not whether two different agent types receive different brief content (still open). One line is noted as contradicting what the harness's own agent listing said about tool access — flagged here, not explained. | `docs/archive/2026-09-07-ready-fourteen-design.md`, `docs/archive/2026-09-07-ready-fourteen.md`, `docs/README.md` |
| `DISPATCH-JOIN-DIFF-260907` | One reader per page plus a shared diff path, against the same reading in-session — the fourth dispatch-vs-inline pair | [reports/2026-09-07-join-pair.md](reports/2026-09-07-join-pair.md) | 2026-09-07 | measured (paired A/B, n=1 per arm, run in a pinned worktree) | 45,111 vs 517,009 tokens, 11.46× — the largest of the four pairs, and the only one where dispatch is both cheaper (0.95×) and faster (0.56×). It moves two variables at once relative to every earlier pair (one-reader-one-page instead of four-readers-by-lens, **and** an extra diff source per reader), so 11.46× cannot be attributed to either variable alone — stated in the report itself, not left for a reader to find. | `docs/archive/2026-09-07-todo-thirteen.md`, `docs/README.md`, `docs/judgements/2026-09-10-shell-whitelist.md` |
| `HAIKU-PAIR-260909` | The first pair's question and method sentences re-run with `haiku` as the parent model on both arms, the four readers still `sonnet` | [reports/2026-09-09-haiku-pair.md](reports/2026-09-09-haiku-pair.md) | 2026-09-09 | measured (paired A/B, n=1 per arm) | 39,040 vs 133,423 tokens, 3.42× — the residue advantage falls because the *inline* arm got cheaper (532,322 on opus to 133,423 on haiku), not because dispatch improved. Money moves the other way, from 1.85× to 4.61×, because the readers are pinned at `sonnet` and so take 89% of the dispatch arm's total against 63% with an opus parent. Wall-clock is a wash (149 vs 153 shell seconds) and `duration_ms` again under-reports the fan-out by 7.1×. Not a strict replication: pinned at a different HEAD (`502dcae` vs `86a104e`) and CLI (2.1.265 vs 2.1.259), so only the within-pair ratios compare. Answer quality is not equal — the dispatch arm returned 19 anchors with no directory on any of them, against 11 fully-pathed from the inline arm — but neither arm's findings were checked for correctness. | `docs/README.md` |
| `REVIEWER-COST-260907` | Whether a reviewer per task and a mutation per fix earned their cost, over one real ten-task build | [reports/2026-09-07-reviewer-cost.md](reports/2026-09-07-reviewer-cost.md) | 2026-09-07 | build record (task ledger and commit history, not a controlled trial) | 10 tasks, 10 reviewer dispatches, 26 rulings, 13 commits. Reviewers caught 5 real findings across 3 tasks, all while the test suite stayed green. The suite in turn caught 3 things reviewers missed (two cross-file test failures, one orphan export), because each implementer ran only their own test file, not the full suite. The one mutation run (n=1) proved a reviewer-found gap rather than discovering it itself, and its cheapness (three commands, a few seconds) is not claimed to generalize past this one fix. | `docs/archive/2026-09-07-todo-thirteen.md`, `docs/README.md` |
| `DESIGN-AXIS-260909` | What sixteen design axes six installed design skills each set, where the six vocabularies conflict, and the four gaps between them | [reports/2026-09-09-design-axis-inventory.md](reports/2026-09-09-design-axis-inventory.md) | 2026-09-09 | inventory, read in verbatim from caveman.zip — not measured in this repository | **The subject is this machine, not this repository.** The scan covered 68 design-related skills under `~/.claude/plugins/cache/`, across four marketplaces, and the text is copied without rewriting or translation — so the finding does not move when this repository's code changes, and it does not re-derive on a machine with a different set of plugins installed. Its numbers describe an installed-plugin population of one machine on one day; nothing here is a claim about fankeel. | `docs/README.md`, `docs/improvement-brief.md`, `docs/judgements/2026-09-11-todo-split.md` |
| `PROFILE-EVIDENCE-260909` | Which development-preference answers a profile could actually be built from today: what the registry records hold, and where land's answer lives instead | [reports/2026-09-09-profile-evidence.md](reports/2026-09-09-profile-evidence.md) | 2026-09-09 | measured (field census over 106 registry records, plus git history; the records are per-machine and not version-controlled) | 106 session records at one registry path, and the second known path does not exist. Other registries on this machine were not enumerated, so 106 is a lower bound and not a census — the report says so itself. The count includes this session's own live entry, still being written when it was taken. The merge figure is 49, after a first pass reported 10 from a `head`-truncated run; that trap is recorded in the report rather than only in the fix. | `docs/README.md`, `docs/improvement-brief.md` |
| `HOOK-PAYLOAD-260911` | Whether a main-session `PostToolUse` payload carries `tool_response`, and whether a hook fired inside a subagent carries `agent_type` | [reports/2026-09-11-hook-payload-probe.md](reports/2026-09-11-hook-payload-probe.md) | 2026-09-11 | measured (one headless `claude -p` turn with a one-off probe hook, n=1) | One run, with neither field rerun to check it holds. No `PowerShell` tool call fired, so the `PowerShell` half of `hooks/guard.js`'s `Bash|PowerShell` matcher went unobserved. Only one `fankeel` was loaded under `--setting-sources project`, so whether a user-config plugin changes `agent_type`'s prefix is unmeasured. | `docs/collisions.md`, `docs/archive/2026-09-11-backlog-all.md`, `docs/README.md`, `lib/guard.js` |
| `INTENT-SIGNAL-260912` | Whether either of two candidate signals can tell a `design-intent` plan whose work has landed apart from one that is merely waiting, for both `docs-audit`'s `landed` check and `lib/map.js`'s planned-not-built list | [reports/2026-09-12-intent-plan-signal.md](reports/2026-09-12-intent-plan-signal.md) | 2026-09-12 | measured — three scripted probes over the current tree, one subject plan and two landed controls | n=1 in the strongest sense: the repository holds exactly one live `design-intent` plan, so the subject is a single page and the two controls both come from the same day and the same branch. The direct evidence that removing the `kind === 'intent'` skip would report the subject as landed is historical, and it names the same page, not just the same class: commit `43a7c12`'s message records that before the skip existed, the real sweep reported `docs/plans/2026-09-09-design-class-prompt.md` — this measurement's own subject — as landed and made the whole run exit 1 every time. This measurement did not repeat that mutation; instead it re-confirmed the three conditions individually — conditions (i) and (ii), named files exist and none unbuilt, by `points.js`, and condition (iii), the three-day settle window, by hand from `git log`: the subject's only commit is 2026-09-09 and the measurement was taken on 2026-09-12, exactly the three days `LANDED_QUIET` requires. No script in the evidence directory checks the settle condition, so that figure is hand-derived, and sitting exactly on the boundary makes it clock-dependent — one day earlier it would not have passed. The reason the second signal fails is specific to this repository: `lib/stages.js` and `scripts/task.js` change constantly for unrelated reasons, so any plan naming them reads as changed-after-filing — a repository without such churn-heavy shared files might not behave this way. `lib/map.js`'s half was not probed separately — the same pre-filter (`plan` role on the docs-audit side, `archive` role excluded on the map side) narrows both consumers to this same one page, so the argument above settles it rather than a second run. The evidence scripts stopped re-running on 2026-09-13: `points.js` and `ages.js` name the two control plans by their `docs/plans/` paths and both were archived that day. Neither the scripts nor the report was changed — what they measured was the state of the tree on 2026-09-12, and repointing them would make `points.js` print `role: archive` where the published table says `plan`. | `docs/README.md`, `skills/fankeel-audit/rationale.md` |
| `WAITING-PROBES-260915` | Whether the five `## Waiting` entries of 2026-09-15 could lift — each `lifts when:` named a run fankeel had to do itself, not an event in the world — by doing each run: three scripted probes and two eval re-runs | [reports/2026-09-15-waiting-probes.md](reports/2026-09-15-waiting-probes.md) | 2026-09-15 | measured — three scripted probes with a control arm each, and two evals re-run five times on one commit; `provenance.txt` pins the HEAD sha and `claude --version` | Each of the five is a small run on one commit, not a series. Two of the three control arms caught a wrong conclusion before it was written: the dispatch probe's first round ran under `bypassPermissions`, where `--allowedTools` is a no-op, and the link probe's first grader matched the injected `SKILL.md` body rather than the model's answer. The `auto` half of the `permissions.deny` probe tested the `--permission-mode auto` flag, not the `defaultMode: "auto"` settings key the page asked about. The link probe covers an invoked skill, whose body arrives injected; a reader handed the `SKILL.md` path and left to open it was not run. `route-typo`'s five greens say it did not flip on that commit, that model, that day — the flips it was filed for were measured on two different commits. | `docs/README.md`, `docs/archive/2026-09-17-needs-decision-all.md`, `docs/archive/2026-09-18-ready-five-design.md`, `docs/archive/2026-09-18-ready-five.md`, `docs/collisions.md`, `docs/decisions/2026-09-05-skill-split-design.md`, `docs/decisions/2026-09-10-todo-ten.md`, `docs/decisions/2026-09-11-todo-eight.md`, `docs/decisions/fankeel-shell.md` |
| `SEVENTEEN-ITEMS-260918` | Where each of the user's seventeen 2026-09-18 afternoon suggestions stands against the code, and whether a Sonnet main session that only dispatches would cut the cost of context stacking | [reports/2026-09-18-seventeen-items.md](reports/2026-09-18-seventeen-items.md) | 2026-09-18 | review — no new measurement: status read off the code by four `sonnet` readers and spot-checked in-session; stacking figures re-quoted from `docs/improvement-brief.md` §6.2 and the 09-18 decision record, dispatch cost from `DISPATCH-VS-INLINE-260903` (1.85×) and `DISPATCH-NAMED-260903` (1.59×), quoted together as a range, per-turn re-read cost computed from the claude-api skill's price list cached 2026-06-24 | Nothing in the evaluation was run. The Sonnet 5 cache-read price (~$0.20/M) is derived from the ~0.1× rule, not listed; how a subscription quota converts is not public, so the per-turn table compares models, not bills. The three-step recommendation is argued from where the tokens land, not measured against a control | `docs/README.md` |
| `SURVEY-BRAIN-AB-260920` | Whether survey run by a Sonnet controller and an Opus stage agent (`stage.agents` true) works end to end, and what it costs against the Opus main session doing it | [reports/2026-09-20-survey-brain-ab.md](reports/2026-09-20-survey-brain-ab.md) | 2026-09-20 | measured — two interactive runs checked against their transcripts, four headless arms alternating old, new, old, new (`claude -p --output-format json`, n=2 per mode), and a control batch of four alternating old with a new mode whose stage agent has no `Agent` tool (`ab2.sh`), run again after each of two changes to the stage agent's brief (`ab3.sh`, `ab4.sh`); `ab/`, `ab2/`, `ab3/` and `ab4/provenance.txt` pin the HEAD sha and `claude --version` | With four readers dispatched the new mode was 9.0–10.5× slower and 4.0–5.6× dearer; with none it was still 6.5–9.0× slower and 2.5–2.8× dearer. The controller's context at the gate was larger in the first batch (69,840 and 81,675 against 58,420 and 66,902) and 1,093–8,529 smaller in the control, where it cost $0.23–0.25; the rest is the stage agent, which made 34 and 48 tool calls where the old main session made 5 and 6. Told to batch its commands (`577f3e1`) it made 2 and 6 Bash calls instead of 31 and 20, but still 30 and 28 tool calls in all, opening each cited location with its own Read, and stayed 2.2–2.8× dearer. Told to read those lines with `sed -n` in Bash (`357924c`) it made 11 and 18 tool calls and was still 2.6–2.8× dearer: the stage agent alone cost more than the whole old session, with 3.6–4.7× its output tokens. Split by a fit over 208 messages (R² 0.984, thinking estimated from signature length), over half of the stage agent's output was thinking, 2.5–4.0× the old session's, and its handoff Write alone outweighed the old session's whole visible output. At `effort: medium` (`d3d6913`) its thinking fell to 3,386 on the one valid pair of the fifth batch — two of that batch's controllers stopped to ask about the measurement's own plugin path instead of dispatching — but prices backed out of `modelUsage` put output at 26–31% of the stage agent's cost, the rest cache read and cache write for a second context. After a fourth change (`0e32d9a`, the output rule's word count bound to the handoff — which did not shrink it) the last batch's within-pair cost was 1.05× and 0.97× with time 1.44× and 1.57×; the old mode grew dearer over the same batches as the scanned tree went from 566 to 656 files, so only within-pair figures compare. `-p` has no AskUserQuestion, so the verbatim-gate check rests on the interactive run alone | `docs/README.md`, `docs/decisions/2026-09-20-survey-brain.md`, `docs/reports/2026-09-21-long-task-projection.md` |
| `LONG-TASK-COST-260920` | Whether a task's cost composition moves with its length, and so whether a survey-only A/B can be carried over to a seven-stage task | [reports/2026-09-20-long-task-cost-composition.md](reports/2026-09-20-long-task-cost-composition.md) | 2026-09-20 | measured — one run of `node scripts/spend.js` at `983823ad38f4ffd65cc11ce9581e24a799714d0c`, over every registry `lib/station.js`'s `discover()` finds: 311 session files scanned, 217 carrying no `spend` field, 0 unreadable, 94 priced against `lib/prices.js` (`verified: 2026-09-04`). The per-bucket shares are re-summed from that table's own printed columns by an awk program kept beside it, so they are rounded to the cent before summing; both runs and the program are in [evidence/2026-09-20-long-task-cost-composition/](reports/evidence/2026-09-20-long-task-cost-composition/), each file carrying its HEAD sha and a clean-tree line | In four buckets by request count, cache write falls 44.5% → 24.6% → 20.9% → 20.2% while cache read rises 31.0% → 50.0% → 59.6% → 61.4%, and the main session's share of cost falls 72.9% → 83.0% → 76.4% → 59.4%; uncached input is $0.87, 0.014% of the $6,377.56 total. Restricted to the 78 sessions under `F:\ymlab\fankeel` the same direction holds (46.7% → 21.8%, 28.7% → 57.0%, 69.2% → 53.8%), so the shift is not the four registries' differing work. Not an A/B: every priced session is the old mode, so this says what a long task costs, not what changing the mode does to one. The buckets are observed rather than assigned — a long task is long because it was hard. 78 of the 94 are this repository's own development. 7 carry a `<synthetic>` model `lib/prices.js` has no rate for, whose tokens are left out rather than counted as zero, so those totals are low. Only 34 carry a `version`, so nothing here compares one plugin version against another | `docs/README.md`, `docs/decisions/2026-09-20-survey-brain.md`, `docs/reports/2026-09-21-long-task-projection.md` |
| `LONG-TASK-PROJECTION-260921` | 把主控從 Opus 換成 Sonnet，在這個 registry 的長任務上會省多少，以及那個數字站在什麼假設上 | [reports/2026-09-21-long-task-projection.md](reports/2026-09-21-long-task-projection.md) | 2026-09-21 | projected, not measured — 沒有對照組，所以這是投影不是 A/B。兩支 evidence 腳本各自綁著產生它的 sha：逐站表由 `3886dc8` 上的那一版 `stages.js` 產生（`.fankeel/sessions` 180 個 entry、0 個讀不出來、79 個帶 `spend`、551 個 stage 列、0 列無報價），投影由同一個 commit 上的那一版 `project.js` 從那張表自己的欄位重算，不重掃 registry，並在 block 5 印出長任務 `old_usd` 的分佈與一對 A/B 的價錢（四分位為內插式）。逐站的錢由 `lib/registry.js` 的 `seriesOf` 與 `lib/prices.js` 的 `costOf` 算，不是這份報告算的；價格比由 `project.js` 從費率表逐分量算出而非寫死，四個分量都是 0.4；survey 站的兩個端點取自 [evidence/2026-09-20-survey-brain-ab/ab7-table.txt](reports/evidence/2026-09-20-survey-brain-ab/ab7-table.txt) 的逐模型未捨入列。額度那一格是 2026-09-20T16:30:41Z 的一次 statusline payload 捕捉，n=1，捕捉本身存在 [evidence/2026-09-21-long-task-projection/quota-capture-260920T163041Z.txt](reports/evidence/2026-09-21-long-task-projection/quota-capture-260920T163041Z.txt)，視窗的加總由 `stages.js` 的 block 3c 產生 | 43 筆長任務（`200-799` 22 筆、`800+` 21 筆）合計 $3,848.10，投影 $1,695.88–1,716.66，比值 0.4407–0.4461；破平衡點 k = 2.5052，Sonnet 要在沒有大腦的站上花超過 2.5 倍 token 才會變貴。survey 在長任務裡的中位數佔比只有 7.96%（最低 0.00%、最高 44.99%），所以 `SURVEY-BRAIN-AB-260920` 量到的打平是量在唯一有大腦的那一站的結果，再調大腦對長任務幾乎無效。ab7 的主控加權 token 倍數是 0.83 與 1.01（成本比 ÷ 0.4，因為四個分量的價格比相同），但那是一個把工作交給大腦的主控——build 與 verify 沒有任何量測，而那是整個投影唯一的支點。43 筆裡有 2 筆沒有 survey 站，整筆套價格比，比值因此恰好 0.4；單位是 session 不是 task。費率表在 2026-09-21 對過公布的價目，四個模型逐列相符，含 Fable 5.1 的 `cacheRead` 特例 0.25；一度懷疑 `[1m]` 後綴被丟掉會低估 1M context 的 session，查證後沒有這回事——Opus 5 的 context 本來就是 1M、就是標準費率，後綴也從沒進過 transcript。額度：5h 2%、7d 0%，而同一個 7 天視窗 registry 記到 $1,094.68、18.9 億 token（95.8% 快取讀取），所以計量不與它們成正比，一個整數讀數只給得出上界 | `docs/README.md`, `docs/reports/2026-09-21-quota-calibration.md` |
| `QUOTA-CALIBRATION-260921` | 同一組 5h／7d 視窗裡的兩次 statusline 讀數能不能給出一個百分點值多少錢，以及逐站花費帳的邊界戳記晚了多少 | [reports/2026-09-21-quota-calibration.md](reports/2026-09-21-quota-calibration.md) | 2026-09-21 | measured, n=2 readings — 兩個讀數不是兩個樣本，是同一組視窗的兩個時刻（5h `resets_at` 1789936800、7d `1790276400` 在兩次都相同），所以差值成立而速率只有兩段可比。四份輸出全部由同名腳本在 `43daef5` 上產生：`windows-at-43daef5.txt` 掃全機 427 份 transcript、逐分段給 request／token／錢與模型拆解，`basis-at-43daef5.txt` 拿兩次捕捉裡 Claude Code 自己的 `cost.total_cost_usd` 當對照，`drift-at-43daef5.txt` 把 66 個 session 分兩次桶，`sonnet-at-43daef5.txt` 量那趟 Sonnet 主控。錢一律由 `lib/prices.js` 的 `costOf` 算、視窗分桶一律由 `lib/usage.js` 自己的 `stageAt` 做，沒有任何費率或分桶規則在 evidence 裡重寫。整數讀數的 ±0.5 有帶進每一個區間，顯示為 0 的讀數不向下放寬。費率表那次讀取的引文在 `pricing-read-260921.txt`，六列與 `lib/prices.js` 逐列相符 | 5h：兩段的 $／點區間有交集（$7.64–$8.50），token／點區間完全不相交，所以排除了「數未加權 token」；但 API 等價美元本身就是加權後的 token，所以「按錢走」與「按價格加權的 token 走」在兩段資料上分不開，一整個視窗約 $764–$917 這個換算吃了前者的假設。7d：同一段給 $26.19–$45.84 一點，但視窗裡已累積的 $1,231.94 卻讀 0%，兩個讀數共用不了同一個速率，差至少 4.7 倍，所以 7d 的絕對水位不是這個帳號 transcript 流量的函數。每一個 $／點都帶一個**不對稱、n=2 的不確定**（不是一個 ±帶）：transcript 基準對 Claude Code 自己的數字，讀數 A 高 1.48%、讀數 B 低 10.69%，所以偏差兩個方向都有，不是下限——這一頁先前寫成下限，是審查在落地前推翻的。另外 7d 被排除的只是「從 `resets_at` 往前七天當起點、單一固定速率累積」這一個模型；起點挪晚就能調和兩個讀數。戳記偏移那組（66／280／13.9%／7.2%／183.1 分鐘）是點值，比對式還有一個未量化的殘餘缺陷。戳記偏移：66 個 session、280 個邊界，中位數 13.9% 的花費坐在錯的站，≥$50 的 31 個是 7.2%，最壞的單一邊界晚 183.1 分鐘；兩種分桶總額相等，是純重分配。方向上 `clock` **高估** survey（61 個裡 59 個變小，10.45%→8.22%），所以 `LONG-TASK-PROJECTION-260921` 的 7.96% 是高估、它那句結論被加強。Sonnet 主控一條臂 $6.9784、1,555 萬 token、每一輪都是 `claude-sonnet-5`，沒有對照臂所以給不出 `k` | `docs/README.md` |

## Consulted with no usable numbers

A report read for a figure and found to carry none gets a row here instead of
no row, so the next reader does not open it again to find that out.

| ID | What it was asked | Link | Checked | Evidence level | Scope | Cited by |
|---|---|---|---|---|---|---|
| `STYLE-TO-SUBAGENT-260907` | Does a `/config` output style reach a dispatched subagent's context? | [reports/2026-09-07-style-to-subagent.md](reports/2026-09-07-style-to-subagent.md) | 2026-09-07 | measured, operation-check only (4 runs plus 1 negative control, no usable figure) | Unanswered. In all four styled runs (2 control, 2 styled) the parent process itself never saw the style text either, so the subagent's `NO` cannot be read as "styles don't reach subagents" — only as "this operation (`claude -p --settings <file>`) never took effect." A follow-up run with a bogus style name confirmed the settings key is silently accepted without validating or applying, but could not distinguish "ignored" from "never read." Only one delivery channel (headless `--settings`) and one style (`fankeel-terse`, not a Claude Code built-in) were tried; the interactive `/config` path — the one this question is actually about — remains untried. | `docs/archive/2026-09-07-todo-thirteen.md`, `docs/README.md` |

[Back to the index](README.md)
