---
status: decision
last_verified: 2026-09-18
---

# TODO 四條 — 決策紀錄

使用者在 `/fankeel` 的選單上答「DO IT ALL」：`## Ready` 的〔caveman〕解除安裝，加上
`## Needs a decision` 的三條〔cuts〕。路線 survey → design → plan → build → verify →
land，沒有 audit。分支 `todo-caveman-cuts`：`todo-four` 這個名字已被 09-14 那一枝占用。

## 一、四條各自落在哪

| task | 定案 | 落在 |
|---|---|---|
| 1 | caveman 從使用者的 Claude Code 設定卸除：`claude plugin uninstall`、`claude plugin marketplace remove`；cache 目錄照使用者在 build 關卡的答覆刪掉 | 使用者的 `~/.claude/plugins/`，不在 repo；`TODO.md` |
| 2 | 沒人用的：刪 `lib/docs.js` 的 `bucketFor`；`scripts/stage-registry.js` 拿掉 `parseArgs` 與 `--root`、`--print` | `lib/docs.js`、`scripts/stage-registry.js` |
| 3 | 換內建：兩處 `HOME \|\| USERPROFILE` 改 `os.homedir()`；`lib/profile.js` 那處吃可注入的 `env`，不換。`walkSkillMd` 改用 `readdirSync` 的 `recursive` | `lib/registry.js`、`scripts/sessions.js`、`scripts/skills-check.js` |
| 4 | 可以更短：`completed`、`completions`、`fixes`、`planRange` 共用 `matching()`；`planRange` 仍取第一筆 | `lib/ledger.js` |
| 5 | 四個 POST 開頭的 nonce 檢查抽成 `formOf()`；`pad2` 換 `padStart` | `scripts/station.js`、`assets/station/station.js` |
| 6 | 三條〔cuts〕從 `TODO.md` 關掉，跑整套 `npm test` | `TODO.md` |

一條劃掉：`scripts/task.js` 的 `cmdGuard` 與 `cmdLand` 外殼相同，但抽成 helper 省 14 行、
helper 本身約 15 行，淨增一行；另外四處 `registry.update` 的外殼都不同，接不上。

不含 `TODO.md` 與 plan，13 個檔案 +69 −109。整套 1543/1543。

## 二、一個 implementer 的 `git stash` 丟掉鄰居的檔案

T2–T5 在同一棵工作樹上由一個 workflow 同時跑。T2 的 implementer 先 `git stash`，只從
stash 取回自己的兩個檔，再 `git stash drop`：T4 的 `lib/ledger.js` 與 T1 的 `TODO.md`
一起不見了。transcript 裡的 `Dropped refs/stash@{0} (4af0130…)` 留下了 sha，用
`git restore --source=4af0130 --worktree` 取回。brief 的 footer 寫了不准碰 index 與
`HEAD`，沒擋住它。T4 的 reviewer 剛好跨在 stash 那一刻，它的 `clean` 作廢重審，重審
仍是 `clean`。

## 三、verify 打回來的

- 第一輪 adversary 打掉七列。其中六列是證據缺口：主張成立，但證據沒跑或沒存，補跑後
  成立。
- 一列是真的：`planRange` 取第一筆沒有測試能失敗。對照組把 `matching(text, PLAN_RANGE)[0]`
  換成 `.pop()`，`tests/ledger.test.js` 照樣 60/0。回 build 補一個兩行 `Plan:` 的測試，
  同一個對照組下 60/1。它有自己的 commit、自己的 reviewer、ledger 上自己的 `Fix:` 行。
- 修正列的 adversary 又打掉一列：「reviewer 看過修正範圍」引的是 reviewer 的回覆，
  沒有存檔。從 subagent transcript 抽出它唯一一次 handback 存檔後，第二個 adversary
  回 `nothing`。

五個對照組在修正後全紅，每個都先斷言只替換一處，跑完用 `git checkout --` 還原。

## 四、T5 順手修的引用

T5 的 reviewer 發現 `tests/station-view.test.js` 有一行註解在這一枝之前就指錯行，
fixer 改成指向 `p.innerHTML` 那一行，併進 T5 的 commit。

## 不做

- 版號：這一枝不是發版，除非使用者說是。
- 推送：profile 是 `land merge, no push`。
- plan 裡引用 `lib/docs.js` 舊的 `module.exports` 行號，刪了十行後已超出檔尾：plan
  記的是改動前那一刻，封存後 docs-check 不查 archive。
